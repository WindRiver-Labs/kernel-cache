From d2166b2abff94be49413de1aa09b1f85703a9843 Mon Sep 17 00:00:00 2001
From: Shinya Kuribayashi <shinya.kuribayashi@necel.com>
Date: Mon, 7 Sep 2009 09:29:59 +0900
Subject: [PATCH 10/12] USB: EMMA EHCI/OHCI: Make needlessly global functions static

Signed-off-by: Shinya Kuribayashi <shinya.kuribayashi@necel.com>
---
 drivers/usb/host/ehci-emma.c |    7 ++++---
 drivers/usb/host/ohci-emma.c |    7 ++++---
 2 files changed, 8 insertions(+), 6 deletions(-)

diff --git a/drivers/usb/host/ehci-emma.c b/drivers/usb/host/ehci-emma.c
index 360aa3b..5ed135c 100644
--- a/drivers/usb/host/ehci-emma.c
+++ b/drivers/usb/host/ehci-emma.c
@@ -79,8 +79,8 @@ static void emma_stop_ehc(struct platform_device *pdev)
  *
  * Store this function in the HCD's struct platform_driver as probe().
  */
-int usb_ehci_emma_probe(const struct hc_driver *driver,
-			struct platform_device *pdev)
+static int usb_ehci_emma_probe(const struct hc_driver *driver,
+			       struct platform_device *pdev)
 {
 	struct usb_hcd	*hcd;
 	struct ehci_hcd *ehci;
@@ -173,7 +173,8 @@ int usb_ehci_emma_probe(const struct hc_driver *driver,
  *
  * Store this function in the HCD's struct platform_driver as remove().
  */
-void usb_ehci_emma_remove(struct usb_hcd *hcd, struct platform_device *dev)
+static void usb_ehci_emma_remove(struct usb_hcd *hcd,
+				 struct platform_device *dev)
 {
 	usb_remove_hcd(hcd);
 	emma_stop_ehc(dev);
diff --git a/drivers/usb/host/ohci-emma.c b/drivers/usb/host/ohci-emma.c
index 60b47ce..ce37019 100644
--- a/drivers/usb/host/ohci-emma.c
+++ b/drivers/usb/host/ohci-emma.c
@@ -78,8 +78,8 @@ static void emma_stop_hc(struct platform_device *pdev)
  *
  * Store this function in the HCD's struct platform_driver as probe().
  */
-int usb_hcd_emma_probe(const struct hc_driver *driver,
-		       struct platform_device *pdev)
+static int usb_hcd_emma_probe(const struct hc_driver *driver,
+			      struct platform_device *pdev)
 {
 	struct resource *res;
 	struct usb_hcd	*hcd;
@@ -167,7 +167,8 @@ int usb_hcd_emma_probe(const struct hc_driver *driver,
  *
  * Store this function in the HCD's struct platform_driver as remove().
  */
-void usb_hcd_emma_remove(struct usb_hcd *hcd, struct platform_device *pdev)
+static void usb_hcd_emma_remove(struct usb_hcd *hcd,
+				struct platform_device *pdev)
 {
 	usb_remove_hcd(hcd);
 	emma_stop_hc(pdev);
-- 
1.6.3.3

