From 74df5fb7538e6c74b5d078bc9d8f1483366b2420 Mon Sep 17 00:00:00 2001
From: "P. Sadik" <psadik@netlogicmicro.com>
Date: Tue, 31 Jan 2012 16:24:48 +0530
Subject: [PATCH 1344/1532] Fix compilation error due to the recent changes in
 API.

The function nlm_hal_write_mac_reg() takes 5 parameters. The function
nlm_xlp_nae_set_hwaddr() uses this and passed only 4 parameters. Fixed.
[Based on SDK 3.2]
Signed-off-by: Quanyang Wang <quanyang.wang@windriver.com>
---
 drivers/net/ethernet/broadcom/nae/xlp_nae.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/net/ethernet/broadcom/nae/xlp_nae.c b/drivers/net/ethernet/broadcom/nae/xlp_nae.c
index 262fa73..5fe3d7a 100755
--- a/drivers/net/ethernet/broadcom/nae/xlp_nae.c
+++ b/drivers/net/ethernet/broadcom/nae/xlp_nae.c
@@ -1308,13 +1308,13 @@ static int nlm_xlp_nae_set_hwaddr(struct net_device *dev, void *p)
 
 	if (priv->type == SGMII_IF)
 	{
-		nlm_hal_write_mac_reg(priv->block, priv->index, MAC_ADDR0_LO, 
+	  nlm_hal_write_mac_reg(priv->node,priv->block, priv->index, MAC_ADDR0_LO, 
 				(addr->sa_data[5] << 24) | 
 				(addr->sa_data[4] << 16) | 
 				(addr->sa_data[3] << 8) | 
 				(addr->sa_data[2]));
 
-		nlm_hal_write_mac_reg(priv->block, priv->index, MAC_ADDR0_HI, 
+	  nlm_hal_write_mac_reg(priv->node,priv->block, priv->index, MAC_ADDR0_HI, 
 				(addr->sa_data[1] << 24) | 
 				(addr->sa_data[0] << 16));
 	}
-- 
1.9.1

