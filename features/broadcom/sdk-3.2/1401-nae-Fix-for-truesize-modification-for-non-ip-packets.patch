From d37e23d548e69422da84654a7633884c36e35321 Mon Sep 17 00:00:00 2001
From: Hareesh R <hareeshr@broadcom.com>
Date: Mon, 8 Apr 2013 17:55:52 +0530
Subject: [PATCH 1401/1532] nae: Fix for truesize modification for non-ip
 packets

The truesize modification for non-ip packets can cause
invalid freein-fifo selection if ucore does not check
for non-ip packets.
[Based on SDK 3.2]
Signed-off-by: Quanyang Wang <quanyang.wang@windriver.com>
---
 drivers/net/ethernet/broadcom/nae/xlpge_rx.c | 2 --
 1 file changed, 2 deletions(-)

diff --git a/drivers/net/ethernet/broadcom/nae/xlpge_rx.c b/drivers/net/ethernet/broadcom/nae/xlpge_rx.c
index f4bd070..302bb7f 100644
--- a/drivers/net/ethernet/broadcom/nae/xlpge_rx.c
+++ b/drivers/net/ethernet/broadcom/nae/xlpge_rx.c
@@ -715,8 +715,6 @@ static inline void process_rx_packets(int cpu, unsigned int src_id,
 	skb->protocol = eth_type_trans(skb, pdev);
 
 	truesize = skb->truesize;
-	if(skb->protocol != htons(ETH_P_IP))
-		truesize = NLM_RX_JUMBO_BUF_SIZE + sizeof(struct sk_buff);
 
 #ifdef CONFIG_NLM_NET_OPTS
 	/* Pass the packet to Network stack */
-- 
1.9.1

