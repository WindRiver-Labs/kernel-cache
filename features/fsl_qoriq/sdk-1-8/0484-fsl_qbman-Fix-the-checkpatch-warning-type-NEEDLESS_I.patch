From 466671f19f2971a8a73cbb2e47689ac7ae12a324 Mon Sep 17 00:00:00 2001
From: Emil Medve <Emilian.Medve@Freescale.com>
Date: Wed, 8 Jan 2014 04:41:02 -0600
Subject: [PATCH 484/987] fsl_qbman: Fix the checkpatch warning type
 NEEDLESS_IF

[Original patch taken from QorIQ-SDK-V1.6-SOURCE-20140619-yocto.iso]

WARNING:NEEDLESS_IF: debugfs_remove_recursive(NULL) is safe this check is probably not required
WARNING:NEEDLESS_IF: kfree(NULL) is safe this check is probably not required

Signed-off-by: Emil Medve <Emilian.Medve@Freescale.com>
Change-Id: I4cc50940f601b6159f2e0b7b6305a44b72e2a8af
Reviewed-on: http://git.am.freescale.net:8181/7781
Tested-by: Review Code-CDREVIEW <CDREVIEW@freescale.com>
Reviewed-by: Haiying Wang <Haiying.Wang@freescale.com>
Reviewed-by: Geoff Thorpe <Geoff.Thorpe@freescale.com>
Reviewed-by: Emilian Medve <Emilian.Medve@freescale.com>
---
 drivers/staging/fsl_qbman/bman_debugfs.c | 3 +--
 drivers/staging/fsl_qbman/bman_high.c    | 6 ++----
 drivers/staging/fsl_qbman/qman_debugfs.c | 3 +--
 drivers/staging/fsl_qbman/qman_high.c    | 3 +--
 4 files changed, 5 insertions(+), 10 deletions(-)

diff --git a/drivers/staging/fsl_qbman/bman_debugfs.c b/drivers/staging/fsl_qbman/bman_debugfs.c
index 9a5dd97..9690934 100644
--- a/drivers/staging/fsl_qbman/bman_debugfs.c
+++ b/drivers/staging/fsl_qbman/bman_debugfs.c
@@ -104,8 +104,7 @@ static int __init bman_debugfs_module_init(void)
 	return 0;
 
 _return:
-	if (dfs_root)
-		debugfs_remove_recursive(dfs_root);
+	debugfs_remove_recursive(dfs_root);
 	return ret;
 }
 
diff --git a/drivers/staging/fsl_qbman/bman_high.c b/drivers/staging/fsl_qbman/bman_high.c
index 03ca52d..6efc2fa 100644
--- a/drivers/staging/fsl_qbman/bman_high.c
+++ b/drivers/staging/fsl_qbman/bman_high.c
@@ -284,8 +284,7 @@ fail_rcr_empty:
 fail_affinity:
 	free_irq(config->public_cfg.irq, portal);
 fail_irq:
-	if (portal->pools)
-		kfree(portal->pools);
+	kfree(portal->pools);
 fail_pools:
 	bm_isr_finish(__p);
 fail_isr:
@@ -647,8 +646,7 @@ err:
 	if (params->flags & BMAN_POOL_FLAG_DYNAMIC_BPID)
 		bman_release_bpid(bpid);
 	if (pool) {
-		if (pool->sp)
-			kfree(pool->sp);
+		kfree(pool->sp);
 		kfree(pool);
 	}
 	return NULL;
diff --git a/drivers/staging/fsl_qbman/qman_debugfs.c b/drivers/staging/fsl_qbman/qman_debugfs.c
index 3a53e66..26ab5ed 100644
--- a/drivers/staging/fsl_qbman/qman_debugfs.c
+++ b/drivers/staging/fsl_qbman/qman_debugfs.c
@@ -1589,8 +1589,7 @@ static int __init qman_debugfs_module_init(void)
 	return 0;
 
 _return:
-	if (dfs_root)
-		debugfs_remove_recursive(dfs_root);
+	debugfs_remove_recursive(dfs_root);
 	return ret;
 }
 
diff --git a/drivers/staging/fsl_qbman/qman_high.c b/drivers/staging/fsl_qbman/qman_high.c
index 6513525..1edd49c 100644
--- a/drivers/staging/fsl_qbman/qman_high.c
+++ b/drivers/staging/fsl_qbman/qman_high.c
@@ -530,8 +530,7 @@ fail_devalloc:
 		for (ret = 0; ret < num_ceetms; ret++)
 			kfree(portal->ccgrs[ret]);
 fail_ccgrs:
-	if (portal->cgrs)
-		kfree(portal->cgrs);
+	kfree(portal->cgrs);
 fail_cgrs:
 	qm_isr_finish(__p);
 fail_isr:
-- 
1.9.1

