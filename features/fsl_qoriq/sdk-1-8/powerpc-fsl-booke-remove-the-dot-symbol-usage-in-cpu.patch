From 0f3d08a96020ab3f700a1600638fde5a365bb52e Mon Sep 17 00:00:00 2001
From: Kevin Hao <kexin.hao@windriver.com>
Date: Mon, 28 Dec 2015 17:35:41 +0800
Subject: [PATCH 01/45] powerpc: fsl-booke: remove the dot symbol usage in cpu
 setup function

The binutils can branch to the correct text address when branching to
a function. So there is no need to explicitly to use a dot symbol in
this case. With this change we can also make sure that the same code
is also built for ppc32.

Signed-off-by: Kevin Hao <kexin.hao@windriver.com>
---
 arch/powerpc/kernel/cpu_setup_fsl_booke.S |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/arch/powerpc/kernel/cpu_setup_fsl_booke.S b/arch/powerpc/kernel/cpu_setup_fsl_booke.S
index a110b3f..eeae203 100644
--- a/arch/powerpc/kernel/cpu_setup_fsl_booke.S
+++ b/arch/powerpc/kernel/cpu_setup_fsl_booke.S
@@ -80,7 +80,7 @@ _GLOBAL(has_pw20_altivec_idle)
 #define PW20_WAIT_IDLE_BIT		50 /* 1ms, TB frequency is 41.66MHZ */
 _GLOBAL(setup_pw20_idle)
 	mflr	r10
-	bl	.has_pw20_altivec_idle
+	bl	has_pw20_altivec_idle
 	mtlr	r10
 	cmpwi	r3, 0
 	beq	2f
@@ -105,7 +105,7 @@ _GLOBAL(setup_pw20_idle)
 #define AV_WAIT_IDLE_BIT		50 /* 1ms, TB frequency is 41.66MHZ */
 _GLOBAL(setup_altivec_idle)
 	mflr	r10
-	bl	.has_pw20_altivec_idle
+	bl	has_pw20_altivec_idle
 	mtlr	r10
 	cmpwi	r3, 0
 	beq	2f
-- 
1.7.5.4

