From b98a4948a7e910903926fdb511dbeb8a9a6037b5 Mon Sep 17 00:00:00 2001
From: Jiang Lu <lu.jiang@windriver.com>
Date: Thu, 29 Aug 2013 13:47:51 +0800
Subject: [PATCH 2/4] dpaa:qman add kdump cleanup routine

We need release all fq before booting the dump kernel. So
register a crash shutdown hook to do this.

Signed-off-by: Jiang Lu <lu.jiang@windriver.com>
---
 drivers/staging/fsl_qbman/qman_config.c |   15 +++++++++++++++
 1 files changed, 15 insertions(+), 0 deletions(-)

diff --git a/drivers/staging/fsl_qbman/qman_config.c b/drivers/staging/fsl_qbman/qman_config.c
index ccef57b..87e453c 100644
--- a/drivers/staging/fsl_qbman/qman_config.c
+++ b/drivers/staging/fsl_qbman/qman_config.c
@@ -716,6 +716,17 @@ int qman_have_ccsr(void)
 	return qm ? 1 : 0;
 }
 
+#ifdef CONFIG_KEXEC
+#include <linux/kexec.h>
+
+void qman_release_fqid_all(void);
+
+void qman_crash_shutdown(void)
+{
+	qman_release_fqid_all();
+}
+#endif
+
 __init void qman_init_early(void)
 {
 	struct device_node *dn;
@@ -733,6 +744,10 @@ __init void qman_init_early(void)
 			BUG_ON(ret);
 		}
 	}
+
+#ifdef CONFIG_KEXEC
+	crash_shutdown_register(&qman_crash_shutdown);
+#endif
 }
 
 static void log_edata_bits(u32 bit_count)
-- 
1.7.5.4

