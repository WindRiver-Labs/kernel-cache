From 17447b0c018e8bba209c1cc6e5b43066addfa508 Mon Sep 17 00:00:00 2001
From: Tiejun Chen <tiejun.chen@windriver.com>
Date: Mon, 23 Apr 2012 14:33:52 +0800
Subject: [PATCH 15/38] powerpc/85xx: updated lbc binding document

Extracted from QorIQ-DPAA-SDK-v1.1-20111026-systembuilder.iso

Signed-off-by: Shaohui Xie <Shaohui.Xie@freescale.com>
Signed-off-by: Kumar Gala <galak@kernel.crashing.org>
---
 .../devicetree/bindings/powerpc/fsl/lbc.txt        |   55 +++++++++++++++-----
 1 files changed, 41 insertions(+), 14 deletions(-)

diff --git a/Documentation/devicetree/bindings/powerpc/fsl/lbc.txt b/Documentation/devicetree/bindings/powerpc/fsl/lbc.txt
index 3300fec..bb87818 100644
--- a/Documentation/devicetree/bindings/powerpc/fsl/lbc.txt
+++ b/Documentation/devicetree/bindings/powerpc/fsl/lbc.txt
@@ -2,6 +2,16 @@
 
 Properties:
 - name : Should be localbus
+- compatible :	should include "fsl,elbc" for processors which have all eLBC
+		interrupts routed to Int9(eLBC). should include
+		"fsl,p3041-rev1.0-elbc" for processors which have eLBC
+		error interrupts routed to Int0 while eLBC event interrupts
+		routed to Int9.
+- interrupts    : <interrupt mapping for eLBC IRQ>
+		should be one or two. one is for compatible "fsl,elbc", two
+		are for compatible "fsl,p3041-rev1.0-elbc". The first of the
+		two is for eLBC event interrupt, the second of the two is for
+		eLBC error interrupt.
 - #address-cells : Should be either two or three.  The first cell is the
                    chipselect number, and the remaining cells are the
                    offset into the chipselect.
@@ -10,26 +20,43 @@ Properties:
 - ranges : Each range corresponds to a single chipselect, and cover
            the entire access window as configured.
 
-Example:
-	localbus@f0010100 {
-		compatible = "fsl,mpc8272-localbus",
-			   "fsl,pq2-localbus";
+Example 1
+	/* for compatible "fsl,elbc" */
+	localbus@ffe124000 {
+		compatible = "fsl,p4080-elbc", "fsl,elbc", "simple-bus";
+		reg = <0xf 0xfe124000 0 0x1000>;
+		interrupts = <25 2 0 0>;
 		#address-cells = <2>;
 		#size-cells = <1>;
-		reg = <f0010100 40>;
 
-		ranges = <0 0 fe000000 02000000
-			  1 0 f4500000 00008000>;
+		ranges = <0 0 0xf 0xe8000000 0x08000000>;
 
 		flash@0,0 {
-			compatible = "jedec-flash";
-			reg = <0 0 2000000>;
-			bank-width = <4>;
-			device-width = <1>;
+			compatible = "cfi-flash";
+			reg = <0 0 0x08000000>;
+			bank-width = <2>;
+			device-width = <2>;
 		};
+	};
+
+Example 2
+	/* for compatible "fsl,p3041-rev1.0-elbc" */
+	localbus@ffe124000 {
+		compatible = "fsl,p3041-rev1.0-elbc", "simple-bus";
+		reg = <0xf 0xfe124000 0 0x1000>;
+		interrupts = <
+			25 2 0 0
+			16 2 1 19
+			>;
+		#address-cells = <2>;
+		#size-cells = <1>;
+
+		ranges = <0 0 0xf 0xe8000000 0x08000000>;
 
-		board-control@1,0 {
-			reg = <1 0 20>;
-			compatible = "fsl,mpc8272ads-bcsr";
+		flash@0,0 {
+			compatible = "cfi-flash";
+			reg = <0 0 0x08000000>;
+			bank-width = <2>;
+			device-width = <2>;
 		};
 	};
-- 
1.7.0.4

