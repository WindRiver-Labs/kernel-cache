See the drbd-tools Makefile in the wrll-wrlinux layer.

The kernel feature patch is generated by make drbd-tools.kernel

=============== < from: drbd-tools/Makefile > ===============

...
# WRS: To be used manually to generate the DRBD kernel patch
# when upreving the kernel feature. The resulting patch will
# be found under the drbd build directory:
# patch-linux-drbd-<drbd ver>
# You need an unpacked Linux tree in $(linux_SRC_DIR)
drbd-tools.kernel: drbd-tools.unpack
   @$(call echo_action, Generating Kernel Patch, DRDB)
      @cd $(drbd-tools_BUILD) && $(MAKE) clean && $(MAKE) KDIR="$(linux_SRC_DIR)" kernel-patch
...
