From b12b973e159439b65c8f8115a9f584472df16d2c Mon Sep 17 00:00:00 2001
From: Suman Anna <s-anna@ti.com>
Date: Mon, 28 Jul 2014 15:07:08 -0500
Subject: [PATCH 0988/1587] remoteproc: fix some checkpatch warnings

This patch fixes some of the existing checkpatch warnings in core
remoteproc code. The fixes are to the following warnings:
1. braces {} are not necessary for single statement blocks
2. missing space after return type
3. Unnecessary space after function pointer name

Signed-off-by: Suman Anna <s-anna@ti.com>
[zou: Original patch taken from
ti-sdk-am335x-evm-08.00.00.00-Linux-x86-Install.bin]
Signed-off-by: Cao Zou <cao.zou@windriver.com>
---
 drivers/remoteproc/remoteproc_core.c     |    3 +--
 drivers/remoteproc/remoteproc_internal.h |    4 ++--
 2 files changed, 3 insertions(+), 4 deletions(-)

diff --git a/drivers/remoteproc/remoteproc_core.c b/drivers/remoteproc/remoteproc_core.c
index 2c390f2..2d2231dc 100644
--- a/drivers/remoteproc/remoteproc_core.c
+++ b/drivers/remoteproc/remoteproc_core.c
@@ -891,9 +891,8 @@ static int rproc_fw_boot(struct rproc *rproc, const struct firmware *fw)
 
 	/* look for the resource table */
 	table = rproc_find_rsc_table(rproc, fw, &tablesz);
-	if (!table) {
+	if (!table)
 		goto clean_up;
-	}
 
 	/* Verify that resource table in loaded fw is unchanged */
 	if (rproc->table_csum != crc32(0, table, tablesz)) {
diff --git a/drivers/remoteproc/remoteproc_internal.h b/drivers/remoteproc/remoteproc_internal.h
index 70701a5..b905a14 100644
--- a/drivers/remoteproc/remoteproc_internal.h
+++ b/drivers/remoteproc/remoteproc_internal.h
@@ -35,10 +35,10 @@ struct rproc;
  * @get_boot_addr:	get boot address to entry point specified in firmware
  */
 struct rproc_fw_ops {
-	struct resource_table *(*find_rsc_table) (struct rproc *rproc,
+	struct resource_table * (*find_rsc_table)(struct rproc *rproc,
 						const struct firmware *fw,
 						int *tablesz);
-	struct resource_table *(*find_loaded_rsc_table)(struct rproc *rproc,
+	struct resource_table * (*find_loaded_rsc_table)(struct rproc *rproc,
 						const struct firmware *fw);
 	int (*load)(struct rproc *rproc, const struct firmware *fw);
 	int (*sanity_check)(struct rproc *rproc, const struct firmware *fw);
-- 
1.7.5.4

