From f7edadd0b8376ff7226be138d34069e714c97ae8 Mon Sep 17 00:00:00 2001
From: Thomas Gleixner <tglx@linutronix.de>
Date: Thu, 21 Jul 2011 21:05:33 +0200
Subject: [PATCH] net/wireless: Use WARN_ON_NORT()

The softirq counter is meaningless on RT, so the check triggers a
false positive.

Signed-off-by: Thomas Gleixner <tglx@linutronix.de>

diff --git a/net/mac80211/rx.c b/net/mac80211/rx.c
index 5793f75c5ffd..f6f8d9880873 100644
--- a/net/mac80211/rx.c
+++ b/net/mac80211/rx.c
@@ -3554,7 +3554,7 @@ void ieee80211_rx(struct ieee80211_hw *hw, struct sk_buff *skb)
 	struct ieee80211_supported_band *sband;
 	struct ieee80211_rx_status *status = IEEE80211_SKB_RXCB(skb);
 
-	WARN_ON_ONCE(softirq_count() == 0);
+	WARN_ON_ONCE_NONRT(softirq_count() == 0);
 
 	if (WARN_ON(status->band >= IEEE80211_NUM_BANDS))
 		goto drop;
-- 
2.5.0

