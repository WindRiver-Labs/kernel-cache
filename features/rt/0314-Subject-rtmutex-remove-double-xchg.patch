From e45a251edb230b38568b0deb10626258a46dfd90 Mon Sep 17 00:00:00 2001
From: Steven Rostedt <srostedt@redhat.com>
Date: Fri, 5 Sep 2008 00:23:50 -0400
Subject: [PATCH] Subject: rtmutex - remove double xchg
 No reason to update current if we are running. We'll do that when we exit
 the loop.

Signed-off-by: Steven Rostedt <srostedt@redhat.com>
---
 kernel/rtmutex.c |    2 --
 1 files changed, 0 insertions(+), 2 deletions(-)

diff --git a/kernel/rtmutex.c b/kernel/rtmutex.c
index dd4ff53..a12a256 100644
--- a/kernel/rtmutex.c
+++ b/kernel/rtmutex.c
@@ -749,8 +749,6 @@ rt_spin_lock_slowlock(struct rt_mutex *lock)
 		update_current(TASK_UNINTERRUPTIBLE, &saved_state);
 		if (waiter.task)
 			schedule_rt_mutex(lock);
-		else
-			update_current(TASK_RUNNING_MUTEX, &saved_state);
 
 		spin_lock_irqsave(&lock->wait_lock, flags);
 		current->flags |= saved_flags;
-- 
1.6.0.90.g436ed

