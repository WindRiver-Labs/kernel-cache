From: rt-users <linux-rt-users@vger.kernel.org>  
Subject: enable rcu preempt boost by default

This is an import of rcu-preempt-boost-default.patch from the RT
patch series.
---
 kernel/Kconfig.preempt |    1 +
 1 files changed, 1 insertions(+), 0 deletions(-)

diff --git a/kernel/Kconfig.preempt b/kernel/Kconfig.preempt
index e8b6489..d86f0a7 100644
--- a/kernel/Kconfig.preempt
+++ b/kernel/Kconfig.preempt
@@ -69,6 +69,7 @@ config PREEMPT_RCU
 config PREEMPT_RCU_BOOST
 	bool "Enable priority boosting of RCU read-side critical sections"
 	depends on PREEMPT_RCU
+	default y if PREEMPT_RT
 	help
 	  This option permits priority boosting of RCU read-side critical
 	  sections tat have been preempted and a RT process is waiting
