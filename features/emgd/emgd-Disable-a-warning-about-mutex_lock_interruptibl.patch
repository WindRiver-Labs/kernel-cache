From 25d1416ec8ac8e530a691b3d402c552ed0e47806 Mon Sep 17 00:00:00 2001
From: Donn Seeley <donn.seeley@windriver.com>
Date: Tue, 12 Jun 2012 10:45:28 -0600
Subject: [PATCH 12/19] emgd: Disable a warning about
 mutex_lock_interruptible().

Apparently EMGD never checks the return value from
mutex_lock_interruptible().  Normally EMGD captures the return value
and ignores it, so that no compiler warning gets generated, but someone
missed a case.

Signed-off-by: Donn Seeley <donn.seeley@windriver.com>
---
 .../gpu/drm/emgd/emgd/video/overlay/tnc/ovl2_tnc.c |    3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/drivers/gpu/drm/emgd/emgd/video/overlay/tnc/ovl2_tnc.c b/drivers/gpu/drm/emgd/emgd/video/overlay/tnc/ovl2_tnc.c
index 1c577c4..0286dae 100644
--- a/drivers/gpu/drm/emgd/emgd/video/overlay/tnc/ovl2_tnc.c
+++ b/drivers/gpu/drm/emgd/emgd/video/overlay/tnc/ovl2_tnc.c
@@ -268,6 +268,7 @@ unsigned int ovl2_send_instr_tnc(
 	unsigned long tmp;
 	inter_module_dispatch_t *md;
 	platform_context_tnc_t * platform;
+	int ret;
 
 
 	EMGD_TRACE_ENTER;
@@ -325,7 +326,7 @@ unsigned int ovl2_send_instr_tnc(
 	platform = (platform_context_tnc_t *)display->context->
 					platform_context;
 	if(md && md->set_flip_pending){
-		OS_PTHREAD_MUTEX_LOCK(&platform->flip_mutex);
+		ret = OS_PTHREAD_MUTEX_LOCK(&platform->flip_mutex);
 		md->set_flip_pending(MMIO(display), 0x71024);
 		OS_PTHREAD_MUTEX_UNLOCK(&platform->flip_mutex);
 	}
-- 
1.7.9.7

