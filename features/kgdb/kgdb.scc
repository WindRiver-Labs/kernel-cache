# TODO: this will be: scc_src <foo> <branch>

if [ -e /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-edge/git/linux-2.6-kgdb ]; then
  git fetch /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-edge/git/linux-2.6-kgdb +refs/remotes/origin/for_linus:kgdb_merge
else
   git show-ref --quiet --verify -- "refs/heads/kgdb_merge";
   if [ $? -eq 1 ]; then 
      git branch kgdb_merge wrs_base
   fi
fi
