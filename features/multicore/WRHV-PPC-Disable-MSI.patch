From 61d68607daba697cc9a33e47ef34e7c659904663 Mon Sep 17 00:00:00 2001
From: Tiejun Chen <tiejun.chen@windriver.com>
Date: Wed, 12 May 2010 16:39:51 +0800
Subject: [PATCH 04/14] WRHV/PPC: Disable MSI

We have to disable MSI to adopt legacy interrupt mode
for PCIE devices since the BootROM always use MSI by default.

Signed-off-by: Tiejun Chen <tiejun.chen@windriver.com>
---
 arch/powerpc/kernel/vbi/wrhv.c |   18 ++++++++++++++++++
 1 files changed, 18 insertions(+), 0 deletions(-)

diff --git a/arch/powerpc/kernel/vbi/wrhv.c b/arch/powerpc/kernel/vbi/wrhv.c
index 6683ffb..3cede35 100644
--- a/arch/powerpc/kernel/vbi/wrhv.c
+++ b/arch/powerpc/kernel/vbi/wrhv.c
@@ -1138,4 +1138,22 @@ int ppc_get_pci_intr_wrhv(struct pci_dev *dev)
 
 	return -1;
 }
+
+/* On PPC we have to disable MSI firstly to adopt the legacy interrupt 
+ * since the BootROM with supporting PCIE always use MSI way. 
+ */
+void pci_msi_disable(struct pci_dev *dev)
+{
+	u16 control;
+	int pos = pci_find_capability(dev, PCI_CAP_ID_MSI);
+
+	if (pos) {
+		pci_read_config_word(dev, pos + PCI_MSI_FLAGS, &control);
+		if (control & PCI_MSI_FLAGS_ENABLE) {
+			control &= ~PCI_MSI_FLAGS_ENABLE;
+			pci_write_config_word(dev, pos + PCI_MSI_FLAGS, control);
+		}
+	}
+}
+DECLARE_PCI_FIXUP_FINAL(PCI_ANY_ID, PCI_ANY_ID, pci_msi_disable);
 #endif
-- 
1.6.5.2

