Kmemleak notes: 

1. Only x86 is validated.

2. How to build this feature
This feature is not enabled by default, enable use of this feature in your configure
line as per below:
	path_to_wrlinux-3.0/wrlinux/configure \
	--enable-rootfs=glibc_std --enable-board=common_pc \
	--enable-kernel=standard+features/kmemleak/kmemleak-enable.scc
