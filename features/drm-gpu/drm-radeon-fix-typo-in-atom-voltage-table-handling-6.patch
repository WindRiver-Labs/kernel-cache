From 5eee419c40b1a966ffeed57d5d887d5ba96ed1bd Mon Sep 17 00:00:00 2001
From: Alex Deucher <alexander.deucher@amd.com>
Date: Wed, 27 Mar 2013 20:34:19 -0400
Subject: [PATCH 533/886] drm/radeon: fix typo in atom voltage table handling
 (6xx-ni)

commit 7a80c2c9a957b1ab056fac235140ebd6c43d9831 upstream

Signed-off-by: Alex Deucher <alexander.deucher@amd.com>
---
 drivers/gpu/drm/radeon/radeon_atombios.c |    8 ++++----
 1 file changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/gpu/drm/radeon/radeon_atombios.c b/drivers/gpu/drm/radeon/radeon_atombios.c
index adfec6b..f992fc6 100644
--- a/drivers/gpu/drm/radeon/radeon_atombios.c
+++ b/drivers/gpu/drm/radeon/radeon_atombios.c
@@ -3155,7 +3155,7 @@ radeon_atom_is_voltage_gpio(struct radeon_device *rdev,
 				break;
 			case 2:
 				num_indices = (size - sizeof(ATOM_COMMON_TABLE_HEADER)) /
-					sizeof(ATOM_VOLTAGE_OBJECT_INFO_V2);
+					sizeof(ATOM_VOLTAGE_OBJECT_V2);
 
 				for (i = 0; i < num_indices; i++) {
 					if ((voltage_info->v2.asVoltageObj[i].ucVoltageType == voltage_type) &&
@@ -3236,7 +3236,7 @@ int radeon_atom_get_max_voltage(struct radeon_device *rdev,
 			break;
 		case 2:
 			num_indices = (size - sizeof(ATOM_COMMON_TABLE_HEADER)) /
-				sizeof(ATOM_VOLTAGE_OBJECT_INFO_V2);
+				sizeof(ATOM_VOLTAGE_OBJECT_V2);
 
 			for (i = 0; i < num_indices; i++) {
 				if (voltage_info->v2.asVoltageObj[i].ucVoltageType == voltage_type) {
@@ -3292,7 +3292,7 @@ int radeon_atom_get_min_voltage(struct radeon_device *rdev,
 			break;
 		case 2:
 			num_indices = (size - sizeof(ATOM_COMMON_TABLE_HEADER)) /
-				sizeof(ATOM_VOLTAGE_OBJECT_INFO_V2);
+				sizeof(ATOM_VOLTAGE_OBJECT_V2);
 
 			for (i = 0; i < num_indices; i++) {
 				if (voltage_info->v2.asVoltageObj[i].ucVoltageType == voltage_type) {
@@ -3411,7 +3411,7 @@ int radeon_atom_get_voltage_table(struct radeon_device *rdev,
 				return -EINVAL;
 			case 2:
 				num_indices = (size - sizeof(ATOM_COMMON_TABLE_HEADER)) /
-					sizeof(ATOM_VOLTAGE_OBJECT_INFO_V2);
+					sizeof(ATOM_VOLTAGE_OBJECT_V2);
 
 				for (i = 0; i < num_indices; i++) {
 					if (voltage_info->v2.asVoltageObj[i].ucVoltageType == voltage_type) {
-- 
1.7.9.5

