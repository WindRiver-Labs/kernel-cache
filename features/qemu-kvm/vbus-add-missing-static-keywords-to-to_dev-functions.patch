From 5204e242b6d99e6bdf5e5e2bf882212a699cc176 Mon Sep 17 00:00:00 2001
From: Ira W. Snyder <iws@ovro.caltech.edu>
Date: Wed, 14 Oct 2009 20:07:29 -0400
Subject: [PATCH 070/119] vbus: add missing static keywords to to_dev() functions

Both the PCI-bridge and null-connector have functions named to_dev(). Both
functions were not declared as static, which caused a link error. Add the
static keyword to both functions to avoid the error.

Signed-off-by: Ira W. Snyder <iws@ovro.caltech.edu>
Signed-off-by: Gregory Haskins <ghaskins@novell.com>
---
 drivers/vbus/pci-bridge.c     |    2 +-
 kernel/vbus/connectors/null.c |    2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/vbus/pci-bridge.c b/drivers/vbus/pci-bridge.c
index fcde495..f32afcb 100644
--- a/drivers/vbus/pci-bridge.c
+++ b/drivers/vbus/pci-bridge.c
@@ -124,7 +124,7 @@ vbus_pci_buscall(unsigned long nr, void *data, unsigned long len)
 	return ret;
 }
 
-struct vbus_pci_device *
+static struct vbus_pci_device *
 to_dev(struct vbus_device_proxy *vdev)
 {
 	return container_of(vdev, struct vbus_pci_device, vdev);
diff --git a/kernel/vbus/connectors/null.c b/kernel/vbus/connectors/null.c
index 0661a2b..7142de7 100644
--- a/kernel/vbus/connectors/null.c
+++ b/kernel/vbus/connectors/null.c
@@ -89,7 +89,7 @@ struct guest_device {
 	struct vbus_device_proxy dev;
 };
 
-struct guest_device *
+static struct guest_device *
 to_dev(struct vbus_device_proxy *dev)
 {
 	return container_of(dev, struct guest_device, dev);
-- 
1.6.5.2

