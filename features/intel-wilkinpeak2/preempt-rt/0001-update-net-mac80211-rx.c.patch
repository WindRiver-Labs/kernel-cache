From 5e1b31e071c93bc3faadd59cd032b2390404fb71 Mon Sep 17 00:00:00 2001
From: Yunguo Wei <yunguo.wei@windriver.com>
Date: Fri, 9 Aug 2013 11:46:12 +0800
Subject: [PATCH] revert 35ddf2eef8 related to mac80211 subsystem

Commit 35ddf2eef8 introduced the changed to net/mac80211/rx.c,
and it is blocking applying the common net/mac80211 patch. So revert
35ddf2eef8's change to net/mac80211/rx.c, and that change will be
re-added after the common patch is applied.

Signed-off-by: Yunguo Wei <yunguo.wei@windriver.com>
---
 net/mac80211/rx.c | 2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/net/mac80211/rx.c b/net/mac80211/rx.c
index c788dfc..8ce9feb 100644
--- a/net/mac80211/rx.c
+++ b/net/mac80211/rx.c
@@ -3056,7 +3056,7 @@ void ieee80211_rx(struct ieee80211_hw *hw, struct sk_buff *skb)
 	struct ieee80211_supported_band *sband;
 	struct ieee80211_rx_status *status = IEEE80211_SKB_RXCB(skb);
 
-	WARN_ON_ONCE_NONRT(softirq_count() == 0);
+	WARN_ON_ONCE(softirq_count() == 0);
 
 	if (WARN_ON(status->band < 0 ||
 		    status->band >= IEEE80211_NUM_BANDS))
-- 
1.8.1.2

