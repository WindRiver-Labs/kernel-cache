# disable ftrace for mips by default. If you want
# this for a mips board, enable it in the BSP and
# use at your own risk. The support is known to
# cause module loading problems.

if [ ! "$KERNEL_ARCH" = "mips" ]; then
    kconf non-hardware ftrace.cfg
fi
