From 90612706cf54263bd09f4d10528a6b8544447d50 Mon Sep 17 00:00:00 2001
From: Dave Lerner <dave.lerner@windriver.com>
Date: Wed, 9 Jun 2010 15:22:49 -0700
Subject: [PATCH] oprofile: fix mips backtrace logic following branch

Fix the next pc start address calculation by a decrement of one instruction
like the other offset adjustements in the logic, because the next iteration
examines the pc address at pcCurStart + 1.

Signed-off-by: Dave Lerner <dave.lerner@windriver.com>
---
 arch/mips/oprofile/mips_crawl.c |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/arch/mips/oprofile/mips_crawl.c b/arch/mips/oprofile/mips_crawl.c
index ba03e5e..6b84b78 100644
--- a/arch/mips/oprofile/mips_crawl.c
+++ b/arch/mips/oprofile/mips_crawl.c
@@ -1162,7 +1162,7 @@ follow_branch:
 				    ("  %p: arbitrary branch [delay] by %ld to %p\n",
 				     pcCur - 1, offset, pcCur + offset);
 
-				pcCurStart = pcCur + offset;
+				pcCurStart = pcCur + offset - 1;
 				i = 0;
 
 				/* we limit the number of arbitrary branches taken */
-- 
1.6.5.2

