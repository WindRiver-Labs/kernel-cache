---
 include/linux/bottom_half.h |    2 +-
 kernel/softirq.c            |    4 ++++
 2 files changed, 5 insertions(+), 1 deletions(-)

diff --git a/include/linux/bottom_half.h b/include/linux/bottom_half.h
index 2e0e961..37dc356 100644
--- a/include/linux/bottom_half.h
+++ b/include/linux/bottom_half.h
@@ -1,7 +1,7 @@
 #ifndef _LINUX_BH_H
 #define _LINUX_BH_H
 
-#ifdef CONFIG_PREEMPT_RT
+#ifdef CONFIG_PREEMPT_HARDIRQS
 # define local_bh_disable()		do { } while (0)
 # define __local_bh_disable(ip)		do { } while (0)
 # define _local_bh_enable()		do { } while (0)
diff --git a/kernel/softirq.c b/kernel/softirq.c
index bfd7d95..09d45d7 100644
--- a/kernel/softirq.c
+++ b/kernel/softirq.c
@@ -146,6 +146,8 @@ static void trigger_softirqs(void)
 	}
 }
 
+#ifndef CONFIG_PREEMPT_HARDIRQS
+
 /*
  * This one is for softirq.c-internal use,
  * where hardirqs are disabled legitimately:
@@ -237,6 +239,8 @@ void local_bh_enable_ip(unsigned long ip)
 }
 EXPORT_SYMBOL(local_bh_enable_ip);
 
+#endif	/* CONFIG_PREEMPT_HARDIRQS */
+
 /*
  * We restart softirq processing MAX_SOFTIRQ_RESTART times,
  * and we fall back to softirqd after that.
