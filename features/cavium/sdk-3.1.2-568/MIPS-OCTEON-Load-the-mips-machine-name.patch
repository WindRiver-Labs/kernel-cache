From 148214f616d4b66a6c952d036b2eef5b68e36556 Mon Sep 17 00:00:00 2001
From: Bin Jiang <bin.jiang@windriver.com>
Date: Thu, 4 Dec 2014 10:59:36 +0800
Subject: [PATCH 4/4] MIPS: OCTEON: Load the mips machine name

The mips machine name is used by cpuinfo.

Signed-off-by: Bin Jiang <bin.jiang@windriver.com>
---
 arch/mips/cavium-octeon/setup.c |    5 +++++
 1 files changed, 5 insertions(+), 0 deletions(-)

diff --git a/arch/mips/cavium-octeon/setup.c b/arch/mips/cavium-octeon/setup.c
index ddf2528..77d4d99 100644
--- a/arch/mips/cavium-octeon/setup.c
+++ b/arch/mips/cavium-octeon/setup.c
@@ -1261,6 +1261,8 @@ extern const char __dtb_octeon_3xxx_begin;
 extern const char __dtb_octeon_3xxx_end;
 extern const char __dtb_octeon_68xx_begin;
 extern const char __dtb_octeon_68xx_end;
+extern int __init early_init_dt_scan_model(unsigned long node, const char *uname,
+					int depth, void *data);
 void __init device_tree_init(void)
 {
 	int dt_size;
@@ -1290,6 +1292,9 @@ void __init device_tree_init(void)
 		panic("Could not allocate initial_boot_params\n");
 	memcpy(initial_boot_params, fdt, dt_size);
 
+	/* try to load the mips machine name */
+	of_scan_flat_dt(early_init_dt_scan_model, NULL);
+
 	if (do_prune) {
 		octeon_prune_device_tree();
 		pr_info("Using internal Device Tree.\n");
-- 
1.7.5.4

