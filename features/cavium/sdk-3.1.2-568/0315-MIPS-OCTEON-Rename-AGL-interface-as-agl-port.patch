From ab40a125df8ac55df6da6a1870b1f60111e82ca8 Mon Sep 17 00:00:00 2001
From: Chandrakala Chavva <cchavva@caviumnetworks.com>
Date: Sat, 28 Sep 2013 10:29:21 -0700
Subject: [PATCH 315/974] MIPS: OCTEON: Rename AGL interface as agl port.

Signed-off-by: Chandrakala Chavva <cchavva@caviumnetworks.com>
[Original patch taken from Cavium SDK 3.1.2-568]
Signed-off-by: Quanyang Wang <quanyang.wang@windriver.com>
---
 drivers/net/ethernet/octeon/ethernet.c | 3 ++-
 1 file changed, 2 insertions(+), 1 deletion(-)

diff --git a/drivers/net/ethernet/octeon/ethernet.c b/drivers/net/ethernet/octeon/ethernet.c
index 0f99bb2..dff2a5f 100644
--- a/drivers/net/ethernet/octeon/ethernet.c
+++ b/drivers/net/ethernet/octeon/ethernet.c
@@ -782,6 +782,7 @@ static int cvm_oct_get_port_status(struct device_node *pip)
 				cvmx_helper_set_1000x_mode(i, j, false);
 				break;
 			case CVMX_HELPER_INTERFACE_MODE_SGMII:
+			case CVMX_HELPER_INTERFACE_MODE_AGL:
 			{
 				struct device_node *n;
 				if (cvm_oct_node_for_port(pip, i, j) != NULL)
@@ -998,7 +999,7 @@ static int cvm_oct_probe(struct platform_device *pdev)
 				dev->netdev_ops = priv->tx_lockless ?
 					&cvm_oct_sgmii_lockless_netdev_ops : &cvm_oct_sgmii_netdev_ops;
 				priv->gmx_base = CVMX_AGL_GMX_RXX_INT_REG(0);
-				strcpy(dev->name, "eth%d");
+				strcpy(dev->name, "agl%d");
 				break;
 
 			case CVMX_HELPER_INTERFACE_MODE_SPI:
-- 
2.6.2

