From b8eff5a1c63123fee06068a313f9467e1c7003bb Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Fri, 14 Jun 2013 15:08:09 -0700
Subject: [PATCH 148/974] gpio: gpio-octeon: Clean up code.

Signed-off-by: David Daney <david.daney@cavium.com>
[Original patch taken from Cavium SDK 3.1.2-568]
Signed-off-by: Quanyang Wang <quanyang.wang@windriver.com>
---
 drivers/gpio/gpio-octeon.c | 12 +++---------
 1 file changed, 3 insertions(+), 9 deletions(-)

diff --git a/drivers/gpio/gpio-octeon.c b/drivers/gpio/gpio-octeon.c
index 0cf8680..f5bd127 100644
--- a/drivers/gpio/gpio-octeon.c
+++ b/drivers/gpio/gpio-octeon.c
@@ -3,7 +3,7 @@
  * License.  See the file "COPYING" in the main directory of this archive
  * for more details.
  *
- * Copyright (C) 2011,2012 Cavium Inc.
+ * Copyright (C) 2011, 2012 Cavium Inc.
  */
 
 #include <linux/platform_device.h>
@@ -15,9 +15,6 @@
 #include <asm/octeon/octeon.h>
 #include <asm/octeon/cvmx-gpio-defs.h>
 
-#define DRV_VERSION "1.0"
-#define DRV_DESCRIPTION "Cavium Inc. OCTEON GPIO Driver"
-
 #define RX_DAT 0x80
 #define TX_SET 0x88
 #define TX_CLEAR 0x90
@@ -60,7 +57,6 @@ static int octeon_gpio_dir_out(struct gpio_chip *chip, unsigned offset,
 	struct octeon_gpio *gpio = container_of(chip, struct octeon_gpio, chip);
 	union cvmx_gpio_bit_cfgx cfgx;
 
-
 	octeon_gpio_set(chip, offset, value);
 
 	cfgx.u64 = 0;
@@ -106,7 +102,6 @@ static int octeon_gpio_probe(struct platform_device *pdev)
 	gpio->register_base = (u64)devm_ioremap(&pdev->dev, res_mem->start,
 						resource_size(res_mem));
 
-
 	pdev->dev.platform_data = chip;
 	chip->label = "octeon-gpio";
 	chip->dev = &pdev->dev;
@@ -122,7 +117,7 @@ static int octeon_gpio_probe(struct platform_device *pdev)
 	if (err)
 		goto out;
 
-	dev_info(&pdev->dev, "version: " DRV_VERSION "\n");
+	dev_info(&pdev->dev, "OCTEON GPIO\n");
 out:
 	return err;
 }
@@ -153,7 +148,6 @@ static struct platform_driver octeon_gpio_driver = {
 
 module_platform_driver(octeon_gpio_driver);
 
-MODULE_DESCRIPTION(DRV_DESCRIPTION);
+MODULE_DESCRIPTION("Cavium Inc. OCTEON GPIO Driver");
 MODULE_AUTHOR("David Daney");
 MODULE_LICENSE("GPL");
-MODULE_VERSION(DRV_VERSION);
-- 
2.6.2

