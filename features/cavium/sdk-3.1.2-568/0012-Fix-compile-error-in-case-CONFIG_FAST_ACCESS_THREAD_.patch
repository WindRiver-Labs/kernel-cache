From dc145c2c29ef675f781a7857788fe1355eda431d Mon Sep 17 00:00:00 2001
From: Andreas Herrmann <andreas.herrmann@caviumnetworks.com>
Date: Tue, 28 Oct 2014 15:59:53 +0100
Subject: [PATCH 012/184] Fix compile error in case
 CONFIG_FAST_ACCESS_THREAD_REGISTER=n

Source: Cavium Networks, Inc.
MR: 00000
Type: Integration
Disposition: Merged from Octeon Tree
ChangeID: 305193b8fa730c68113a8117676312276be52cad
Description:

Signed-off-by: Andreas Herrmann <andreas.herrmann@caviumnetworks.com>
Signed-off-by: Corey Minyard <cminyard@mvista.com>
[Original patch taken from octeon-linux-kernel-patches-SDK-3.1.2-release]
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 arch/mips/mm/tlbex.c | 15 ++++++++++++---
 1 file changed, 12 insertions(+), 3 deletions(-)

diff --git a/arch/mips/mm/tlbex.c b/arch/mips/mm/tlbex.c
index 5cb7256..c83a8ff 100644
--- a/arch/mips/mm/tlbex.c
+++ b/arch/mips/mm/tlbex.c
@@ -37,6 +37,17 @@
 
 static int __cpuinitdata mips_xpa_disabled;
 
+#ifdef CONFIG_FAST_ACCESS_TO_THREAD_POINTER
+#define restore_fast_access_to_thread_pointer(t)		\
+	do {							\
+		/* K0 = thread pointer */			\
+		UASM_i_LW(t, K0, FAST_ACCESS_THREAD_OFFSET, 0);	\
+	} while (0)
+#else
+#define restore_fast_access_to_thread_pointer(t)		\
+	do { } while (0)
+#endif
+
 static int __init xpa_disable(char *s)
 {
 	mips_xpa_disabled = 1;
@@ -1940,9 +1951,7 @@ build_r4000_tlbchange_handler_tail(u32 **p, struct uasm_label **l,
 	build_tlb_write_entry(p, l, r, tlb_indexed);
 	uasm_l_leave(l, *p);
 	build_restore_work_registers(p);
-#ifdef CONFIG_FAST_ACCESS_TO_THREAD_POINTER
-	UASM_i_LW(p, K0, FAST_ACCESS_THREAD_OFFSET, 0);  /* K0 = thread ptr */
-#endif
+	restore_fast_access_to_thread_pointer(p);
 	uasm_i_eret(p); /* return from trap */
 
 #ifdef CONFIG_64BIT
-- 
1.9.1

