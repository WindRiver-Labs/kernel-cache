From dc77ca8a74391135648529c1730f73f7cc0f7b7a Mon Sep 17 00:00:00 2001
From: Vinita Gupta <vgupta@caviumnetworks.com>
Date: Fri, 7 Feb 2014 09:25:00 -0800
Subject: [PATCH 013/202] usb: xhci-octeon: replaced cvmx_wait with
 udelay/ndelay

Signed-off-by: Vinita Gupta <vgupta@caviumnetworks.com>
[Original patch taken from Cavium SDK 3.1.1-544]
Signed-off-by: Bin Jiang <bin.jiang@windriver.com>
---
 drivers/usb/host/xhci-octeon.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/usb/host/xhci-octeon.c b/drivers/usb/host/xhci-octeon.c
index f8f4a4f..e153500 100644
--- a/drivers/usb/host/xhci-octeon.c
+++ b/drivers/usb/host/xhci-octeon.c
@@ -204,7 +204,7 @@ static void octeon3_usb_clocks_start(struct platform_device *pdev, int index)
 	cvmx_write_csr(CVMX_USBDRDX_UCTL_CTL(index), uctl_ctl.u64);
 
 	/* Step ??*/
-	cvmx_wait(2000);
+	udelay(10);
 
 	/* Step 6a-6d & 7: Reference clock configuration */
 	uctl_ctl.u64 = cvmx_read_csr(CVMX_USBDRDX_UCTL_CTL(index));
@@ -250,7 +250,7 @@ static void octeon3_usb_clocks_start(struct platform_device *pdev, int index)
 	cvmx_write_csr(CVMX_USBDRDX_UCTL_CTL(index), uctl_ctl.u64);
 
 	/* Step 9c*/
-	cvmx_wait(10);
+	ndelay(200);
 
 	/* Step 10*/
 	uctl_ctl.u64 = cvmx_read_csr(CVMX_USBDRDX_UCTL_CTL(index));
-- 
1.8.2.1

