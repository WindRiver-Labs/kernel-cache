From ea5fbd83444315ee1a0c59d1cdc596259b80d3cd Mon Sep 17 00:00:00 2001
From: Corey Minyard <cminyard@mvista.com>
Date: Fri, 7 Sep 2012 13:00:33 -0500
Subject: [PATCH 157/337] octeon: Add ifdefs around xkphys usage

Based On SDK 3.0.0-482

xkphys is Octeon-only, so ifdef the code using it.  Otherwise the build
of other MIPS platforms is broken.

Signed-off-by: Corey Minyard <cminyard@mvista.com>
Signed-off-by: Jack Tan <jack.tan@windriver.com>
---
 arch/mips/kernel/syscall.c |    4 ++++
 1 files changed, 4 insertions(+), 0 deletions(-)

diff --git a/arch/mips/kernel/syscall.c b/arch/mips/kernel/syscall.c
index 3cb5bc6..142e2fe 100644
--- a/arch/mips/kernel/syscall.c
+++ b/arch/mips/kernel/syscall.c
@@ -44,8 +44,10 @@
 #include <asm/uaccess.h>
 #include <asm/switch_to.h>
 
+#ifdef CONFIG_CPU_CAVIUM_OCTEON
 extern int xkphys_usermem_read(long);
 extern int xkphys_usermem_write(long, int);
+#endif
 
 /*
  * For historic reasons the pipe(2) syscall on MIPS has an unusual calling
@@ -304,11 +306,13 @@ _sys_sysmips(nabi_no_regargs struct pt_regs regs)
 		__flush_cache_all();
 		return 0;
 
+#ifdef CONFIG_CPU_CAVIUM_OCTEON
 	case MIPS_CAVIUM_XKPHYS_READ:
 		return xkphys_usermem_read(arg1);
 
 	case MIPS_CAVIUM_XKPHYS_WRITE:
 		return xkphys_usermem_write(arg1, arg2);
+#endif
 	}
 
 	return -EINVAL;
-- 
1.7.5.4

