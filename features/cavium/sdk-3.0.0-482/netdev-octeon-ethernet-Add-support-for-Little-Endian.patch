From 4b64e97d2b3cbf2b7a4533e27ea9904fd8e1e27d Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Thu, 27 Sep 2012 14:56:42 -0700
Subject: [PATCH 179/337] netdev: octeon-ethernet: Add support for Little
 Endian kernels.

Based On SDK 3.0.0-482

Both IPD and PKO must be put in little endian mode.

Signed-off-by: David Daney <david.daney@cavium.com>
Signed-off-by: Corey Minyard <cminyard@mvista.com>
Signed-off-by: Jack Tan <jack.tan@windriver.com>
---
 drivers/net/ethernet/octeon/ethernet-tx.c |    3 +++
 drivers/net/ethernet/octeon/ethernet.c    |    8 ++++++++
 2 files changed, 11 insertions(+), 0 deletions(-)

diff --git a/drivers/net/ethernet/octeon/ethernet-tx.c b/drivers/net/ethernet/octeon/ethernet-tx.c
index 99e48b8..dfdacbb 100644
--- a/drivers/net/ethernet/octeon/ethernet-tx.c
+++ b/drivers/net/ethernet/octeon/ethernet-tx.c
@@ -228,6 +228,9 @@ int cvm_oct_xmit(struct sk_buff *skb, struct net_device *dev)
 
 	/* Build the PKO command */
 	pko_command.u64 = 0;
+#ifdef __LITTLE_ENDIAN
+	pko_command.s.le = 1;
+#endif
 	/* Don't pollute L2 with the outgoing packet */
 	pko_command.s.n2 = 1;
 	pko_command.s.segs = 1;
diff --git a/drivers/net/ethernet/octeon/ethernet.c b/drivers/net/ethernet/octeon/ethernet.c
index 167c0d4..79199731 100644
--- a/drivers/net/ethernet/octeon/ethernet.c
+++ b/drivers/net/ethernet/octeon/ethernet.c
@@ -146,6 +146,8 @@ static int cvm_oct_num_output_buffers;
 
 static __devinit int cvm_oct_configure_common_hw(void)
 {
+	union cvmx_ipd_ctl_status ipd_ctl_status;
+
 	/* Setup the FPA */
 	cvmx_fpa_enable();
 
@@ -176,6 +178,12 @@ static __devinit int cvm_oct_configure_common_hw(void)
 
 	cvmx_helper_initialize_packet_io_global();
 
+#ifdef __LITTLE_ENDIAN
+	ipd_ctl_status.u64 = cvmx_read_csr(CVMX_IPD_CTL_STATUS);
+	ipd_ctl_status.s.pkt_lend = 1;
+	ipd_ctl_status.s.wqe_lend = 1;
+	cvmx_write_csr(CVMX_IPD_CTL_STATUS, ipd_ctl_status.u64);
+#endif
 	/* Enable red after interface is initialized */
 	if (USE_RED)
 		cvmx_helper_setup_red(num_packet_buffers / 4,
-- 
1.7.5.4

