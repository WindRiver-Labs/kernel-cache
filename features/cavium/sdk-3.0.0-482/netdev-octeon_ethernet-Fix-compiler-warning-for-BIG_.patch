From 52835beb1cf7a9bef13992fee118685acb99c145 Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Mon, 22 Oct 2012 11:25:07 -0700
Subject: [PATCH 209/337] netdev: octeon_ethernet: Fix compiler warning for
 BIG_ENDIAN builds.

Based On SDK 3.0.0-482

Signed-off-by: David Daney <david.daney@cavium.com>
Signed-off-by: Corey Minyard <cminyard@mvista.com>
Signed-off-by: Jack Tan <jack.tan@windriver.com>
---
 drivers/net/ethernet/octeon/ethernet.c |   13 +++++++------
 1 files changed, 7 insertions(+), 6 deletions(-)

diff --git a/drivers/net/ethernet/octeon/ethernet.c b/drivers/net/ethernet/octeon/ethernet.c
index 15cefc5..b97130d 100644
--- a/drivers/net/ethernet/octeon/ethernet.c
+++ b/drivers/net/ethernet/octeon/ethernet.c
@@ -152,8 +152,6 @@ static int cvm_oct_num_output_buffers;
 
 static __devinit int cvm_oct_configure_common_hw(void)
 {
-	union cvmx_ipd_ctl_status ipd_ctl_status;
-
 	/* Setup the FPA */
 	cvmx_fpa_enable();
 
@@ -185,10 +183,13 @@ static __devinit int cvm_oct_configure_common_hw(void)
 	cvmx_helper_initialize_packet_io_global();
 
 #ifdef __LITTLE_ENDIAN
-	ipd_ctl_status.u64 = cvmx_read_csr(CVMX_IPD_CTL_STATUS);
-	ipd_ctl_status.s.pkt_lend = 1;
-	ipd_ctl_status.s.wqe_lend = 1;
-	cvmx_write_csr(CVMX_IPD_CTL_STATUS, ipd_ctl_status.u64);
+	{
+		union cvmx_ipd_ctl_status ipd_ctl_status;
+		ipd_ctl_status.u64 = cvmx_read_csr(CVMX_IPD_CTL_STATUS);
+		ipd_ctl_status.s.pkt_lend = 1;
+		ipd_ctl_status.s.wqe_lend = 1;
+		cvmx_write_csr(CVMX_IPD_CTL_STATUS, ipd_ctl_status.u64);
+	}
 #endif
 	/* Enable red after interface is initialized */
 	if (USE_RED)
-- 
1.7.5.4

