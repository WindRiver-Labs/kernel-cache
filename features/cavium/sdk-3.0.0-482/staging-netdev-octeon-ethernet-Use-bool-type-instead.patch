From a9594102f5621da9289a5b6c43b1c25dadabc389 Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Wed, 11 Jul 2012 16:26:40 -0700
Subject: [PATCH 129/337] staging/netdev: octeon-ethernet: Use bool type
 instead of int.

Based On SDK 3.0.0-482

Signed-off-by: David Daney <david.daney@cavium.com>
Signed-off-by: Corey Minyard <cminyard@mvista.com>
Signed-off-by: Jack Tan <jack.tan@windriver.com>
---
 drivers/staging/octeon/ethernet-rx.c |   10 +++++-----
 1 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/drivers/staging/octeon/ethernet-rx.c b/drivers/staging/octeon/ethernet-rx.c
index c5a3271..ffe2b5e 100644
--- a/drivers/staging/octeon/ethernet-rx.c
+++ b/drivers/staging/octeon/ethernet-rx.c
@@ -299,7 +299,7 @@ static int cvm_oct_napi_poll(struct napi_struct *napi, int budget)
 	uint64_t	old_group_mask;
 	uint64_t	old_scratch;
 	int		rx_count = 0;
-	int		did_work_request = 0;
+	bool		did_work_request = false;
 	bool		packet_copied;
 
 	char		*p = (char *)cvm_oct_by_port;
@@ -321,13 +321,13 @@ static int cvm_oct_napi_poll(struct napi_struct *napi, int budget)
 
 	if (USE_ASYNC_IOBDMA) {
 		cvmx_pow_work_request_async(CVMX_SCR_SCRATCH, CVMX_POW_NO_WAIT);
-		did_work_request = 1;
+		did_work_request = true;
 	}
 
 	while (rx_count < budget) {
 		struct sk_buff *skb = NULL;
 		struct sk_buff **pskb = NULL;
-		int skb_in_hw;
+		bool skb_in_hw;
 		cvmx_wqe_t *work;
 		unsigned int segments;
 		int packets_to_replace = 0;
@@ -341,7 +341,7 @@ static int cvm_oct_napi_poll(struct napi_struct *napi, int budget)
 			work = cvmx_pow_work_request_sync(CVMX_POW_NO_WAIT);
 
 		prefetch(work);
-		did_work_request = 0;
+		did_work_request = false;
 		if (work == NULL) {
 			union cvmx_pow_wq_int wq_int;
 			wq_int.u64 = 0;
@@ -355,7 +355,7 @@ static int cvm_oct_napi_poll(struct napi_struct *napi, int budget)
 
 		if (USE_ASYNC_IOBDMA && rx_count < (budget - 1)) {
 			cvmx_pow_work_request_async_nocheck(CVMX_SCR_SCRATCH, CVMX_POW_NO_WAIT);
-			did_work_request = 1;
+			did_work_request = true;
 		}
 
 		if (rx_count == 0) {
-- 
1.7.5.4

