From bc982252b694759b0137dcf1819226f0508ed387 Mon Sep 17 00:00:00 2001
From: czou <cao.zou@windriver.com>
Date: Tue, 24 May 2016 16:20:40 +0800
Subject: [PATCH] mmc:imx: remove esdhc_get_max_timeout_counter funciton

Function esdhc_get_max_timeout_count has covered the old function
esdhc_get_max_timeout_counter, it isn't needed again, here remove it,
it will fixed the build warning as follow:

drivers/mmc/host/sdhci-esdhc-imx.c:1016:21: warning:
'esdhc_get_max_timeout_counter' defined but not used [-Wunused-function]
 static unsigned int esdhc_get_max_timeout_counter(struct sdhci_host
 *host)

Signed-off-by: czou <cao.zou@windriver.com>
---
 drivers/mmc/host/sdhci-esdhc-imx.c |    5 -----
 1 files changed, 0 insertions(+), 5 deletions(-)

diff --git a/drivers/mmc/host/sdhci-esdhc-imx.c b/drivers/mmc/host/sdhci-esdhc-imx.c
index 53be028..6a2f439 100644
--- a/drivers/mmc/host/sdhci-esdhc-imx.c
+++ b/drivers/mmc/host/sdhci-esdhc-imx.c
@@ -1013,11 +1013,6 @@ static void esdhc_set_timeout(struct sdhci_host *host, struct mmc_command *cmd)
 			SDHCI_TIMEOUT_CONTROL);
 }
 
-static unsigned int esdhc_get_max_timeout_counter(struct sdhci_host *host)
-{
-	return 1 << 28;
-}
-
 static struct sdhci_ops sdhci_esdhc_ops = {
 	.read_l = esdhc_readl_le,
 	.read_w = esdhc_readw_le,
-- 
1.7.5.4

