From 81e8e7d91d81351d9fea4ad7c289b122706640fd Mon Sep 17 00:00:00 2001
From: Fugang Duan <b38611@freescale.com>
Date: Wed, 19 Aug 2015 14:39:20 +0800
Subject: [PATCH 0476/1691] MLK-11382 tty: serial: imx: disable irq after
 suspend

commit 81e8e7d91d81351d9fea4ad7c289b122706640fd from
git://git.freescale.com/imx/linux-2.6-imx.git imx_4.1.15_1.0.0_ga

Disable rx irq after suspend to avoid interrupt coming in early resume.

Signed-off-by: Fugang Duan <B38611@freescale.com>
---
 drivers/tty/serial/imx.c | 8 ++------
 1 file changed, 2 insertions(+), 6 deletions(-)

diff --git a/drivers/tty/serial/imx.c b/drivers/tty/serial/imx.c
index 92e0640..3e6b173 100644
--- a/drivers/tty/serial/imx.c
+++ b/drivers/tty/serial/imx.c
@@ -1849,9 +1849,7 @@ static int serial_imx_suspend(struct platform_device *dev, pm_message_t state)
 	writel(val, sport->port.membase + UCR3);
 
 	uart_suspend_port(&imx_reg, &sport->port);
-
-	if (sport->port.irq_wake)
-		disable_irq(sport->port.irq);
+	disable_irq(sport->port.irq);
 
 	/* Save necessary regs */
 	clk_prepare_enable(sport->clk_ipg);
@@ -1896,10 +1894,8 @@ static int serial_imx_resume(struct platform_device *dev)
 		writel(USR1_AWAKE, sport->port.membase + USR1);
 	clk_disable_unprepare(sport->clk_ipg);
 
-	if (sport->port.irq_wake)
-		enable_irq(sport->port.irq);
-
 	uart_resume_port(&imx_reg, &sport->port);
+	enable_irq(sport->port.irq);
 
 	return 0;
 }
-- 
1.9.1

