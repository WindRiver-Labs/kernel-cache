From b7c86b88b1d430e733c4d6538fe03491f2011e58 Mon Sep 17 00:00:00 2001
From: Fugang Duan <b38611@freescale.com>
Date: Wed, 11 Mar 2015 19:07:42 +0800
Subject: [PATCH 0135/1594] MLK-10406 ARM: imx: check the clk_set_rate()
 return value

commit bcd9d743ef534cf58d2d007e6333270be12c9f06 from
git://git.freescale.com/imx/linux-2.6-imx.git

Add return value check for clk_set_rate() to avoid set clock
rate fail.

Signed-off-by: Fugang Duan <B38611@freescale.com>
---
 drivers/tty/serial/imx.c |    9 +++++++--
 1 files changed, 7 insertions(+), 2 deletions(-)

diff --git a/drivers/tty/serial/imx.c b/drivers/tty/serial/imx.c
index dff500c..57533e8 100644
--- a/drivers/tty/serial/imx.c
+++ b/drivers/tty/serial/imx.c
@@ -2071,8 +2071,13 @@ static int serial_imx_probe(struct platform_device *pdev)
 	}
 
 	sport->port.uartclk = clk_get_rate(sport->clk_per);
-	if (sport->port.uartclk > IMX_MODULE_MAX_CLK_RATE)
-		clk_set_rate(sport->clk_per, IMX_MODULE_MAX_CLK_RATE);
+	if (sport->port.uartclk > IMX_MODULE_MAX_CLK_RATE) {
+		ret = clk_set_rate(sport->clk_per, IMX_MODULE_MAX_CLK_RATE);
+		if (ret < 0) {
+			dev_err(&pdev->dev, "clk_set_rate() failed\n");
+			return ret;
+		}
+	}
 	sport->port.uartclk = clk_get_rate(sport->clk_per);
 
 	/*
-- 
1.7.5.4

