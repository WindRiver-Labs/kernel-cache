From 713266450275ebbe22c14821d9131cdb6c929b9e Mon Sep 17 00:00:00 2001
From: Liu Ying <Ying.Liu@freescale.com>
Date: Mon, 2 Mar 2015 17:40:24 +0800
Subject: [PATCH 0874/1594] MLK-10373-5 video: mxsfb: remove duplicate clock
 rate setting of lcdif pix clk

commit a761602e287d1324bdd2967902351760f757bb74 from
git://git.freescale.com/imx/linux-2.6-imx.git

Remove the duplicated clock rate set for the lcdif pix
clk when enable the lcdif controller.

Signed-off-by: Liu Ying <Ying.Liu@freescale.com>
Signed-off-by: Fancy Fang <chen.fang@freescale.com>
---
 drivers/video/fbdev/mxsfb.c |    4 +---
 1 files changed, 1 insertions(+), 3 deletions(-)

diff --git a/drivers/video/fbdev/mxsfb.c b/drivers/video/fbdev/mxsfb.c
index 92ed0c0..b2436e4 100644
--- a/drivers/video/fbdev/mxsfb.c
+++ b/drivers/video/fbdev/mxsfb.c
@@ -496,9 +496,7 @@ static void mxsfb_enable_controller(struct fb_info *fb_info)
 
 	if (host->clk_disp_axi)
 		clk_prepare_enable(host->clk_disp_axi);
-	clk_set_rate(host->clk, PICOS2KHZ(fb_info->var.pixclock) * 1000U);
-	ret =
-	    clk_set_rate(host->clk,
+	ret = clk_set_rate(host->clk,
 			 PICOS2KHZ(fb_info->var.pixclock) * 1000U);
 	if (ret) {
 		dev_err(&host->pdev->dev,
-- 
1.7.5.4

