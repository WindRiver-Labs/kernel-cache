From 4b6e87c56bdd12704938cc9932c9ed9cfe07ce9d Mon Sep 17 00:00:00 2001
From: Li Jun <B47624@freescale.com>
Date: Sat, 27 Sep 2014 21:03:55 +0800
Subject: [PATCH 1423/1594] MLK-9638-3 usb: chipidea: udc: test: handle
 otg_hnp_req set feature request

commit 03f3f886029fa8f25dde34fcfeb28589ead0064f from
git://git.freescale.com/imx/linux-2.6-imx.git

This patch adds otg_hnp_reqd set feature request from A host for chipidea udc
driver.

Acked-by: Peter Chen <peter.chen@freescale.com>
Signed-off-by: Li Jun <b47624@freescale.com>
(cherry picked from commit 450d4a1eb40ec4284661c1130bdcc4f4d2e6c527)
---
 drivers/usb/chipidea/udc.c |   19 ++++++++++++++++++-
 1 files changed, 18 insertions(+), 1 deletions(-)

diff --git a/drivers/usb/chipidea/udc.c b/drivers/usb/chipidea/udc.c
index 8354995..00d3def 100644
--- a/drivers/usb/chipidea/udc.c
+++ b/drivers/usb/chipidea/udc.c
@@ -752,8 +752,10 @@ __acquires(ci->lock)
 {
 	int retval;
 
-	if (ci_otg_is_fsm_mode(ci))
+	if (ci_otg_is_fsm_mode(ci)) {
 		ci->fsm.otg_srp_reqd = 0;
+		ci->fsm.otg_hnp_reqd = 0;
+	}
 
 	spin_unlock(&ci->lock);
 	if (ci->gadget.speed != USB_SPEED_UNKNOWN)
@@ -1021,6 +1023,18 @@ static int otg_srp_reqd(struct ci_hdrc *ci)
 	}
 }
 
+static int otg_hnp_reqd(struct ci_hdrc *ci)
+{
+	if (ci_otg_is_fsm_mode(ci)) {
+		ci->fsm.otg_hnp_reqd = 1;
+		ci->fsm.b_bus_req = 1;
+		ci->gadget.host_request_flag = 1;
+		return isr_setup_status_phase(ci);
+	} else {
+		return -ENOTSUPP;
+	}
+}
+
 /**
  * isr_setup_packet_handler: setup packet handler
  * @ci: UDC descriptor
@@ -1146,6 +1160,9 @@ __acquires(ci->lock)
 				case TEST_OTG_SRP_REQD:
 					err = otg_srp_reqd(ci);
 					break;
+				case TEST_OTG_HNP_REQD:
+					err = otg_hnp_reqd(ci);
+					break;
 				default:
 					break;
 				}
-- 
1.7.5.4

