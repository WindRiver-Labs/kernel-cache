From 3ead801ef2dd3558ebf78a26be1b32a1c2158538 Mon Sep 17 00:00:00 2001
From: Paul Barrette <paul.barrette@windriver.com>
Date: Wed, 26 Jun 2013 10:58:13 -0400
Subject: [PATCH] remoteproc: fix compilation error for STE_MODEM

This fix completes the work for the following commit:

cab3c26f369784473e8fffe2bd814812d46410fa
remoteproc: Pass struct fw to load_segments and find_rsc_table.

Signed-off-by: Paul Barrette <paul.barrette@windriver.com>
---
 drivers/remoteproc/remoteproc_core.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/drivers/remoteproc/remoteproc_core.c b/drivers/remoteproc/remoteproc_core.c
index c494c62..1a1d6fc 100644
--- a/drivers/remoteproc/remoteproc_core.c
+++ b/drivers/remoteproc/remoteproc_core.c
@@ -1035,7 +1035,7 @@ static int rproc_fw_boot(struct rproc *rproc, const struct firmware *fw)
 	rproc->bootaddr = ehdr->e_entry;
 
 	/* look for the resource table */
-	table = rproc_find_rsc_table(rproc, fw->data, fw->size, &tablesz);
+	table = rproc_find_rsc_table(rproc, fw, &tablesz);
 	if (!table) {
 		ret = -EINVAL;
 		goto clean_up;
-- 
1.7.9.7

