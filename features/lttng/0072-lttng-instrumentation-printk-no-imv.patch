From 70bc1018553de1fa9730de94953563fb51ac4eaf Mon Sep 17 00:00:00 2001
From: Mathieu Desnoyers <mathieu.desnoyers@polymtl.ca>
Date: Tue, 12 Aug 2008 10:32:40 -0400
Subject: [PATCH] lttng-instrumentation-printk-no-imv

LTTng instrumentation printk no imv

Printk should not use immediate values because it is called from lockdep.

Signed-off-by: Mathieu Desnoyers <mathieu.desnoyers@polymtl.ca>
---
 kernel/kernel-trace.c |    4 ++--
 kernel/printk.c       |    4 ++--
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/kernel/kernel-trace.c b/kernel/kernel-trace.c
index eb6b8cd..6bbfcba 100644
--- a/kernel/kernel-trace.c
+++ b/kernel/kernel-trace.c
@@ -178,7 +178,7 @@ static void probe_timer_timeout(struct task_struct *p)
 
 static void probe_kernel_printk(unsigned long retaddr)
 {
-	trace_mark(kernel_printk, "ip %lu", retaddr);
+	_trace_mark(kernel_printk, "ip %lu", retaddr);
 }
 
 static void probe_kernel_vprintk(unsigned long retaddr, char *buf, int len)
@@ -203,7 +203,7 @@ static void probe_kernel_vprintk(unsigned long retaddr, char *buf, int len)
 			mark_len--;
 		saved_char = mark_buf[mark_len];
 		mark_buf[mark_len] = '\0';
-		trace_mark(kernel_vprintk, "loglevel %c string %s ip %lu",
+		_trace_mark(kernel_vprintk, "loglevel %c string %s ip %lu",
 			loglevel, mark_buf, retaddr);
 		mark_buf[mark_len] = saved_char;
 	}
diff --git a/kernel/printk.c b/kernel/printk.c
index af7b2c7..aaeef5c 100644
--- a/kernel/printk.c
+++ b/kernel/printk.c
@@ -659,7 +659,7 @@ asmlinkage int printk(const char *fmt, ...)
 	int r;
 
 	va_start(args, fmt);
-	trace_kernel_printk(_RET_IP_);
+	_trace_kernel_printk(_RET_IP_);
 	r = vprintk(fmt, args);
 	va_end(args);
 
@@ -736,7 +736,7 @@ asmlinkage int vprintk(const char *fmt, va_list args)
 	raw_local_irq_save(flags);
 	this_cpu = smp_processor_id();
 
-	trace_kernel_vprintk(_RET_IP_, printk_buf, printed_len);
+	_trace_kernel_vprintk(_RET_IP_, printk_buf, printed_len);
 
 	/*
 	 * Ouch, printk recursed into itself!
-- 
1.5.5.1

