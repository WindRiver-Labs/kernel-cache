From 9c36ee2a2b5e94f51a5101752b39a443b681db02 Mon Sep 17 00:00:00 2001
From: Mathieu Desnoyers <mathieu.desnoyers@efficios.com>
Date: Thu, 13 May 2010 19:26:08 -0400
Subject: [PATCH 102/391] lttng-instrumentation/lttng-instrumentation-printk-no-imv

LTTng instrumentation printk no imv

Printk should not use immediate values because it is called from lockdep.

Signed-off-by: Mathieu Desnoyers <mathieu.desnoyers@polymtl.ca>
---
 kernel/printk.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/kernel/printk.c b/kernel/printk.c
index f921a3f..6513b77 100644
--- a/kernel/printk.c
+++ b/kernel/printk.c
@@ -592,7 +592,7 @@ asmlinkage int printk(const char *fmt, ...)
 	int r;
 
 	va_start(args, fmt);
-	trace_kernel_printk(_RET_IP_);
+	_trace_kernel_printk(_RET_IP_);
 	r = vprintk(fmt, args);
 	va_end(args);
 
@@ -715,7 +715,7 @@ asmlinkage int vprintk(const char *fmt, va_list args)
 	printed_len += vscnprintf(printk_buf + printed_len,
 				  sizeof(printk_buf) - printed_len, fmt, args);
 
-	trace_kernel_vprintk(_RET_IP_, printk_buf, printed_len);
+	_trace_kernel_vprintk(_RET_IP_, printk_buf, printed_len);
 
 	p = printk_buf;
 
-- 
1.6.5.2

