From 37a541ab5232893861386fbb27e6686ee5ad2dac Mon Sep 17 00:00:00 2001
From: Rui Wang <rui.wang@windriver.com>
Date: Mon, 24 Dec 2018 06:03:57 +0000
Subject: [PATCH 1/2] ALSA: hda - fix compiling warning "IS_SKL_PLUS"
 redefined

We backported a patch "ALSA: hda - Add PCI ID for Kabylake"(cc388d064e476
in wrlinux) from upstream, but missed deleting a macro that is redefined
in the patch, which caused a warning "IS_SKL_PLUS" redefined. Deleting the
old version of macro will fix this.

Signed-off-by: Rui Wang <rui.wang@windriver.com>
---
 sound/pci/hda/hda_intel.c |    1 -
 1 file changed, 1 deletion(-)

diff --git a/sound/pci/hda/hda_intel.c b/sound/pci/hda/hda_intel.c
index 77b275d..fa3a18b 100644
--- a/sound/pci/hda/hda_intel.c
+++ b/sound/pci/hda/hda_intel.c
@@ -850,7 +850,6 @@ static int azx_resume(struct device *dev)
 #define IS_KBL_LP(pci) ((pci)->vendor == 0x8086 && (pci)->device == 0x9d71)
 #define IS_KBL_H(pci) ((pci)->vendor == 0x8086 && (pci)->device == 0xa2f0)
 #define IS_BXT(pci) ((pci)->vendor == 0x8086 && (pci)->device == 0x5a98)
-#define IS_SKL_PLUS(pci) (IS_SKL(pci) || IS_SKL_LP(pci) || IS_BXT(pci))
 #define IS_SKL_PLUS(pci) (IS_SKL(pci) || IS_SKL_LP(pci) || IS_BXT(pci)) || \
 			IS_KBL(pci) || IS_KBL_LP(pci) || IS_KBL_H(pci)
 
-- 
1.7.9.5

