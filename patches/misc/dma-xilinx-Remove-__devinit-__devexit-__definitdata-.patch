From 286d40ad1f57e4aba6965f7e7c5d57c9f640a281 Mon Sep 17 00:00:00 2001
From: Liming Wang <liming.wang@windriver.com>
Date: Tue, 25 Mar 2014 16:17:07 +0800
Subject: [PATCH 107/628] dma: xilinx: Remove __devinit, __devexit,
 __definitdata, __devexit_p, __exit_p

git://github.com/Xilinx/linux-xlnx.git xilinx-v14.7
commit 068160c0 Merge commit 'v3.8' into master-next

cherry picked the xilinx dma driver modification.

Signed-off-by: Liming Wang <liming.wang@windriver.com>
---
 drivers/dma/xilinx/xilinx_axicdma.c |    8 ++++----
 drivers/dma/xilinx/xilinx_axidma.c  |    8 ++++----
 drivers/dma/xilinx/xilinx_axivdma.c |    8 ++++----
 3 files changed, 12 insertions(+), 12 deletions(-)

diff --git a/drivers/dma/xilinx/xilinx_axicdma.c b/drivers/dma/xilinx/xilinx_axicdma.c
index 3c89741..744c363 100644
--- a/drivers/dma/xilinx/xilinx_axicdma.c
+++ b/drivers/dma/xilinx/xilinx_axicdma.c
@@ -858,7 +858,7 @@ static void xilinx_cdma_chan_remove(struct xilinx_cdma_chan *chan)
  * . Get channel features from the device tree entry
  * . Initialize special channel handling routines
  */
-static int __devinit xilinx_cdma_chan_probe(struct xilinx_cdma_device *xdev,
+static int xilinx_cdma_chan_probe(struct xilinx_cdma_device *xdev,
 	struct device_node *node, u32 feature)
 {
 	struct xilinx_cdma_chan *chan;
@@ -974,7 +974,7 @@ out_return:
 	return err;
 }
 
-static int __devinit xilinx_cdma_of_probe(struct platform_device *op)
+static int xilinx_cdma_of_probe(struct platform_device *op)
 {
 	struct xilinx_cdma_device *xdev;
 	struct device_node *child, *node;
@@ -1045,7 +1045,7 @@ out_return:
 	return err;
 }
 
-static int __devexit xilinx_cdma_of_remove(struct platform_device *op)
+static int xilinx_cdma_of_remove(struct platform_device *op)
 {
 	struct xilinx_cdma_device *xdev;
 	int i;
@@ -1077,7 +1077,7 @@ static struct platform_driver xilinx_cdma_of_driver = {
 		.of_match_table = xilinx_cdma_of_ids,
 	},
 	.probe = xilinx_cdma_of_probe,
-	.remove = __devexit_p(xilinx_cdma_of_remove),
+	.remove = xilinx_cdma_of_remove,
 };
 
 module_platform_driver(xilinx_cdma_of_driver);
diff --git a/drivers/dma/xilinx/xilinx_axidma.c b/drivers/dma/xilinx/xilinx_axidma.c
index 5387524..b4a85c7 100644
--- a/drivers/dma/xilinx/xilinx_axidma.c
+++ b/drivers/dma/xilinx/xilinx_axidma.c
@@ -960,7 +960,7 @@ static void xilinx_dma_chan_remove(struct xilinx_dma_chan *chan)
  * . Get channel features from the device tree entry
  * . Initialize special channel handling routines
  */
-static int __devinit xilinx_dma_chan_probe(struct xilinx_dma_device *xdev,
+static int xilinx_dma_chan_probe(struct xilinx_dma_device *xdev,
 	struct device_node *node, u32 feature)
 {
 	struct xilinx_dma_chan *chan;
@@ -1075,7 +1075,7 @@ out_return:
 	return err;
 }
 
-static int __devinit xilinx_dma_of_probe(struct platform_device *op)
+static int xilinx_dma_of_probe(struct platform_device *op)
 {
 	struct xilinx_dma_device *xdev;
 	struct device_node *child, *node;
@@ -1152,7 +1152,7 @@ out_return:
 	return err;
 }
 
-static int __devexit xilinx_dma_of_remove(struct platform_device *op)
+static int xilinx_dma_of_remove(struct platform_device *op)
 {
 	struct xilinx_dma_device *xdev;
 	int i;
@@ -1184,7 +1184,7 @@ static struct platform_driver xilinx_dma_of_driver = {
 		.of_match_table = xilinx_dma_of_ids,
 	},
 	.probe = xilinx_dma_of_probe,
-	.remove = __devexit_p(xilinx_dma_of_remove),
+	.remove = xilinx_dma_of_remove,
 };
 
 module_platform_driver(xilinx_dma_of_driver);
diff --git a/drivers/dma/xilinx/xilinx_axivdma.c b/drivers/dma/xilinx/xilinx_axivdma.c
index d7e2098..4720757 100644
--- a/drivers/dma/xilinx/xilinx_axivdma.c
+++ b/drivers/dma/xilinx/xilinx_axivdma.c
@@ -1052,7 +1052,7 @@ static void xilinx_vdma_chan_remove(struct xilinx_vdma_chan *chan)
  * . Get channel features from the device tree entry
  * . Initialize special channel handling routines
  */
-static int __devinit xilinx_vdma_chan_probe(struct xilinx_vdma_device *xdev,
+static int xilinx_vdma_chan_probe(struct xilinx_vdma_device *xdev,
 	struct device_node *node, u32 feature)
 {
 	struct xilinx_vdma_chan *chan;
@@ -1190,7 +1190,7 @@ out_return:
 	return err;
 }
 
-static int __devinit xilinx_vdma_of_probe(struct platform_device *op)
+static int xilinx_vdma_of_probe(struct platform_device *op)
 {
 	struct xilinx_vdma_device *xdev;
 	struct device_node *child, *node;
@@ -1276,7 +1276,7 @@ out_return:
 	return err;
 }
 
-static int __devexit xilinx_vdma_of_remove(struct platform_device *op)
+static int xilinx_vdma_of_remove(struct platform_device *op)
 {
 	struct xilinx_vdma_device *xdev;
 	int i;
@@ -1308,7 +1308,7 @@ static struct platform_driver xilinx_vdma_of_driver = {
 		.of_match_table = xilinx_vdma_of_ids,
 	},
 	.probe = xilinx_vdma_of_probe,
-	.remove = __devexit_p(xilinx_vdma_of_remove),
+	.remove = xilinx_vdma_of_remove,
 };
 
 module_platform_driver(xilinx_vdma_of_driver);
-- 
1.7.5.4

