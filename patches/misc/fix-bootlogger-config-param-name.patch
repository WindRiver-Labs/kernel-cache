From 5411a8ceec8a1bb7d3de01e061bab8b822f24839 Mon Sep 17 00:00:00 2001
From: Dave Lerner <dave.lerner@windriver.com>
Date: Tue, 4 Mar 2014 17:30:05 -0600
Subject: [PATCH] fix bootlogger config param name

Issue: LIN6-6826

In a previous patch that added the CONFIG_WR_BOOT kernel parameter,
enabling early boottime logging, the symbol name incorrectly included
the 'CONFIG_' prefix in the patch. That caused the generated parameter
name to be CONFIG_CONFIG_WR_BOOT instead of CONFIG_WR_BOOT.
Neither templates that set CONFIG_WR_BOOT nor the '#if's in the source
that tested for CONFIG_WR_BOOT worked with the incorrectly named
parameter.

Signed-off-by: Dave Lerner <dave.lerner@windriver.com>
---
 kernel/trace/Kconfig |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/kernel/trace/Kconfig b/kernel/trace/Kconfig
index 511ce32..7392c39 100644
--- a/kernel/trace/Kconfig
+++ b/kernel/trace/Kconfig
@@ -226,7 +226,7 @@ config SCHED_TRACER
 	  This tracer tracks the latency of the highest priority task
 	  to be scheduled in, starting from the point it has woken up.
 
-config CONFIG_WR_BOOT
+config WR_BOOT
 	bool "Wind River boottime analysis"
 
 config ENABLE_DEFAULT_TRACERS
-- 
1.7.5.4

