From 4b0404408871b0cf74a7d8da2dffeb3a59df9c07 Mon Sep 17 00:00:00 2001
From: Michal Simek <michal.simek@xilinx.com>
Date: Mon, 8 Jul 2013 11:05:29 +0200
Subject: [PATCH 263/628] spi: xilinx-qps: Use of_property_read_u32

git://github.com/Xilinx/linux-xlnx.git xilinx-v14.7
commit f4671cbcebbde272743c196dfa7e08e9381ea3cf

Simplify num-chip-select property reading.

Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Liming Wang <liming.wang@windriver.com>
---
 drivers/spi/spi-xilinx-qps.c |    9 +++------
 1 files changed, 3 insertions(+), 6 deletions(-)

diff --git a/drivers/spi/spi-xilinx-qps.c b/drivers/spi/spi-xilinx-qps.c
index baab545..c48dbd5 100644
--- a/drivers/spi/spi-xilinx-qps.c
+++ b/drivers/spi/spi-xilinx-qps.c
@@ -1020,7 +1020,6 @@ static int xqspips_probe(struct platform_device *pdev)
 	struct spi_master *master;
 	struct xqspips *xqspi;
 	struct resource *res;
-	const unsigned int *prop;
 
 	master = spi_alloc_master(&pdev->dev, sizeof(*xqspi));
 	if (master == NULL)
@@ -1093,11 +1092,9 @@ static int xqspips_probe(struct platform_device *pdev)
 
 	init_completion(&xqspi->done);
 
-	prop = of_get_property(pdev->dev.of_node, "num-chip-select", NULL);
-	if (prop) {
-		master->num_chipselect = be32_to_cpup(prop);
-	} else {
-		ret = -ENXIO;
+	ret = of_property_read_u32(pdev->dev.of_node, "num-chip-select",
+				   (u32 *)&master->num_chipselect);
+	if (ret < 0) {
 		dev_err(&pdev->dev, "couldn't determine num-chip-select\n");
 		goto clk_unreg_notif;
 	}
-- 
1.7.5.4

