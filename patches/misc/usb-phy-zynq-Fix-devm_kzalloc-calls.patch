From 8dc5ade6ccfaf73311659ba0edb96ee3cf9f089b Mon Sep 17 00:00:00 2001
From: Soren Brinkmann <soren.brinkmann@xilinx.com>
Date: Thu, 28 Nov 2013 12:05:10 -0800
Subject: [PATCH 568/628] usb: phy-zynq: Fix devm_kzalloc() calls

git://github.com/Xilinx/linux-xlnx.git xilinx-v2013.4
commit bffc502a6e23bf45aa7d1bb6f6fd6273564e9a67

The calls to devm_kzalloc had several issues:
 - missing struct device pointer
 - sizeof argument should be within parentheses
 - sizeof argument should use variable identifier opposed to a type

Signed-off-by: Soren Brinkmann <soren.brinkmann@xilinx.com>
Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Liming Wang <liming.wang@windriver.com>
---
 drivers/usb/phy/phy-zynq-usb.c |    5 +++--
 1 files changed, 3 insertions(+), 2 deletions(-)

diff --git a/drivers/usb/phy/phy-zynq-usb.c b/drivers/usb/phy/phy-zynq-usb.c
index 8b2514f..c605667 100644
--- a/drivers/usb/phy/phy-zynq-usb.c
+++ b/drivers/usb/phy/phy-zynq-usb.c
@@ -1961,7 +1961,7 @@ static int xusbps_otg_probe(struct platform_device *pdev)
 
 	dev_dbg(&pdev->dev, "\notg controller is detected.\n");
 
-	xotg = devm_kzalloc(&pdev->dev, sizeof *xotg, GFP_KERNEL);
+	xotg = devm_kzalloc(&pdev->dev, sizeof(*xotg), GFP_KERNEL);
 	if (xotg == NULL)
 		return -ENOMEM;
 
@@ -1970,7 +1970,8 @@ static int xusbps_otg_probe(struct platform_device *pdev)
 	/* Setup ulpi phy for OTG */
 	xotg->ulpi = pdata->ulpi;
 
-	xotg->otg.otg = devm_kzalloc(sizeof(struct usb_otg), GFP_KERNEL);
+	xotg->otg.otg = devm_kzalloc(&pdev->dev, sizeof(*xotg->otg.otg),
+			GFP_KERNEL);
 	if (!xotg->otg.otg)
 		return -ENOMEM;
 
-- 
1.7.5.4

