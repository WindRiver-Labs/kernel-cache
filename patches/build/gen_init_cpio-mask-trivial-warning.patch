From 700531f64eb99ae3fc2b1cc96c8c3de99ea9fbb6 Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Sun, 25 Jan 2009 17:58:09 -0500
Subject: [PATCH] gen_init_cpio: mask trivial warning

Newer HOSTCC will complain about various stdio fcns because
they are now declared with "warn_unused_return" -- trivially
assign the return value so we aren't overwhelmed with these
once the kernel build kicks off.

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 usr/gen_init_cpio.c |    3 ++-
 1 files changed, 2 insertions(+), 1 deletions(-)

diff --git a/usr/gen_init_cpio.c b/usr/gen_init_cpio.c
index 7abc07f..4cdbd97 100644
--- a/usr/gen_init_cpio.c
+++ b/usr/gen_init_cpio.c
@@ -354,7 +354,8 @@ static int cpio_mkfile(const char *name, const char *location,
 		push_pad();
 
 		if (size) {
-			fwrite(filebuf, size, 1, stdout);
+			int f;
+			f = fwrite(filebuf, size, 1, stdout);
 			offset += size;
 			push_pad();
 		}
-- 
1.6.0.4

