From eca9298a9451a8a2ad90e2eb06d0a042ff37cb7e Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:35:14 -0500
Subject: [PATCH 78/80] vbiVioapicVectorGet --> vbi_get_vioapic_vec

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/vbi.h    |    4 ++--
 kernel/vbi/io_apic.c |   10 +++++-----
 2 files changed, 7 insertions(+), 7 deletions(-)

diff --git a/include/vbi/vbi.h b/include/vbi/vbi.h
index a3bf8d5..96c4bd5 100644
--- a/include/vbi/vbi.h
+++ b/include/vbi/vbi.h
@@ -76,7 +76,7 @@ various data structures.
  * 
  * vbiVioapicVectorSet()
  * 
- * vbiVioapicVectorGet()
+ * vbi_get_vioapic_vec()
  * 
  * vbi_ack_vioapic_irq()
  * 
@@ -522,7 +522,7 @@ extern void ASSERT_FN(const char *, const char *, int);
 extern void vbi_idle(uint64_t);
 extern void *vbi_get_vioapic_addr(void);
 extern vbiStatus_t vbiVioapicVectorSet(vbiIrq_t irq, vbiVector_t vector); 
-extern vbiVector_t vbiVioapicVectorGet(vbiIrq_t irq);
+extern vbiVector_t vbi_get_vioapic_vec(vbiIrq_t irq);
 extern vbiStatus_t vbi_unmask_vioapic_irq(vbiIrq_t irq);
 extern vbiStatus_t vbi_mask_vioapic_irq(vbiIrq_t irq);
 extern vbiStatus_t vbi_ack_vioapic_irq(vbiIrq_t irq);
diff --git a/kernel/vbi/io_apic.c b/kernel/vbi/io_apic.c
index 34eeb69..c1c639b 100644
--- a/kernel/vbi/io_apic.c
+++ b/kernel/vbi/io_apic.c
@@ -146,7 +146,7 @@ void *vbi_get_vioapic_addr(void)
  * This routine sets a vector for the specified entry in the VIOAPIC
  * redirection table. The previous entry in the table is trashed. Before
  * setting the new vector in the entry the user can obtain the previous
- * entry by calling vbiVioapicVectorGet(). The first argument passed to
+ * entry by calling vbi_get_vioapic_vec(). The first argument passed to
  * this function represent the index to the VIOAPIC redirection table.  The
  * second argument is the vector to set in the specified entry.  For Intel
  * architectures when an interrupt is raised the vector determines the
@@ -175,7 +175,7 @@ vbiStatus_t vbiVioapicVectorSet(vbiIrq_t irq, vbiVector_t vector)
 }
 
 /*
- * vbiVioapicVectorGet - Get a vector in the specified irq entry
+ * vbi_get_vioapic_vec - Get a vector in the specified irq entry
  *
  * This routine gets a vector for the specified entry in the VIOAPIC table in
  * VIOAPIC redirection table. It may be used for saving the previous entry
@@ -185,7 +185,7 @@ vbiStatus_t vbiVioapicVectorSet(vbiIrq_t irq, vbiVector_t vector)
  * This routine is currently not supported for PPC
  *
  */
-vbiVector_t vbiVioapicVectorGet(vbiIrq_t irq)
+vbiVector_t vbi_get_vioapic_vec(vbiIrq_t irq)
 {
 	volatile VIOAPIC *pVioapic = (volatile VIOAPIC *)VBI_VIOAPIC_BASE_GET();
 	vbiVector_t vector;
@@ -196,11 +196,11 @@ vbiVector_t vbiVioapicVectorGet(vbiIrq_t irq)
 	if (irq > VIOAPIC_MAX_REDTABLE_ENTRIES-1)
 		return VBI_VIOAPIC_IRQ_OUTBOUND;
 
-	VB_DEBUG_MSG("vbiVioapicVectorGet: base @ 0x%x\n", vioapicBase);
+	VB_DEBUG_MSG("vbi_get_vioapic_vec: base @ 0x%x\n", vioapicBase);
 
 	vector = pVioapic->entry[irq].vioapicLow.field.vector;
 
-	VB_DEBUG_MSG("vbiVioapicVectorGet: vector %d: for irq 0x%x\n", vector, irq);
+	VB_DEBUG_MSG("vbi_get_vioapic_vec: vector %d: for irq 0x%x\n", vector, irq);
 
 	return vector;
 }
-- 
1.6.5.2

