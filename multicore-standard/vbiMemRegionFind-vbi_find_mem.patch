From 3ac30f7a8f9c39ed76cd0c3d47540e1357877cb8 Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:35:02 -0500
Subject: [PATCH 60/80] vbiMemRegionFind --> vbi_find_mem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/shmem.h |    2 +-
 kernel/vbi/shmem.c  |    6 +++---
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/include/vbi/shmem.h b/include/vbi/shmem.h
index 6c90137..fc5fc46 100644
--- a/include/vbi/shmem.h
+++ b/include/vbi/shmem.h
@@ -28,7 +28,7 @@ This header file declares the vbi API for managing memory regions
 extern vbiStatus_t vbiShmemRegionFind(int8_t *smName, void **addr,
 		uint32_t *length, uint32_t *attr);
 
-extern vbiStatus_t vbiMemRegionFind( int8_t * name, void **addr,       
+extern vbiStatus_t vbi_find_mem( int8_t * name, void **addr,       
 		uint32_t *length, uint32_t *attr);
 
 /* find the base address and length of a core's private memory */
diff --git a/kernel/vbi/shmem.c b/kernel/vbi/shmem.c
index 6b004c1..2b270ce 100644
--- a/kernel/vbi/shmem.c
+++ b/kernel/vbi/shmem.c
@@ -23,7 +23,7 @@ configuration structure. There is a descriptor table for each type of memory.
 MEMORY TYPES
 
 There is a memory memory region assigned only to each board which can be
-retrieved by calling vbiMemRegionFind().
+retrieved by calling vbi_find_mem().
 
 There is a shared memory which may be shared with other boards in the
 system. The shared memory can be retrieved by using vbiShmemRegionFind()
@@ -73,7 +73,7 @@ vbiStatus_t vbiShmemRegionFind(int8_t *smName, void **addr,
 EXPORT_SYMBOL(vbiShmemRegionFind);
 
 /*
- * vbiMemRegionFind - locate the memory parameters for a given region
+ * vbi_find_mem - locate the memory parameters for a given region
  *
  * This function finds the memory region associated with the name specified.
  * The first argument to this function indicates the name of the memory to find
@@ -82,7 +82,7 @@ EXPORT_SYMBOL(vbiShmemRegionFind);
  * arguments.
  *
  */
-vbiStatus_t vbiMemRegionFind(int8_t *name, void **addr,
+vbiStatus_t vbi_find_mem(int8_t *name, void **addr,
 				uint32_t *length, uint32_t *attr)
 {
 	struct vb_config *config = VBI_CONFIG_ADDR_GET();
-- 
1.6.5.2

