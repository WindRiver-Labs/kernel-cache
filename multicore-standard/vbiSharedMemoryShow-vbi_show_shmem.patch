From 623270dd2d08cfa11d8277e31d57430ffb7e401c Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:35:07 -0500
Subject: [PATCH 66/80] vbiSharedMemoryShow --> vbi_show_shmem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/vbi.h |    2 +-
 kernel/vbi/show.c |    6 +++---
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/include/vbi/vbi.h b/include/vbi/vbi.h
index 89809f9..f57e7e3 100644
--- a/include/vbi/vbi.h
+++ b/include/vbi/vbi.h
@@ -539,7 +539,7 @@ extern vbiStatus_t vbi_guest_phys_to_phys(vbiGuestPhysAddr_t gaddr,
 				vbiPhysAddr_t  *paddr);
 
 extern void vbiVioapicDisplay(void);
-extern void vbiSharedMemoryShow(struct vb_config *config);
+extern void vbi_show_shmem(struct vb_config *config);
 extern void vbiStatusShow(struct vb_status *p);
 extern void vbi_show_ctrl(struct vb_control *p);
 extern void vbi_show_cfg(struct vb_config *p);
diff --git a/kernel/vbi/show.c b/kernel/vbi/show.c
index 16cac83..138eafc 100644
--- a/kernel/vbi/show.c
+++ b/kernel/vbi/show.c
@@ -27,7 +27,7 @@ control and configuration data structures.
 #include <vbi/vbi.h>
 
 /*
- * vbiSharedMemoryShow - print information about the shared memory
+ * vbi_show_shmem - print information about the shared memory
  * configuration
  *
  * This routine traverse the array of shared memories descriptor for a
@@ -35,7 +35,7 @@ control and configuration data structures.
  *
  */
 
-void vbiSharedMemoryShow(struct vb_config *config)
+void vbi_show_shmem(struct vb_config *config)
 {
 	struct vb_sm_info *p = config->sharedMemoryRegionsConfigAddress;
 	int num_sm = config->num_sm;
@@ -184,6 +184,6 @@ void vbi_show_cfg(struct vb_config *p)
 	printk("num_ints:             %d\n", p->num_ints);
 
 	vbi_show_mem(p);
-	vbiSharedMemoryShow(p);
+	vbi_show_shmem(p);
 	vbi_show_irq(p);
 }
-- 
1.6.5.2

