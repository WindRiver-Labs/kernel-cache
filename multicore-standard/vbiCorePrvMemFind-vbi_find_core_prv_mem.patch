From f69d87bd35f29baa5ceddfe827355da70ef8a66a Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:34:55 -0500
Subject: [PATCH 50/80] vbiCorePrvMemFind --> vbi_find_core_prv_mem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/shmem.h |    2 +-
 kernel/vbi/shmem.c  |    6 +++---
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/include/vbi/shmem.h b/include/vbi/shmem.h
index 99447d7..6c90137 100644
--- a/include/vbi/shmem.h
+++ b/include/vbi/shmem.h
@@ -32,7 +32,7 @@ extern vbiStatus_t vbiMemRegionFind( int8_t * name, void **addr,
 		uint32_t *length, uint32_t *attr);
 
 /* find the base address and length of a core's private memory */
-extern vbiStatus_t vbiCorePrvMemFind (void **addr, size_t *length);  
+extern vbiStatus_t vbi_find_core_prv_mem (void **addr, size_t *length);  
 
 #endif /* _ASMLANGUAGE */
 
diff --git a/kernel/vbi/shmem.c b/kernel/vbi/shmem.c
index 30afff4..6b004c1 100644
--- a/kernel/vbi/shmem.c
+++ b/kernel/vbi/shmem.c
@@ -29,7 +29,7 @@ There is a shared memory which may be shared with other boards in the
 system. The shared memory can be retrieved by using vbiShmemRegionFind()
 
 Each core in the system is assigned a private memory region. The base address
-and the length of this region may obtained by calling vbiCorePrvMemFind ()
+and the length of this region may obtained by calling vbi_find_core_prv_mem ()
 Alternatively the following macros can be used to obtained similar information
 
 */
@@ -104,13 +104,13 @@ vbiStatus_t vbiMemRegionFind(int8_t *name, void **addr,
 }
 
 /*
- * vbiCorePrvMemFind - locate the private memory for a core
+ * vbi_find_core_prv_mem - locate the private memory for a core
  *
  * This function gets the base address of the private memory region assigned to
  * the running core.
  *
  */
-vbiStatus_t vbiCorePrvMemFind(void **addr, size_t *length)
+vbiStatus_t vbi_find_core_prv_mem(void **addr, size_t *length)
 {
 	*addr = (void *)VBI_VCORE_PRIVMEM_BASE_GET();
 	*length = VBI_VCORE_PRIVMEM_SIZE_GET();
-- 
1.6.5.2

