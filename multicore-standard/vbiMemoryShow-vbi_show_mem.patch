From 0d1cc96d066106400fccfe33be6d1a323474aa35 Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:35:02 -0500
Subject: [PATCH 59/80] vbiMemoryShow --> vbi_show_mem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/vbi.h |    2 +-
 kernel/vbi/show.c |    6 +++---
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/include/vbi/vbi.h b/include/vbi/vbi.h
index 97c4fea..89809f9 100644
--- a/include/vbi/vbi.h
+++ b/include/vbi/vbi.h
@@ -543,7 +543,7 @@ extern void vbiSharedMemoryShow(struct vb_config *config);
 extern void vbiStatusShow(struct vb_status *p);
 extern void vbi_show_ctrl(struct vb_control *p);
 extern void vbi_show_cfg(struct vb_config *p);
-extern void vbiMemoryShow(struct vb_config *config);
+extern void vbi_show_mem(struct vb_config *config);
 extern void vbiStsRegsDisplay(void);
 extern void vbi_disp_ctrl_regs(void);
 
diff --git a/kernel/vbi/show.c b/kernel/vbi/show.c
index 89c8f30..16cac83 100644
--- a/kernel/vbi/show.c
+++ b/kernel/vbi/show.c
@@ -54,7 +54,7 @@ void vbiSharedMemoryShow(struct vb_config *config)
 }
 
 /*
- * vbiMemoryShow - print information about a board's  memory
+ * vbi_show_mem - print information about a board's  memory
  * configuration
  *
  * This routine traverse the array of memory regions descriptor for a
@@ -63,7 +63,7 @@ void vbiSharedMemoryShow(struct vb_config *config)
  * RETURNS: returns OK or errno in case of failure
  *
  */
-void vbiMemoryShow(struct vb_config *config)
+void vbi_show_mem(struct vb_config *config)
 {
 	struct vb_mem_info *p = config->memoryRegionsConfigAddress;
 	int num_sm = config->num_mem;
@@ -183,7 +183,7 @@ void vbi_show_cfg(struct vb_config *p)
 
 	printk("num_ints:             %d\n", p->num_ints);
 
-	vbiMemoryShow(p);
+	vbi_show_mem(p);
 	vbiSharedMemoryShow(p);
 	vbi_show_irq(p);
 }
-- 
1.6.5.2

