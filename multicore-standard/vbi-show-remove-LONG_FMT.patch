From c7c05e25c3399ce5e27351e28b832ff5c908a7b5 Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Fri, 23 Oct 2009 16:31:42 -0400
Subject: [PATCH 7/8] vbi show: remove LONG_FMT

Not sure what the original intention was here, but it seems
rather useless, so it goes away.

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 kernel/vbi/show.c |    7 +++----
 1 files changed, 3 insertions(+), 4 deletions(-)

diff --git a/kernel/vbi/show.c b/kernel/vbi/show.c
index 2f3507c..05e4edf 100644
--- a/kernel/vbi/show.c
+++ b/kernel/vbi/show.c
@@ -22,7 +22,6 @@ control and configuration data structures.
 
 */
 
-#define LONG_FMT "0x%lx"
 #include <linux/types.h>
 #include <linux/kernel.h>
 #include <vbi/vbi.h>
@@ -166,10 +165,10 @@ void vbiConfigShow(VB_CONFIG *p)
 	VB_PRINTF("pid:                %d\n", p->pid);
 	VB_PRINTF("Board ID:           %d\n", p->boardID);
 
-	VB_PRINTF("Status Address:     " LONG_FMT "\n", p->vbStatus);
-	VB_PRINTF("Control Address:    " LONG_FMT "\n", p->vbControl);
+	VB_PRINTF("Status Address:     0x%lx\n", p->vbStatus);
+	VB_PRINTF("Control Address:    0x%lx\n", p->vbControl);
 #if defined (_WRHV_ARCH_HAS_VB_SYSTBL)
-	VB_PRINTF("Syscall Table Address:    " LONG_FMT "\n",
+	VB_PRINTF("Syscall Table Address:    0x%lx\n",
 			p->vbSyscallTable);
 #endif
 	VB_PRINTF("SupervisoryMode:    %d\n", p->supervisoryMode);
-- 
1.6.5.rc3

