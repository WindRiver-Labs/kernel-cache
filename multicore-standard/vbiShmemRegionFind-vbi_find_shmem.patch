From 9053a18f8bfa2426b1ee5ae31102d1e98447951c Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Wed, 18 Nov 2009 12:35:07 -0500
Subject: [PATCH 67/80] vbiShmemRegionFind --> vbi_find_shmem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/shmem.h |    2 +-
 kernel/vbi/shmem.c  |    8 ++++----
 2 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/include/vbi/shmem.h b/include/vbi/shmem.h
index fc5fc46..d91d8e7 100644
--- a/include/vbi/shmem.h
+++ b/include/vbi/shmem.h
@@ -25,7 +25,7 @@ This header file declares the vbi API for managing memory regions
 
 #ifndef	_ASMLANGUAGE
 
-extern vbiStatus_t vbiShmemRegionFind(int8_t *smName, void **addr,
+extern vbiStatus_t vbi_find_shmem(int8_t *smName, void **addr,
 		uint32_t *length, uint32_t *attr);
 
 extern vbiStatus_t vbi_find_mem( int8_t * name, void **addr,       
diff --git a/kernel/vbi/shmem.c b/kernel/vbi/shmem.c
index 2b270ce..3f932f6 100644
--- a/kernel/vbi/shmem.c
+++ b/kernel/vbi/shmem.c
@@ -26,7 +26,7 @@ There is a memory memory region assigned only to each board which can be
 retrieved by calling vbi_find_mem().
 
 There is a shared memory which may be shared with other boards in the
-system. The shared memory can be retrieved by using vbiShmemRegionFind()
+system. The shared memory can be retrieved by using vbi_find_shmem()
 
 Each core in the system is assigned a private memory region. The base address
 and the length of this region may obtained by calling vbi_find_core_prv_mem ()
@@ -41,7 +41,7 @@ Alternatively the following macros can be used to obtained similar information
 #include <vbi/private.h>
 
 /*
- * vbiShmemRegionFind - locate the shared memory parameters for a given region
+ * vbi_find_shmem - locate the shared memory parameters for a given region
  *
  * This function finds the shared memory region associated with the name
  * specified. The first argument to this function indicates the name of the
@@ -50,7 +50,7 @@ Alternatively the following macros can be used to obtained similar information
  * arguments.
  *
  */
-vbiStatus_t vbiShmemRegionFind(int8_t *smName, void **addr,
+vbiStatus_t vbi_find_shmem(int8_t *smName, void **addr,
 				uint32_t *length, uint32_t *attr)
 {
 	struct vb_config *config = VBI_CONFIG_ADDR_GET();
@@ -70,7 +70,7 @@ vbiStatus_t vbiShmemRegionFind(int8_t *smName, void **addr,
 	*addr = 0;
 	return VBI_INVALID_SHMEM;
 }
-EXPORT_SYMBOL(vbiShmemRegionFind);
+EXPORT_SYMBOL(vbi_find_shmem);
 
 /*
  * vbi_find_mem - locate the memory parameters for a given region
-- 
1.6.5.2

