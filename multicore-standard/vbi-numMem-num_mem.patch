From c3a4c7b547af58aa49396d7e8218a40f606c5f9e Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Fri, 30 Oct 2009 00:02:01 -0400
Subject: [PATCH 22/26] vbi: numMem -> num_mem

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 include/vbi/interface.h |    2 +-
 kernel/vbi/shmem.c      |    2 +-
 kernel/vbi/show.c       |    2 +-
 3 files changed, 3 insertions(+), 3 deletions(-)

diff --git a/include/vbi/interface.h b/include/vbi/interface.h
index 350858d..aa616c5 100644
--- a/include/vbi/interface.h
+++ b/include/vbi/interface.h
@@ -546,7 +546,7 @@ struct vb_config
 	uint32_t phys_mem_size;	/* the vbPhysical size of RAM */
 	uint32_t mem_alias_addr;	/* address to which memory is aliased */
 	uint32_t mem_alias_size;	/* size of aliases memory */
-	uint32_t numMem;		/* number of memory regions */
+	uint32_t num_mem;		/* number of memory regions */
 
 	/*
 	 *  shared memory configuration settings
diff --git a/kernel/vbi/shmem.c b/kernel/vbi/shmem.c
index 2c8d847..a029703 100644
--- a/kernel/vbi/shmem.c
+++ b/kernel/vbi/shmem.c
@@ -90,7 +90,7 @@ vbiStatus_t vbiMemRegionFind(int8_t *name, void **addr,
 {
 	struct vb_config *config = VBI_CONFIG_ADDR_GET();
 	struct vb_mem_info *info = config->memoryRegionsConfigAddress;
-	int32_t num  = config->numMem;
+	int32_t num  = config->num_mem;
 	int32_t i;
 
 	for (i = 0; i < num; i++, info++)
diff --git a/kernel/vbi/show.c b/kernel/vbi/show.c
index b81ff59..2210017 100644
--- a/kernel/vbi/show.c
+++ b/kernel/vbi/show.c
@@ -67,7 +67,7 @@ void vbiSharedMemoryShow(struct vb_config *config)
 void vbiMemoryShow(struct vb_config *config)
 {
 	struct vb_mem_info *p = config->memoryRegionsConfigAddress;
-	int numSm = config->numMem;
+	int numSm = config->num_mem;
 	int i;
 
 	printk("%d memory regions, Config at 0x%x:\n", numSm, p);
-- 
1.6.5.2

