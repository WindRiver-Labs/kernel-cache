From b0d94a60f0d91e4b534cad4fd1c0413b805a1be5 Mon Sep 17 00:00:00 2001
From: Andrew Liu <shengping.liu@windriver.com>
Date: Tue, 3 Aug 2010 16:09:41 +0800
Subject: [PATCH 168/252] bman: clarify usage of params in header comments.

It is from FSL vendor SDK 2.x.

The struct parameter is copied by the driver and it is
the copy that is modified, not the original. The comments
 were misleading about this.

Signed-off-by: Geoff Thorpe <Geoff.Thorpe@freescale.com>
---
 include/linux/fsl_bman.h |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/include/linux/fsl_bman.h b/include/linux/fsl_bman.h
index 20c756d..d323a5d 100644
--- a/include/linux/fsl_bman.h
+++ b/include/linux/fsl_bman.h
@@ -372,7 +372,7 @@ typedef void (*bman_cb_depletion)(struct bman_portal *bm,
 
 /* This struct specifies parameters for a bman_pool object. */
 struct bman_pool_params {
-	/* index of the buffer pool to encapsulate (0-63), overwritten if
+	/* index of the buffer pool to encapsulate (0-63), ignored if
 	 * BMAN_POOL_FLAG_DYNAMIC_BPID is set. */
 	u32 bpid;
 	/* bit-mask of BMAN_POOL_FLAG_*** options */
-- 
1.6.5.2

