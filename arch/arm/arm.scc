# patches are for everyone, but the kconfig data is just for ARM builds.
if [ "$WRS_ARCH" = "arm" ]; then
	kconf hardware arm.cfg
fi
patch DSS2-fix-a-compile-problem-in-DSS2.patch
patch omapfb-Fix-omapfb_get_recommended_bpp.patch
patch OMAP-fix-a-gpmc-nand-problem.patch
