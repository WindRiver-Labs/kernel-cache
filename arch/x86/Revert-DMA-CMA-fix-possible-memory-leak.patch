From 0948d99a77a55898c70079c1f643b6f0d93b4f9f Mon Sep 17 00:00:00 2001
From: chunguang <chunguang.yang@windriver.com>
Date: Wed, 22 Oct 2014 15:13:41 +0800
Subject: [PATCH 01/10] Revert "DMA, CMA: fix possible memory leak"

This reverts commit 300e56a4b57a8be6f7aae7328bf23a4e32b53302. this commit
was fixing a memory leak, but conflict with new implementation, the new
implementation addressed the memory leak in anther way.
static int __init cma_activate_area(struct cma *cma)
                 base_pfn = pfn;
                 for (j = pageblock_nr_pages; j; --j, pfn++) {
                         WARN_ON_ONCE(!pfn_valid(pfn));
+                       /*
+                        * alloc_contig_range requires the pfn range
+                        * specified to be in the same zone. Make this
+                        * simple by forcing the entire CMA resv range
+                        * to be in the same zone.
+                        */
                         if (page_zone(pfn_to_page(pfn)) != zone)
-                               return -EINVAL;
+                               goto err;
                 }
                 init_cma_reserved_pageblock(pfn_to_page(base_pfn));
         } while (--i);

         mutex_init(&cma->lock);
+
+err:
+       kfree(cma->bitmap);
+       return -EINVAL;

Signed-off-by: Chunguang Yang <cyang@pek-lpgtest3.wrs.com>
---
 drivers/base/dma-contiguous.c |   12 +-----------
 1 files changed, 1 insertions(+), 11 deletions(-)

diff --git a/drivers/base/dma-contiguous.c b/drivers/base/dma-contiguous.c
index d3bffa4..165c2c2 100644
--- a/drivers/base/dma-contiguous.c
+++ b/drivers/base/dma-contiguous.c
@@ -155,23 +155,13 @@ static int __init cma_activate_area(struct cma *cma)
 		base_pfn = pfn;
 		for (j = pageblock_nr_pages; j; --j, pfn++) {
 			WARN_ON_ONCE(!pfn_valid(pfn));
-			/*
-			 * alloc_contig_range requires the pfn range
-			 * specified to be in the same zone. Make this
-			 * simple by forcing the entire CMA resv range
-			 * to be in the same zone.
-			 */
 			if (page_zone(pfn_to_page(pfn)) != zone)
-				goto err;
+				return -EINVAL;
 		}
 		init_cma_reserved_pageblock(pfn_to_page(base_pfn));
 	} while (--i);
 
 	return 0;
-
-err:
-	kfree(cma->bitmap);
-	return -EINVAL;
 }
 
 static struct cma cma_areas[MAX_CMA_AREAS];
-- 
1.7.5.4

