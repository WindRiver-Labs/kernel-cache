From ac3804660db223db4c4c5d9779c469e835afa0ee Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Sat, 19 Feb 2011 13:56:45 -0500
Subject: [PATCH] rt: delete spurious 80211b change incorrectly placed in RT merge

This was taken from an upstream commit, not from tip/RT and was
bundled into "preempt-rt: -rt20 applied to 2.6.34 -standard"

It doesn't have anything to do with RT specifically, so remove it.

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 net/mac80211/rx.c |    5 -----
 1 files changed, 0 insertions(+), 5 deletions(-)

diff --git a/net/mac80211/rx.c b/net/mac80211/rx.c
index d8da610..4d67bb8 100644
--- a/net/mac80211/rx.c
+++ b/net/mac80211/rx.c
@@ -2356,11 +2356,6 @@ static int prepare_for_handlers(struct ieee80211_sub_if_data *sdata,
 		/* should never get here */
 		WARN_ON(1);
 		break;
-	case MESH_PLINK_CATEGORY:
-	case MESH_PATH_SEL_CATEGORY:
-		if (ieee80211_vif_is_mesh(&sdata->vif))
-			return ieee80211_mesh_rx_mgmt(sdata, rx->skb);
-		break;
 	}
 
 	return 1;
-- 
1.7.4

