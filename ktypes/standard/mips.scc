# TODO: this will be: scc_src <foo> <branch>

if [ -e /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-2.6.34/git/linux-mips ]; then
  git fetch /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-2.6.34/git/linux-mips +mips_merge:mips_merge
else
   git show-ref --quiet --verify -- "refs/heads/mips_merge";
   if [ $? -eq 1 ]; then 
      git branch mips_merge wrs_base
   fi
fi

