# TODO: this will be: scc_src <foo> <branch>

if [ -e /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-edge/git/linux-omap-2.6 ]; then
  git fetch /opt/wrs.git/master/wrlinux-x/layers/wrll-linux-edge/git/linux-omap-2.6 +master:omap_merge
else
   git show-ref --quiet --verify -- "refs/heads/omap_merge";
   if [ $? -eq 1 ]; then 
      git branch omap_merge wrs_base
   fi
fi

