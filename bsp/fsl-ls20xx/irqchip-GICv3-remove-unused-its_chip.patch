From a9a6931ef41ed9d10aa3ab0d7d2236913a197b08 Mon Sep 17 00:00:00 2001
From: Yanjiang Jin <yanjiang.jin@windriver.com>
Date: Tue, 4 Aug 2015 10:18:29 +0800
Subject: [PATCH] irqchip: GICv3: remove unused its_chip

commit 17040291694c8a2ea91f7830479a1381367d2d53

    irqchip: GICv3: Convert to domain hierarchy

already removed the calling of its_chip, we don't need this now.

Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 drivers/irqchip/irq-gic-v3.c |    4 +---
 1 files changed, 1 insertions(+), 3 deletions(-)

diff --git a/drivers/irqchip/irq-gic-v3.c b/drivers/irqchip/irq-gic-v3.c
index 3149155..a3767ad 100644
--- a/drivers/irqchip/irq-gic-v3.c
+++ b/drivers/irqchip/irq-gic-v3.c
@@ -599,8 +599,6 @@ static struct irq_chip gic_chip = {
 	.irq_set_affinity	= gic_set_affinity,
 };
 
-static struct irq_chip *its_chip;
-
 #define GIC_ID_NR		(1U << gic_data.rdist.id_bits)
 
 static int gic_irq_domain_map(struct irq_domain *d, unsigned int irq,
@@ -788,7 +786,7 @@ static int __init gic_of_init(struct device_node *node, struct device_node *pare
 	set_handle_irq(gic_handle_irq);
 
 	if (gic_dist_supports_lpis())
-		its_chip = its_init(node, &gic_data.rdist, gic_data.domain);
+		its_init(node, &gic_data.rdist, gic_data.domain);
 
 	gic_smp_init();
 	gic_dist_init();
-- 
1.7.5.4

