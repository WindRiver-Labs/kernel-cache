From 8a8aa0f6fc62f8f9015aa669bacec22a774e7786 Mon Sep 17 00:00:00 2001
From: Wally Gleemer <Wally.Gleemer@windriver.com>
Date: Thu, 13 Mar 2008 21:16:48 -0700
Subject: [PATCH] cavium: add in mpl/mtp offsets

Add in the MPL and MTP offsets for the Cavium into the asm-offsets
for mips.

Signed-off-by: Wally Gleemer <Wally.Gleemer@windriver.com>
---
 arch/mips/kernel/asm-offsets.c |    4 ++++
 1 files changed, 4 insertions(+), 0 deletions(-)

diff --git a/arch/mips/kernel/asm-offsets.c b/arch/mips/kernel/asm-offsets.c
index ca13629..92ebfa7 100644
--- a/arch/mips/kernel/asm-offsets.c
+++ b/arch/mips/kernel/asm-offsets.c
@@ -74,6 +74,10 @@ void output_ptreg_defines(void)
 #ifdef CONFIG_MIPS_MT_SMTC
 	offset("#define PT_TCSTATUS  ", struct pt_regs, cp0_tcstatus);
 #endif /* CONFIG_MIPS_MT_SMTC */
+#ifdef CONFIG_CPU_CAVIUM_OCTEON
+	offset("#define PT_MPL    ", struct pt_regs, mpl);
+	offset("#define PT_MTP    ", struct pt_regs, mtp);
+#endif
 	size("#define PT_SIZE   ", struct pt_regs);
 	linefeed;
 }
-- 
1.5.5.1

