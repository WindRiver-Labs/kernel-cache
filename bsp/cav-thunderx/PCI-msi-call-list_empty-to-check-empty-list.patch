From d45daa632637295eaf2fd4292002a93ca9a99146 Mon Sep 17 00:00:00 2001
From: Jiang Lu <lu.jiang@windriver.com>
Date: Fri, 7 Aug 2015 15:18:04 +0800
Subject: [PATCH 2/2] PCI/msi:call list_empty to check empty list

We should call list_empty() to check empty list.

Signed-off-by: Jiang Lu <lu.jiang@windriver.com>
---
 drivers/pci/msi.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/pci/msi.c b/drivers/pci/msi.c
index 0677a21..33ca1cd 100644
--- a/drivers/pci/msi.c
+++ b/drivers/pci/msi.c
@@ -508,7 +508,7 @@ static void __pci_restore_msix_state(struct pci_dev *dev)
 
 	if (!dev->msix_enabled)
 		return;
-	BUG_ON(dev_to_msi_list(&dev->dev));
+	BUG_ON(list_empty(dev_to_msi_list(&dev->dev)));
 	entry = first_msi_entry(&dev->dev);
 	pci_read_config_word(dev, dev->msix_cap + PCI_MSIX_FLAGS, &control);
 
@@ -996,7 +996,7 @@ void pci_msi_shutdown(struct pci_dev *dev)
 	if (!pci_msi_enable || !dev || !dev->msi_enabled)
 		return;
 
-	BUG_ON(dev_to_msi_list(&dev->dev));
+	BUG_ON(list_empty(dev_to_msi_list(&dev->dev)));
 	desc = first_msi_entry(&dev->dev);
 
 	msi_set_enable(dev, 0);
-- 
1.7.5.4

