From 5091e1e9a1a079231c074309f25c00b846511ffd Mon Sep 17 00:00:00 2001
From: Guoqing Jiang <Guoqing.Jiang@windriver.com>
Date: Tue, 5 Jul 2011 10:45:49 +0800
Subject: [PATCH 12/13] TI816x preempt_rt: no need to protect cppi41dma_Interrupt with raw_spin_lock_irqsave/raw_spin_unlock_irqrestore

cppi41dma_Interrupt can be call with enable preempt, and to avoid latency
issue, we should remove raw_spin_lock_irqsave/raw_spin_unlock_irqrestore
in cppi41dma_Interrupt since this ISR is invoked frequently.

Signed-off-by: Guoqing Jiang <Guoqing.Jiang@windriver.com>
---
 drivers/usb/musb/ti816x.c |    5 -----
 1 files changed, 0 insertions(+), 5 deletions(-)

diff --git a/drivers/usb/musb/ti816x.c b/drivers/usb/musb/ti816x.c
index 1bf0e8a..ef4fa2d 100644
--- a/drivers/usb/musb/ti816x.c
+++ b/drivers/usb/musb/ti816x.c
@@ -848,10 +848,8 @@ static irqreturn_t cppi41dma_Interrupt(int irq, void *hci)
 	u32 usb0_tx_intr, usb0_rx_intr;
 	u32 usb1_tx_intr, usb1_rx_intr;
 	void *q_mgr_base = cppi41_queue_mgr[0].q_mgr_rgn_base;
-	unsigned long flags;
 
 	musb = hci;
-	raw_spin_lock_irqsave(&musb->lock, flags);
 	/*
 	 * CPPI 4.1 interrupts share the same IRQ and the EOI register but
 	 * don't get reflected in the interrupt source/mask registers.
@@ -889,7 +887,6 @@ static irqreturn_t cppi41dma_Interrupt(int irq, void *hci)
 
 		DBG(4, "CPPI 4.1 IRQ: Tx %x, Rx %x\n", usb0_tx_intr,
 					usb0_rx_intr);
-		raw_spin_unlock_irqrestore(&musb->lock, flags);
 		if (usb0_tx_intr || usb0_rx_intr) {
 			cppi41_completion(gmusb[0], usb0_rx_intr,
 						usb0_tx_intr);
@@ -903,10 +900,8 @@ static irqreturn_t cppi41dma_Interrupt(int irq, void *hci)
 				usb1_tx_intr);
 			ret = IRQ_HANDLED;
 		}
-		raw_spin_lock_irqsave(&musb->lock, flags);
 		usbss_write(USBSS_IRQ_EOI, 0);
 	}
-	raw_spin_unlock_irqrestore(&musb->lock, flags);
 	return ret;
 }
 #endif
-- 
1.7.0.4

