From 49b9a1f5e1ca032f1ba35e430be5ead7b9ee457c Mon Sep 17 00:00:00 2001
From: Zumeng Chen <zumeng.chen@windriver.com>
Date: Fri, 18 Nov 2011 15:28:47 +0800
Subject: [PATCH 30/41] usb: ar71xx: fix HC_LENGTH error usage

mainline has supported controllers with big endian capability regs
so here, we keep the consistency with the following commit ID:
  c430131a02d677aa708f56342c1565edfdacb3c0

Signed-off-by: Zumeng Chen <zumeng.chen@windriver.com>
---
 drivers/usb/host/ehci-ar71xx.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/usb/host/ehci-ar71xx.c b/drivers/usb/host/ehci-ar71xx.c
index b08db5b..7a1c641 100644
--- a/drivers/usb/host/ehci-ar71xx.c
+++ b/drivers/usb/host/ehci-ar71xx.c
@@ -26,7 +26,7 @@ static int ehci_ar71xx_init(struct usb_hcd *hcd)
 
 	ehci->caps = hcd->regs;
 	ehci->regs = hcd->regs +
-			HC_LENGTH(ehci_readl(ehci, &ehci->caps->hc_capbase));
+		HC_LENGTH(ehci,ehci_readl(ehci, &ehci->caps->hc_capbase));
 	ehci->hcs_params = ehci_readl(ehci, &ehci->caps->hcs_params);
 
 	ehci->sbrn = 0x20;
@@ -50,7 +50,7 @@ static int ehci_ar91xx_init(struct usb_hcd *hcd)
 
 	ehci->caps = hcd->regs + 0x100;
 	ehci->regs = hcd->regs + 0x100 +
-			HC_LENGTH(ehci_readl(ehci, &ehci->caps->hc_capbase));
+		HC_LENGTH(ehci, ehci_readl(ehci, &ehci->caps->hc_capbase));
 	ehci->hcs_params = ehci_readl(ehci, &ehci->caps->hcs_params);
 
 	hcd->has_tt = 1;
-- 
1.7.4.1

