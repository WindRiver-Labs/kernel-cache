From ff5a9539ee485fffea993401166bec3f9492acb6 Mon Sep 17 00:00:00 2001
From: "Patil, Rachna" <rachna@ti.com>
Date: Tue, 7 Aug 2012 12:40:10 +0800
Subject: [PATCH 520/609] input: TSC: Remove support for TSC on alpha EVM

Original commit: a390efb7f9296d66fc258d22a94277dbfcfee851

  From git://arago-project.org/git/projects/linux-am33x.git
  And ti-sdk-am335x-evm-05.05.00.00-Linux-x86-Install image

4 wire analog inputs on alpha EVM's were interchanged, Alpha AM335x
EVM's are not supported anymore.  Code related to this is dropped.

Signed-off-by: Patil, Rachna <rachna@ti.com>
---
 drivers/input/touchscreen/ti_tscadc.c |    9 +++------
 1 files changed, 3 insertions(+), 6 deletions(-)

diff --git a/drivers/input/touchscreen/ti_tscadc.c b/drivers/input/touchscreen/ti_tscadc.c
index abf879c..7980a74 100644
--- a/drivers/input/touchscreen/ti_tscadc.c
+++ b/drivers/input/touchscreen/ti_tscadc.c
@@ -116,7 +116,7 @@ static void tscadc_writel(struct tscadc *tsc, unsigned int reg,
 	writel(val, tsc->tsc_base + reg);
 }
 
-static void tscadc_step_config(struct tscadc *ts_dev)
+static void step_config(struct tscadc *ts_dev)
 {
 	unsigned int	config;
 	int i;
@@ -144,7 +144,6 @@ static void tscadc_step_config(struct tscadc *ts_dev)
 		tscadc_writel(ts_dev, REG_STEPDELAY(i), STEPCONFIG_OPENDLY);
 	}
 
-	config = 0;
 	config = STEPCONFIG_MODE_HWSYNC |
 			STEPCONFIG_SAMPLES_AVG | STEPCONFIG_YNN |
 			STEPCONFIG_INM | STEPCONFIG_FIFO1;
@@ -166,7 +165,6 @@ static void tscadc_step_config(struct tscadc *ts_dev)
 		tscadc_writel(ts_dev, REG_STEPDELAY(i), STEPCONFIG_OPENDLY);
 	}
 
-	config = 0;
 	/* Charge step configuration */
 	config = STEPCONFIG_XPP | STEPCONFIG_YNN |
 			STEPCHARGE_RFP | STEPCHARGE_RFM |
@@ -175,7 +173,6 @@ static void tscadc_step_config(struct tscadc *ts_dev)
 	tscadc_writel(ts_dev, REG_CHARGECONFIG, config);
 	tscadc_writel(ts_dev, REG_CHARGEDELAY, STEPCHARGE_DELAY);
 
-	config = 0;
 	/* Configure to calculate pressure */
 	config = STEPCONFIG_MODE_HWSYNC |
 			STEPCONFIG_SAMPLES_AVG | STEPCONFIG_YPP |
@@ -411,7 +408,7 @@ static int __devinit tscadc_probe(struct platform_device *pdev)
 
 	tscadc_idle_config(ts_dev);
 	tscadc_writel(ts_dev, REG_IRQENABLE, IRQENB_FIFO1THRES);
-	tscadc_step_config(ts_dev);
+	step_config(ts_dev);
 	tscadc_writel(ts_dev, REG_FIFO1THR, 6);
 
 	ctrl |= CNTRLREG_TSCSSENB;
@@ -519,7 +516,7 @@ static int tscadc_resume(struct platform_device *pdev)
 	tscadc_writel(ts_dev, REG_CTRL, restore);
 
 	tsc_idle_config(ts_dev);
-	tsc_step_config(ts_dev);
+	step_config(ts_dev);
 	tscadc_writel(ts_dev, REG_FIFO1THR, 6);
 	restore = tscadc_readl(ts_dev, REG_CTRL);
 	tscadc_writel(ts_dev, REG_CTRL,
-- 
1.7.5.4

