From 0385535170988b6edcdcf1ce9a60f842b6225abc Mon Sep 17 00:00:00 2001
From: Zumeng Chen <zumeng.chen@windriver.com>
Date: Tue, 7 Aug 2012 17:41:23 +0800
Subject: [PATCH 324/609] ethernet: cpsw: fix warning info

This patch is pretty simple to fix compiling warning information for cpsw.

Signed-off-by: Zumeng Chen <zumeng.chen@windriver.com>
---
 drivers/net/ethernet/ti/cpsw.c |    8 ++++++--
 1 files changed, 6 insertions(+), 2 deletions(-)

diff --git a/drivers/net/ethernet/ti/cpsw.c b/drivers/net/ethernet/ti/cpsw.c
index 78ce517..57945c7 100644
--- a/drivers/net/ethernet/ti/cpsw.c
+++ b/drivers/net/ethernet/ti/cpsw.c
@@ -1872,7 +1872,7 @@ static inline void cpsw_add_vlan_ale_entry(struct cpsw_priv *priv,
 
 #endif
 
-static void cpsw_ndo_vlan_rx_add_vid(struct net_device *ndev,
+static int cpsw_ndo_vlan_rx_add_vid(struct net_device *ndev,
 		unsigned short vid)
 {
 	struct cpsw_priv *priv = netdev_priv(ndev);
@@ -1891,9 +1891,11 @@ static void cpsw_ndo_vlan_rx_add_vid(struct net_device *ndev,
 	}
 
 	spin_unlock(&priv->lock);
+
+	return 0;
 }
 
-static void cpsw_ndo_vlan_rx_kill_vid(struct net_device *ndev,
+static int cpsw_ndo_vlan_rx_kill_vid(struct net_device *ndev,
 		unsigned short vid)
 {
 	struct cpsw_priv *priv = netdev_priv(ndev);
@@ -1907,6 +1909,8 @@ static void cpsw_ndo_vlan_rx_kill_vid(struct net_device *ndev,
 	cpsw_ale_vlan_del_mcast(priv->ale, priv->ndev->broadcast, 0, vid);
 
 	spin_unlock(&priv->lock);
+
+	return 0;
 }
 
 #endif /* VLAN_SUPPORT */
-- 
1.7.5.4

