From 02357f9b3f25341a232c8f2e11b9a694d5c5bfa0 Mon Sep 17 00:00:00 2001
From: Xulin Sun <xulin.sun@windriver.com>
Date: Mon, 29 Feb 2016 07:16:13 +0000
Subject: [PATCH 2/2] pci-layerscape: init the uninitialized variable in
 ls_pcie_msi_irq_handler()

Give an initial value to the variable ret to avoid the below warning:
drivers/pci/host/pci-layerscape.c:121:9: warning: 'ret' may be used
uninitialized in this function [-Wmaybe-uninitialized]
  return ret;
         ^

Signed-off-by: Xulin Sun <xulin.sun@windriver.com>
---
 drivers/pci/host/pci-layerscape.c |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/drivers/pci/host/pci-layerscape.c b/drivers/pci/host/pci-layerscape.c
index 07ed09f..9968471 100644
--- a/drivers/pci/host/pci-layerscape.c
+++ b/drivers/pci/host/pci-layerscape.c
@@ -87,7 +87,7 @@ static irqreturn_t ls_pcie_msi_irq_handler(int irq, void *data)
 	struct pcie_port *pp = data;
 	struct ls_pcie *pcie = to_ls_pcie(pp);
 	unsigned long val;
-	int msi_irq, ret, pos;
+	int msi_irq, ret = IRQ_NONE, pos;
 
 	regmap_read(pcie->scfg, LS1021A_MSIR_OFF(pcie->index), (u32 *) &val);
 
-- 
1.7.5.4

