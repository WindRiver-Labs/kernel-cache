From f0a224d53ef9d342a2955ac77818be2b195a7e4c Mon Sep 17 00:00:00 2001
From: Xulin Sun <xulin.sun@windriver.com>
Date: Tue, 23 Feb 2016 16:32:25 +0800
Subject: [PATCH] ls1021a: Add clock controls for SAI dts file

This changes fix below failed init information:

fsl-sai 2b60000.sai: failed to get bus clock: -2
fsl-sai 2b60000.sai: failed to get mclk1 clock: -2
fsl-sai 2b60000.sai: failed to get mclk2 clock: -2
fsl-sai 2b60000.sai: failed to get mclk3 clock: -2

Based on the commit "ASoC: fsl_sai: Add clock controls for SAI", the dts
file should be changed accordingly since the sai driver has added the clock
control.

Signed-off-by: Xulin Sun <xulin.sun@windriver.com>
---
 arch/arm/boot/dts/ls1021a.dtsi |   12 ++++++++----
 1 files changed, 8 insertions(+), 4 deletions(-)

diff --git a/arch/arm/boot/dts/ls1021a.dtsi b/arch/arm/boot/dts/ls1021a.dtsi
index bc9d560..f7073d9 100644
--- a/arch/arm/boot/dts/ls1021a.dtsi
+++ b/arch/arm/boot/dts/ls1021a.dtsi
@@ -582,8 +582,10 @@
 			compatible = "fsl,vf610-sai";
 			reg = <0x0 0x2b50000 0x0 0x10000>;
 			interrupts = <GIC_SPI 132 IRQ_TYPE_LEVEL_HIGH>;
-			clocks = <&platform_clk 1>;
-			clock-names = "sai";
+		        clocks = <&platform_clk 1>,
+				 <&platform_clk 1>,
+		                 <&platform_clk 0>, <&platform_clk 0>;	
+			clock-names = "bus", "mclk1", "mclk2", "mclk3";
 			dma-names = "tx", "rx";
 			dmas = <&edma0 1 47>,
 				<&edma0 1 46>;
@@ -594,8 +596,10 @@
 			compatible = "fsl,vf610-sai";
 	       		reg = <0x0 0x2b60000 0x0 0x10000>;
 			interrupts = <GIC_SPI 133 IRQ_TYPE_LEVEL_HIGH>;
-			clocks = <&platform_clk 1>;
-	       		clock-names = "sai";
+	                clocks = <&platform_clk 1>,
+                                <&platform_clk 1>,
+                                <&platform_clk 0>, <&platform_clk 0>;
+	       		clock-names = "bus", "mclk1", "mclk2", "mclk3";
 			dma-names = "tx", "rx";
 			dmas = <&edma0 1 45>,
 				<&edma0 1 44>;
-- 
1.7.5.4

