From 09bbf5e33ae6fb79024ef0b95a9b5127374ba46d Mon Sep 17 00:00:00 2001
From: Michal Simek <michal.simek@xilinx.com>
Date: Tue, 6 May 2014 12:33:27 +0200
Subject: [PATCH 149/509] spi: cadence: Final syncup with mainline driver

https://github.com/analogdevicesinc/linux.git xcomm_zynq
commit e3c8db90117e4a2d2c4c29eddf129522557f004d

Trivial changes.

Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Zhong Hongbo <hongbo.zhong@windriver.com>
---
 drivers/spi/spi-cadence.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/spi/spi-cadence.c b/drivers/spi/spi-cadence.c
index 3278361..c1a4a60 100644
--- a/drivers/spi/spi-cadence.c
+++ b/drivers/spi/spi-cadence.c
@@ -238,7 +238,7 @@ static void cdns_spi_config_clock_mode(struct spi_device *spi)
  * controller.
  */
 static void cdns_spi_config_clock_freq(struct spi_device *spi,
-				       struct spi_transfer *transfer)
+				  struct spi_transfer *transfer)
 {
 	struct cdns_spi *xspi = spi_master_get_devdata(spi->master);
 	u32 ctrl_reg, baud_rate_val;
@@ -458,10 +458,10 @@ static int cdns_unprepare_transfer_hardware(struct spi_master *master)
 static int cdns_spi_probe(struct platform_device *pdev)
 {
 	int ret = 0, irq;
-	u32 num_cs;
 	struct spi_master *master;
 	struct cdns_spi *xspi;
 	struct resource *res;
+	u32 num_cs;
 
 	master = spi_alloc_master(&pdev->dev, sizeof(*xspi));
 	if (master == NULL)
-- 
1.7.5.4

