From b1ba63613008ef8340074ff74394e5ea17e99098 Mon Sep 17 00:00:00 2001
From: Harini Katakam <harini.katakam@xilinx.com>
Date: Thu, 27 Mar 2014 15:10:22 +0530
Subject: [PATCH 480/509] GPIO: zynq: Declare variables of same type on the
 same line.

https://github.com/analogdevicesinc/linux.git xcomm_zynq
commit 8cde60e0f908d2ce82238b2f09f209550bc7714c

Declare variables of same type on the same line where possible.

Signed-off-by: Harini Katakam <harinik@xilinx.com>
Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Zhong Hongbo <hongbo.zhong@windriver.com>
---
 drivers/gpio/gpio-zynq.c |    6 ++----
 1 files changed, 2 insertions(+), 4 deletions(-)

diff --git a/drivers/gpio/gpio-zynq.c b/drivers/gpio/gpio-zynq.c
index c6c8607..fec4c00 100644
--- a/drivers/gpio/gpio-zynq.c
+++ b/drivers/gpio/gpio-zynq.c
@@ -176,8 +176,7 @@ static void zynq_gpio_set_value(struct gpio_chip *chip, unsigned int pin,
 			       int state)
 {
 	unsigned long flags;
-	unsigned int reg_offset;
-	unsigned int bank_num, bank_pin_num;
+	unsigned int reg_offset, bank_num, bank_pin_num;
 	struct zynq_gpio *gpio = container_of(chip, struct zynq_gpio, chip);
 
 	zynq_gpio_get_bank_pin(pin, &bank_num, &bank_pin_num);
@@ -584,12 +583,11 @@ static const struct dev_pm_ops zynq_gpio_dev_pm_ops = {
  */
 static int zynq_gpio_probe(struct platform_device *pdev)
 {
-	int ret;
+	int ret, pin_num, bank_num, gpio_irq;
 	unsigned int irq_num;
 	struct zynq_gpio *gpio;
 	struct gpio_chip *chip;
 	struct resource *res;
-	int pin_num, bank_num, gpio_irq;
 
 	gpio = devm_kzalloc(&pdev->dev, sizeof(*gpio), GFP_KERNEL);
 	if (!gpio)
-- 
1.7.5.4

