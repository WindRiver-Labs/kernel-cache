From 43a0de21706662105fe59997b0ff372507acab9a Mon Sep 17 00:00:00 2001
From: Soren Brinkmann <soren.brinkmann@xilinx.com>
Date: Mon, 20 Jan 2014 10:40:08 -0800
Subject: [PATCH 172/509] I2C: zynq: Change data types of variables

https://github.com/analogdevicesinc/linux.git xcomm_zynq
commit 02386e4f2cf479598c9ad6d7ba096d1eb67dab82

Make some variable's data types match the data type they are compared
with in some if-statement.

Signed-off-by: Soren Brinkmann <soren.brinkmann@xilinx.com>
Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Zhong Hongbo <hongbo.zhong@windriver.com>
---
 drivers/i2c/busses/i2c-zynq.c |    8 ++++----
 1 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/i2c/busses/i2c-zynq.c b/drivers/i2c/busses/i2c-zynq.c
index 41a4ae3..8a69121 100644
--- a/drivers/i2c/busses/i2c-zynq.c
+++ b/drivers/i2c/busses/i2c-zynq.c
@@ -126,8 +126,8 @@ struct zynq_i2c {
 	unsigned char *p_send_buf;
 	unsigned char *p_recv_buf;
 	u8 suspended;
-	int send_count;
-	int recv_count;
+	unsigned int send_count;
+	unsigned int recv_count;
 	int irq;
 	int cur_timeout;
 	unsigned long input_clk;
@@ -469,9 +469,9 @@ static int zynq_i2c_master_xfer(struct i2c_adapter *adap, struct i2c_msg *msgs,
 				int num)
 {
 	struct zynq_i2c *id = adap->algo_data;
-	unsigned int count, retries;
+	unsigned int retries;
 	unsigned long timeout;
-	int ret;
+	int ret, count;
 
 	/* Waiting for bus-ready. If bus not ready, it returns after timeout */
 	timeout = jiffies + ZYNQ_I2C_TIMEOUT;
-- 
1.7.5.4

