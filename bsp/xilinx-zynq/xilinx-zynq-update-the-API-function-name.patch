From 10ab7159bc116787de9704517c22a8e7291f2ddc Mon Sep 17 00:00:00 2001
From: Yue Tao <Yue.Tao@windriver.com>
Date: Wed, 11 Feb 2015 14:19:24 +0800
Subject: [PATCH] xilinx-zynq: update the API function name

Afater applying a upstream commit (fd9fdb78):
[media] of: move graph helpers from drivers/media/v4l2-core to
drivers/of, the

v4l2_of_get_remote_port_parent

is renamed to

of_graph_get_remote_port_parent

the function
v4l2_of_get_next_endpoint

is renamed to

of_graph_get_next_endpoint

Signed-off-by: Yue Tao <Yue.Tao@windriver.com>
---
 drivers/media/platform/xilinx/xilinx-vipp.c |    8 ++++----
 1 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/media/platform/xilinx/xilinx-vipp.c b/drivers/media/platform/xilinx/xilinx-vipp.c
index 3f8585f..8e227d8 100644
--- a/drivers/media/platform/xilinx/xilinx-vipp.c
+++ b/drivers/media/platform/xilinx/xilinx-vipp.c
@@ -80,7 +80,7 @@ static int xvip_graph_build_one(struct xvip_composite_device *xdev,
 
 	while (1) {
 		/* Get the next endpoint and parse its link. */
-		next = v4l2_of_get_next_endpoint(entity->node, ep);
+		next = of_graph_get_next_endpoint(entity->node, ep);
 		if (next == NULL)
 			break;
 
@@ -201,7 +201,7 @@ static int xvip_graph_build_dma(struct xvip_composite_device *xdev)
 
 	while (1) {
 		/* Get the next endpoint and parse its link. */
-		next = v4l2_of_get_next_endpoint(node, ep);
+		next = of_graph_get_next_endpoint(node, ep);
 		if (next == NULL)
 			break;
 
@@ -354,7 +354,7 @@ static int xvip_graph_parse_one(struct xvip_composite_device *xdev,
 	dev_dbg(xdev->dev, "parsing node %s\n", node->full_name);
 
 	while (1) {
-		next = v4l2_of_get_next_endpoint(node, ep);
+		next = of_graph_get_next_endpoint(node, ep);
 		if (next == NULL)
 			break;
 
@@ -363,7 +363,7 @@ static int xvip_graph_parse_one(struct xvip_composite_device *xdev,
 
 		dev_dbg(xdev->dev, "handling endpoint %s\n", ep->full_name);
 
-		remote = v4l2_of_get_remote_port_parent(ep);
+		remote = of_graph_get_remote_port_parent(ep);
 		if (remote == NULL) {
 			ret = -EINVAL;
 			break;
-- 
1.7.5.4

