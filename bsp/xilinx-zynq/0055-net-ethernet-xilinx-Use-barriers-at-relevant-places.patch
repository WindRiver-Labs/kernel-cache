From 76aba9f8fb231e529a4af2bfbe4f437f2dbcb722 Mon Sep 17 00:00:00 2001
From: Kedareswara rao Appana <appana.durga.rao@xilinx.com>
Date: Thu, 10 Sep 2015 22:24:51 +0530
Subject: [PATCH 55/64] net: ethernet: xilinx: Use barriers at relevant places

This patch comes from:
  https://github.com/Xilinx/linux-xlnx.git

Signed-off-by: Kedareswara rao Appana <appanad@xilinx.com>
Signed-off-by: Michal Simek <michal.simek@xilinx.com>
(cherry picked from commit 1fdeac07e6713d749ccf75f8bab5e7cea4f9681c)
---
 drivers/net/ethernet/xilinx/xilinx_axienet_main.c |   13 +++++++++++++
 1 file changed, 13 insertions(+)

diff --git a/drivers/net/ethernet/xilinx/xilinx_axienet_main.c b/drivers/net/ethernet/xilinx/xilinx_axienet_main.c
index 09d6102..f4d20ce7 100644
--- a/drivers/net/ethernet/xilinx/xilinx_axienet_main.c
+++ b/drivers/net/ethernet/xilinx/xilinx_axienet_main.c
@@ -226,6 +226,11 @@ static int axienet_dma_bd_init(struct net_device *ndev)
 		if (!skb)
 			goto out;
 
+		/* Ensure that the skb is completely updated
+		 * prio to mapping the DMA
+		 */
+		wmb();
+
 		lp->rx_bd_v[i].sw_id_offset = (u32) skb;
 		lp->rx_bd_v[i].phys = dma_map_single(ndev->dev.parent,
 						     skb->data,
@@ -803,6 +808,11 @@ static int axienet_recv(struct net_device *ndev, int budget)
 			break;
 		}
 
+		/* Ensure that the skb is completely updated
+		 * prio to mapping the DMA
+		 */
+		wmb();
+
 		cur_p->phys = dma_map_single(ndev->dev.parent, new_skb->data,
 					     lp->max_frm_size,
 					     DMA_FROM_DEVICE);
@@ -812,6 +822,9 @@ static int axienet_recv(struct net_device *ndev, int budget)
 
 		++lp->rx_bd_ci;
 		lp->rx_bd_ci %= RX_BD_NUM;
+
+		/* Get relevat BD status value */
+		rmb();
 		cur_p = &lp->rx_bd_v[lp->rx_bd_ci];
 		numbdfree++;
 	}
-- 
1.7.10.4

