From caffbba8c0f77176e1f50564e1a37a4553b60e8a Mon Sep 17 00:00:00 2001
From: Michal Simek <michal.simek@xilinx.com>
Date: Fri, 24 Jan 2014 13:25:22 +0100
Subject: [PATCH 382/509] usb: gadget: zynq: Remove additional empty lines

https://github.com/analogdevicesinc/linux.git xcomm_zynq
commit cf0f9fa69ee9c2278c11dc7432a8e950f6b52151

No functional code change.

Signed-off-by: Michal Simek <michal.simek@xilinx.com>
Signed-off-by: Zhong Hongbo <hongbo.zhong@windriver.com>
---
 drivers/usb/gadget/zynq_udc.c |    5 -----
 1 files changed, 0 insertions(+), 5 deletions(-)

diff --git a/drivers/usb/gadget/zynq_udc.c b/drivers/usb/gadget/zynq_udc.c
index 851c643..15d69bc 100644
--- a/drivers/usb/gadget/zynq_udc.c
+++ b/drivers/usb/gadget/zynq_udc.c
@@ -48,7 +48,6 @@
 #define	DRIVER_AUTHOR	"Xilinx, Inc."
 #define	DRIVER_VERSION	"Apr 01, 2011"
 
-
 /* USB registers */
 #define USB_MAX_CTRL_PAYLOAD		64
 
@@ -185,7 +184,6 @@ struct usb_dr_device {
 #define  EPCTRL_TX_EP_TYPE_SHIFT              18
 #define  EPCTRL_RX_EP_TYPE_SHIFT              2
 
-
 /* Endpoint Queue Head data struct
  * Rem: all the variables of qh are LittleEndian Mode
  * and NEXT_POINTER_MASK should operate on a LittleEndian, Phy Addr
@@ -336,7 +334,6 @@ struct zynq_udc {
 #define DBG(fmt, args...)	do {} while (0)
 #endif
 
-
 #ifdef VERBOSE
 #define VDBG		DBG
 #else
@@ -366,7 +363,6 @@ struct zynq_udc {
 #define get_pipe_by_windex(windex)	((windex & USB_ENDPOINT_NUMBER_MASK) \
 					* 2 + ((windex & USB_DIR_IN) ? 1 : 0))
 
-
 static int zynq_udc_clk_notifier_cb(struct notifier_block *nb,
 		unsigned long event, void *data)
 {
@@ -417,7 +413,6 @@ static void zynq_udc_clk_release(struct platform_device *pdev)
 	clk_disable_unprepare(pdata->clk);
 }
 
-
 #define	DMA_ADDR_INVALID	(~(dma_addr_t)0)
 
 static const char driver_name[] = "zynq-udc";
-- 
1.7.5.4

