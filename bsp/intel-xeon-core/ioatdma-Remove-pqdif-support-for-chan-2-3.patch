From 5d45a2c0c49dfb772ea9a58fed40a97b87251590 Mon Sep 17 00:00:00 2001
From: Dave Jiang <dave.jiang@intel.com>
Date: Tue, 26 Mar 2013 13:57:26 -0700
Subject: [PATCH 67/70] ioatdma: Remove pqdif support for chan 2 & 3

This patch is provided by dave.jiang@intel.com via e-mail, and not
committed to mainline yet.

Chan 2 & 3 falsely advertises caps that are not available

Signed-off-by: Dave Jiang <dave.jiang@intel.com>
Signed-off-by: Yunguo Wei <yunguo.wei@windriver.com>
---
 drivers/dma/ioat/dma_v3.c |    7 +++++--
 1 files changed, 5 insertions(+), 2 deletions(-)

diff --git a/drivers/dma/ioat/dma_v3.c b/drivers/dma/ioat/dma_v3.c
index be144d1..f491214 100644
--- a/drivers/dma/ioat/dma_v3.c
+++ b/drivers/dma/ioat/dma_v3.c
@@ -2852,8 +2852,11 @@ int ioat3_dma_probe(struct ioatdma_device *device, int dca)
 
 	device->cap = readl(device->reg_base + IOAT_DMA_CAP_OFFSET);
 
-	if (is_bwd_noraid(pdev))
-		device->cap &= ~(IOAT_CAP_XOR | IOAT_CAP_PQ | IOAT_CAP_RAID16SS);
+	if (is_bwd_noraid(pdev)) {
+		device->cap &= ~(IOAT_CAP_XOR | IOAT_CAP_PQ |
+				 IOAT_CAP_RAID16SS |
+				 IOAT_CAP_RAID8_DIF | IOAT_CAP_RAID16_DIF);
+	}
 
 	/* dca is incompatible with raid operations */
 	if (dca_en && (device->cap & (IOAT_CAP_XOR|IOAT_CAP_PQ)))
-- 
1.7.5.4

