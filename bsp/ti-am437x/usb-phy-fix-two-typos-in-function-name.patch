From 6fbb27b0a48b4411ef5aaf120238aa780932330a Mon Sep 17 00:00:00 2001
From: Kevin Hao <kexin.hao@windriver.com>
Date: Sat, 12 Jul 2014 21:53:35 +0800
Subject: [PATCH 001/285] usb: phy: fix two typos in function name

The patch (usb: phy: add support for am335x") has changed the functions
usb_nop_xceiv_register/unregister to usb_gen_xceiv_register/unregister
for no reason. Restore them back since there are many codes referring
to them.

Signed-off-by: Kevin Hao <kexin.hao@windriver.com>
---
 drivers/usb/phy/phy-generic.c |    8 ++++----
 1 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/usb/phy/phy-generic.c b/drivers/usb/phy/phy-generic.c
index a3051f5..0c3b241 100644
--- a/drivers/usb/phy/phy-generic.c
+++ b/drivers/usb/phy/phy-generic.c
@@ -43,7 +43,7 @@
 
 static struct platform_device *pd;
 
-void usb_gen_xceiv_register(void)
+void usb_nop_xceiv_register(void)
 {
 	if (pd)
 		return;
@@ -53,14 +53,14 @@ void usb_gen_xceiv_register(void)
 		return;
 	}
 }
-EXPORT_SYMBOL(usb_gen_xceiv_register);
+EXPORT_SYMBOL(usb_nop_xceiv_register);
 
-void usb_gen_xceiv_unregister(void)
+void usb_nop_xceiv_unregister(void)
 {
 	platform_device_unregister(pd);
 	pd = NULL;
 }
-EXPORT_SYMBOL(usb_gen_xceiv_unregister);
+EXPORT_SYMBOL(usb_nop_xceiv_unregister);
 
 static int nop_set_suspend(struct usb_phy *x, int suspend)
 {
-- 
1.7.5.4

