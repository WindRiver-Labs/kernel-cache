From 1cfa6fd5744ad97544217d4b0037ff5d091839f7 Mon Sep 17 00:00:00 2001
From: Roger Quadros <rogerq@ti.com>
Date: Mon, 23 Sep 2013 16:22:30 +0300
Subject: [PATCH 037/285] Revert "usb: phy: omap-usb2: Manage phy clock and
 not otg controller clock"

This reverts commit 953145460775ec53c603a00a8bf63648d619e80f.

The clock is in fact a PHY clock. The clock name is misleading.

Signed-off-by: Roger Quadros <rogerq@ti.com>
[Kevin: The original patch taken from TI
ti-sdk-am437x-evm-07.01.00.00-Linux-x86-Install.bin]
Signed-off-by: Kevin Hao <kexin.hao@windriver.com>
---
 drivers/phy/phy-omap-usb2.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/phy/phy-omap-usb2.c b/drivers/phy/phy-omap-usb2.c
index 20089e0..53ad974 100644
--- a/drivers/phy/phy-omap-usb2.c
+++ b/drivers/phy/phy-omap-usb2.c
@@ -221,9 +221,9 @@ static int omap_usb2_probe(struct platform_device *pdev)
 	}
 	clk_prepare(phy->wkupclk);
 
-	phy->optclk = devm_clk_get(phy->dev, "usb_phy_refclk960m");
+	phy->optclk = devm_clk_get(phy->dev, "usb_otg_ss_refclk960m");
 	if (IS_ERR(phy->optclk))
-		dev_dbg(&pdev->dev, "unable to get refclk960m\n");
+		dev_vdbg(&pdev->dev, "unable to get refclk960m\n");
 	else
 		clk_prepare(phy->optclk);
 
-- 
1.7.5.4

