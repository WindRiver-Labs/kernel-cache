From af9b669590fb3ac10a1dcf565ecab82d4122be4f Mon Sep 17 00:00:00 2001
From: Lakshan Dias <ldias@netlogicmicro.com>
Date: Tue, 11 Oct 2011 13:47:14 -0700
Subject: [PATCH 280/565] reinitialize "fn"

reinitialize "fn" within while loop, back to 0 for each row

Based on Broadcom SDK 2.3.

Signed-off-by: Lakshan Dias <ldias@netlogicmicro.com>
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 arch/mips/pci/pci-xlp.c | 1 +
 1 file changed, 1 insertion(+)

diff --git a/arch/mips/pci/pci-xlp.c b/arch/mips/pci/pci-xlp.c
index 5154f01..3f5588a 100644
--- a/arch/mips/pci/pci-xlp.c
+++ b/arch/mips/pci/pci-xlp.c
@@ -689,6 +689,7 @@ int xlp_msi_status_clear(struct pci_dev *dev, int bit)
 	u32 msi_en;
 
 	while (row < 4) {
+		fn = 0;
 		while (fn < 4) {
 			if ((dev->bus->number >= xlp_irq_map[row][fn][1]) &&
 				(dev->bus->number <= xlp_irq_map[row][fn][2])) {
-- 
1.8.4.93.g57e4c17

