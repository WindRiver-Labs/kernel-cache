From 0d68bdc3d173684b110193c729ad230c36c731e8 Mon Sep 17 00:00:00 2001
From: "P. Sadik" <psadik@broadcom.com>
Date: Tue, 18 Dec 2012 00:17:16 +0530
Subject: [PATCH 439/565] defconfig: update xlp2xx Evaluation Platform
 defconfig.

   Also, removed Remove duplicate entry for UDLFB in Kconfig and
   fixed compilation error due to watchdog timer driver.

Based on Broadcom SDK 2.3.

Signed-off-by: P. Sadik <psadik@broadcom.com>
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 drivers/watchdog/nlm_common_wdt.c | 12 ++++++------
 1 file changed, 6 insertions(+), 6 deletions(-)

diff --git a/drivers/watchdog/nlm_common_wdt.c b/drivers/watchdog/nlm_common_wdt.c
index 22d708d..dc91409 100644
--- a/drivers/watchdog/nlm_common_wdt.c
+++ b/drivers/watchdog/nlm_common_wdt.c
@@ -99,7 +99,7 @@ static irqreturn_t xlp_wdt_handler(int irq, void *p)
         val = nlh_pic_r64r(node, XLP_PIC_STATUS);
 
 	wd_id = (int)(val & 0x3);
-	
+
 	/* Writing 1 to PIC status [0:1] cleares inteppupt for watchdog [0:1] */
 	val = (val | wd_id);
 	nlh_pic_w64r(node, XLP_PIC_STATUS, val);
@@ -120,7 +120,7 @@ xlp_wdt_enable(int wd_id, int heartbeat, u64 cpu_mask)
 	u64 val, max_val;
 	int i;
 	int ret;
-	
+
 	if(wd_id == 1)
 	{
 		cpu_mask_1 = cpu_mask;
@@ -157,7 +157,7 @@ xlp_wdt_enable(int wd_id, int heartbeat, u64 cpu_mask)
 		nlh_pic_w64r(node, XLP_PIC_WD_THREN1(wd_id), cpu_mask);
 
 
-	/* Keep the thread active to write on WATCHDOG[01]_BEATCMD register */	
+	/* Keep the thread active to write on WATCHDOG[01]_BEATCMD register */
 	i = node *4;
 	while(cpu_mask)
 	{
@@ -178,7 +178,7 @@ xlp_wdt_disable(int wd_id)
 {
 	u64 val = 0, tmp = 0;
 	u8 node;
-	
+
 	node = hard_smp_processor_id() / NLM_MAX_CPU_PER_NODE;
 	tmp = 1 << wd_id;
 	val = val & ~tmp;
@@ -237,7 +237,7 @@ xlp_wdt_ioctl(struct file *file, unsigned int cmd,
 		break;
 
 	case WDIOC_SETTIMEOUT:
-		
+
 		break;
 	case WDIOC_GETTIMEOUT:
 		if(wdt->wd_id == 1)
@@ -333,7 +333,7 @@ static void __exit xlp_wdt_exit(void)
 module_init(xlp_wdt_init);
 module_exit(xlp_wdt_exit);
 
-MODULE_AUTHOR("Alok Agrawat">);
+MODULE_AUTHOR("Alok Agrawat");
 MODULE_DESCRIPTION("Broadcom Processors Watchdog");
 
 MODULE_LICENSE("GPL");
-- 
1.8.4.93.g57e4c17

