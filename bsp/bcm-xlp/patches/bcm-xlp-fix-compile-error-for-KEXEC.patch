From 9430292663a18644d32e6e5da7524ad111a0f266 Mon Sep 17 00:00:00 2001
From: Yanjiang Jin <yanjiang.jin@windriver.com>
Date: Wed, 16 Oct 2013 09:21:35 +0800
Subject: [PATCH 1/2] bcm-xlp: fix compile error for KEXEC

Fix compile error in arch/mips/include/asm/smp.h for KEXEC, else we
would get the compile error as below:

arch/mips/kernel/smp.c: error:'SMP_DUMP' undeclared(first use in this function)

Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 arch/mips/include/asm/smp.h | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/arch/mips/include/asm/smp.h b/arch/mips/include/asm/smp.h
index d8fd32f..79961a1 100644
--- a/arch/mips/include/asm/smp.h
+++ b/arch/mips/include/asm/smp.h
@@ -51,6 +51,8 @@ extern int __cpu_logical_map[NR_CPUS];
 #define SMP_CALL_FUNCTION	0x2
 /* Octeon - Tell another core to flush its icache */
 #define SMP_ICACHE_FLUSH	0x4
+/* Used by kexec crashdump to save all cpu's state */
+#define SMP_DUMP		0x8
 #ifdef CONFIG_NLM_XLP
 #define SMP_CALL_KGDB_HOOK	0x8
 #endif
@@ -105,4 +107,8 @@ static inline void arch_send_call_function_ipi_mask(const struct cpumask *mask)
 	mp_ops->send_ipi_mask(mask, SMP_CALL_FUNCTION);
 }
 
+#if defined(CONFIG_KEXEC)
+extern void (*dump_ipi_function_ptr)(void *);
+void dump_send_ipi(void (*dump_ipi_callback)(void *));
+#endif
 #endif /* __ASM_SMP_H */
-- 
1.8.4.93.g57e4c17

