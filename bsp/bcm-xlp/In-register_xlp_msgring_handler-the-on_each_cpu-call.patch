From b1f38560e3d36252a70e2797a4facf8bdf37f58f Mon Sep 17 00:00:00 2001
From: Om Narasimhan <onarasimhan@netlogicmicro.com>
Date: Tue, 6 Sep 2011 18:07:20 -0700
Subject: [PATCH 324/761] In register_xlp_msgring_handler(), the on_each_cpu()
 call takes care of calling init_msg_bkp_timer() on
 all cpus including the current one. So this need
 not be called separately.

Based on Broadcom SDK 2.3.

Signed-off-by: Om Narasimhan <onarasimhan@netlogicmicro.com>
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 arch/mips/netlogic/xlp/on_chip.c |    2 +-
 1 file changed, 1 insertion(+), 1 deletion(-)

diff --git a/arch/mips/netlogic/xlp/on_chip.c b/arch/mips/netlogic/xlp/on_chip.c
index 0cfd08b..015ae7a 100644
--- a/arch/mips/netlogic/xlp/on_chip.c
+++ b/arch/mips/netlogic/xlp/on_chip.c
@@ -376,7 +376,7 @@ int register_xlp_msgring_handler(int major,
 	if(msg_handler_timer_enabled == 0) {
 		msg_handler_timer_enabled = 1;
 		spin_unlock_irqrestore(&msgrng_lock, flags);
-		init_msg_bkp_timer(0);
+		// init_msg_bkp_timer(0);	Not required, taken care by on_each_cpu()
 		on_each_cpu(init_msg_bkp_timer, 0, 1);
 		spin_lock_irqsave(&msgrng_lock, flags);
 	}
-- 
1.7.10.4

