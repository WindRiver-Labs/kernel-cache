From 4306860d3be5774620167c22f9964def28a1bbd6 Mon Sep 17 00:00:00 2001
From: henry shao <hshao@netlogicmicro.com>
Date: Wed, 30 Mar 2011 17:16:38 -0700
Subject: [PATCH 270/762] turn off NAND_DEBUG flag

Based on Broadcom SDK 2.3.

Signed-off-by: henry shao <hshao@netlogicmicro.com>
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 drivers/mtd/nand/xlp_plat_nand.c |    5 +++--
 1 files changed, 3 insertions(+), 2 deletions(-)

diff --git a/drivers/mtd/nand/xlp_plat_nand.c b/drivers/mtd/nand/xlp_plat_nand.c
index 7f58095..f99dd95 100644
--- a/drivers/mtd/nand/xlp_plat_nand.c
+++ b/drivers/mtd/nand/xlp_plat_nand.c
@@ -40,7 +40,7 @@ THE POSSIBILITY OF SUCH DAMAGE.
 
 int debug = 0;
 
-#define NAND_DEBUG		1
+#define NAND_DEBUG		0	
 #define BUF_SIZE 		(16*1024)
 #define dbp_print		if(debug) printk
 #define NAND_DEV_CS		1
@@ -728,8 +728,9 @@ static int __devinit xlp_plat_nand_probe(struct platform_device *pdev)
                 goto out;
         }
 
+#ifdef NAND_DEBUG
 	print_onfi_params(&data->chip.onfi_params);
-
+#endif
 
 	/* Register the partitions */
 	add_mtd_partitions(&data->mtd, xlp_nand_partition_info, ARRAY_SIZE(xlp_nand_partition_info));
-- 
1.7.0.4

