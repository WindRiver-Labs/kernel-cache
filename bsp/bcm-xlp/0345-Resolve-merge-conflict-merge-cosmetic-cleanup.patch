From fbcc1acf5addbd1ffcd0110ed772f0ca7e37eba9 Mon Sep 17 00:00:00 2001
From: Zi Shen Lim <zlim@netlogicmicro.com>
Date: Thu, 29 Sep 2011 14:15:53 -0700
Subject: [PATCH 345/762] Resolve merge conflict: merge, cosmetic cleanup

mapped kernel, rixi

Based on Broadcom SDK 2.3.

Signed-off-by: Zi Shen Lim <zlim@netlogicmicro.com>
Signed-off-by: Yanjiang Jin <yanjiang.jin@windriver.com>
---
 arch/mips/include/asm/pgtable-64.h |    6 +++---
 1 files changed, 3 insertions(+), 3 deletions(-)

diff --git a/arch/mips/include/asm/pgtable-64.h b/arch/mips/include/asm/pgtable-64.h
index 60d35c6..d78afba 100644
--- a/arch/mips/include/asm/pgtable-64.h
+++ b/arch/mips/include/asm/pgtable-64.h
@@ -195,11 +195,11 @@ static inline void pud_clear(pud_t *pudp)
 #define pfn_pte(pfn, prot)	__pte(((pfn) << (PAGE_SHIFT + 2)) | pgprot_val(prot))
 #else
 #ifdef CONFIG_NLM_XLP
-#define pte_pfn(x)     ((unsigned long)(((x).pte & ~((1ULL<<_PAGE_NO_READ_SHIFT)|(1ULL<<_PAGE_NO_EXEC_SHIFT))) >> _PFN_SHIFT))
+#define pte_pfn(x)		((unsigned long)(((x).pte & ~((1ULL << _PAGE_NO_READ_SHIFT) | (1ULL << _PAGE_NO_EXEC_SHIFT))) >> _PFN_SHIFT))
 #else
-#define pte_pfn(x)     ((unsigned long)((x).pte >> _PFN_SHIFT))
+#define pte_pfn(x)		((unsigned long)((x).pte >> _PFN_SHIFT))
 #endif
-#define pfn_pte(pfn, prot) __pte(((pfn) << _PFN_SHIFT) | pgprot_val(prot))
+#define pfn_pte(pfn, prot)	__pte(((pfn) << _PFN_SHIFT) | pgprot_val(prot))
 #endif
 
 #define __pgd_offset(address)	pgd_index(address)
-- 
1.7.0.4

