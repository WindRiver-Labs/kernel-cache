From a39d8524cff5d8dd648ed12bb0dacbb14bf9ec59 Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Tue, 6 Sep 2011 11:47:07 -0700
Subject: [PATCH 136/236] MIPS: Octeon: Don't print 'Deleting ...' messages on boot.

Source: Cavium SDK 2.2-414

Only print them at debug level, which usually aren't seen.

Signed-off-by: David Daney <david.daney@cavium.com>
Integrated-by: Yang Shi <yang.shi@windriver.com>
---
 arch/mips/cavium-octeon/octeon-platform.c |   10 +++++-----
 1 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/arch/mips/cavium-octeon/octeon-platform.c b/arch/mips/cavium-octeon/octeon-platform.c
index c77f327..c71e0de 100644
--- a/arch/mips/cavium-octeon/octeon-platform.c
+++ b/arch/mips/cavium-octeon/octeon-platform.c
@@ -312,7 +312,7 @@ static void __init octeon_fdt_pip_port(int iface, int i, int p, int max, u64 *pm
 	if (eth < 0)
 		return;
 	if (p > max) {
-		pr_notice("Deleting port %x:%x\n", i, p);
+		pr_debug("Deleting port %x:%x\n", i, p);
 		octeon_fdt_rm_ethernet(eth);
 		return;
 	}
@@ -388,7 +388,7 @@ int __init octeon_prune_device_tree(void)
 			if (mgmt < 0)
 				continue;
 			if (i >= max_port) {
-				pr_notice("Deleting mix%d\n", i);
+				pr_debug("Deleting mix%d\n", i);
 				octeon_fdt_rm_ethernet(mgmt);
 				fdt_nop_property(initial_boot_params, aliases,
 						 name_buffer);
@@ -428,7 +428,7 @@ int __init octeon_prune_device_tree(void)
 			if (i2c < 0)
 				continue;
 			if (i >= max_port) {
-				pr_notice("Deleting twsi%d\n", i);
+				pr_debug("Deleting twsi%d\n", i);
 				fdt_nop_node(initial_boot_params, i2c);
 				fdt_nop_property(initial_boot_params, aliases,
 						 name_buffer);
@@ -450,7 +450,7 @@ int __init octeon_prune_device_tree(void)
 			if (i2c < 0)
 				continue;
 			if (i >= max_port) {
-				pr_notice("Deleting smi%d\n", i);
+				pr_debug("Deleting smi%d\n", i);
 				fdt_nop_node(initial_boot_params, i2c);
 				fdt_nop_property(initial_boot_params, aliases,
 						 name_buffer);
@@ -499,7 +499,7 @@ int __init octeon_prune_device_tree(void)
 			uart = fdt_path_offset(initial_boot_params, alias_prop);
 			if (uart_mask & (1 << i))
 				continue;
-			pr_notice("Deleting uart%d\n", i);
+			pr_debug("Deleting uart%d\n", i);
 			fdt_nop_node(initial_boot_params, uart);
 			fdt_nop_property(initial_boot_params, aliases,
 					 name_buffer);
-- 
1.7.0

