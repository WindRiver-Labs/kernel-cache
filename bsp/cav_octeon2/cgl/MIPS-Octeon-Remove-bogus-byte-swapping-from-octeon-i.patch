From 5d9cbfeed4d60e8f4b2fc039ace47232a33d39a7 Mon Sep 17 00:00:00 2001
From: David Daney <david.daney@cavium.com>
Date: Wed, 4 Jan 2012 18:21:43 -0800
Subject: [PATCH 183/238] MIPS: Octeon: Remove bogus byte swapping from octeon-irq.c

Source: Cavium SDK 2.3-427

The values supplied by the device tree core are already native-endian.

Signed-off-by: David Daney <david.daney@cavium.com>
Integrated-by: Jiang Bin <bin.jiang@windriver.com>
---
 arch/mips/cavium-octeon/octeon-irq.c |    8 ++++----
 1 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/arch/mips/cavium-octeon/octeon-irq.c b/arch/mips/cavium-octeon/octeon-irq.c
index faa33a4..58d4338 100644
--- a/arch/mips/cavium-octeon/octeon-irq.c
+++ b/arch/mips/cavium-octeon/octeon-irq.c
@@ -55,8 +55,8 @@ static unsigned int octeon_irq_gpio_mapping(struct device_node *controller,
 	unsigned int irq = 0;
 	unsigned int type;
 	unsigned int ciu = 0, bit = 0;
-	unsigned int pin = be32_to_cpup(intspec);
-	unsigned int trigger = be32_to_cpup(intspec + 1);
+	unsigned int pin = intspec[0];
+	unsigned int trigger = intspec[1];
 	bool set_edge_handler = false;
 
 	if (pin >= 16)
@@ -125,8 +125,8 @@ unsigned int irq_create_of_mapping(struct device_node *controller,
 	if (of_device_is_compatible(controller, "cavium,octeon-3860-gpio"))
 		return octeon_irq_gpio_mapping(controller, intspec, intsize);
 
-	ciu = be32_to_cpup(intspec);
-	bit = be32_to_cpup(intspec + 1);
+	ciu = intspec[0];
+	bit = intspec[1];
 
 	if (ciu < 8 && bit < 64)
 		irq = octeon_irq_ciu_to_irq[ciu][bit];
-- 
1.7.0

