From fa1767bada72bab3426010a59fbf3f6873037383 Mon Sep 17 00:00:00 2001
From: Phil Staub <Phil.Staub@windriver.com>
Date: Thu, 29 Sep 2011 11:52:56 -0700
Subject: [PATCH 095/238] MIPS: Fix incorrect build_restore_work_registers sequence

In build_r4000_tlbchange_handler_tail, build_restore_work_registers
should precede the UASM_i_LW() macro. This was incorrectly ported from
Cavium's SDK 2.1 patches.

Signed-off-by: Phil Staub <Phil.Staub@windriver.com>
---
 arch/mips/mm/tlbex.c |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/arch/mips/mm/tlbex.c b/arch/mips/mm/tlbex.c
index b8b78df..327c82d 100644
--- a/arch/mips/mm/tlbex.c
+++ b/arch/mips/mm/tlbex.c
@@ -1837,12 +1837,12 @@ build_r4000_tlbchange_handler_tail(u32 **p, struct uasm_label **l,
 	build_update_entries(p, tmp, ptr);
 	build_tlb_write_entry(p, l, r, tlb_indexed);
 	uasm_l_leave(l, *p);
+	build_restore_work_registers(p);
 #ifndef CONFIG_WRHV
 #ifdef CONFIG_FAST_ACCESS_TO_THREAD_POINTER
 	UASM_i_LW(p, K0, FAST_ACCESS_THREAD_OFFSET, 0);  /* K0 = thread ptr */
 #endif
 #endif
-	build_restore_work_registers(p);
 	uasm_i_eret(p); /* return from trap */
 
 #ifdef CONFIG_64BIT
-- 
1.7.0

