From b4d0e2866dca1e633543968d5514dd0ea159fa6f Mon Sep 17 00:00:00 2001
From: Marian Chereji <marian.chereji@freescale.com>
Date: Tue, 4 Dec 2012 15:47:10 +0200
Subject: [PATCH 430/518] dpa_classifier: Change type of override_fqid
 attribute from int to bool

override_fqid indicates whether the FMan Coarse Classification engine
overrides the frame queue selected by the KeyGen or not. This is a
"yes" or "no" value and should be boolean.

Signed-off-by: Marian Chereji <marian.chereji@freescale.com>
[Grabbed from the branch, LINUX_IR5.2.0, of
https://git.freescale.com/git-private/cgit.cgi/ppc/alu-b4860/linux.git.]
Signed-off-by: Tiejun Chen <tiejun.chen@windriver.com>
---
 include/linux/fsl_dpa_classifier.h |    8 +++-----
 1 files changed, 3 insertions(+), 5 deletions(-)

diff --git a/include/linux/fsl_dpa_classifier.h b/include/linux/fsl_dpa_classifier.h
index b3ad095..f6d35b7 100644
--- a/include/linux/fsl_dpa_classifier.h
+++ b/include/linux/fsl_dpa_classifier.h
@@ -80,9 +80,7 @@ enum dpa_cls_tbl_action_type {
 	/* Go to another table and re-classify the packet */
 	DPA_CLS_TBL_ACTION_NEXT_TABLE,
 
-	/*
-	 * Send frames to a multicast group
-	 */
+	/* Send frames to a multicast group */
 	DPA_CLS_TBL_ACTION_MCAST
 
 };
@@ -232,9 +230,9 @@ struct dpa_cls_tbl_enq_action_desc {
 
 	/*
 	 * Override the frame queue Id from KeyGen and use the one
-	 * specified in this enqueue action descriptor if nonzero
+	 * specified in this enqueue action descriptor if set to true
 	 */
-	int					override_fqid;
+	bool					override_fqid;
 
 	/*
 	 * Id of the frame queue where to send the frames in case of
-- 
1.7.5.4

