From e93d601d02c9303983ed6c4657358436fe7b53d6 Mon Sep 17 00:00:00 2001
From: Marian Chereji <marian.chereji@freescale.com>
Date: Thu, 9 Aug 2012 16:12:21 +0000
Subject: [PATCH 278/518] dpa_classifier: Cosmetics

Renamed two attributes in the dpa_cls_hm_update_params structure.

Signed-off-by: Marian Chereji <marian.chereji@freescale.com>
[Grabbed from the branch, LINUX_IR5.2.0, of
https://git.freescale.com/git-private/cgit.cgi/ppc/alu-b4860/linux.git.]
Signed-off-by: Tiejun Chen <tiejun.chen@windriver.com>
---
 drivers/staging/fsl_dpa_offload/dpa_classifier.c |    4 ++--
 include/linux/fsl_dpa_classifier.h               |    4 ++--
 2 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/drivers/staging/fsl_dpa_offload/dpa_classifier.c b/drivers/staging/fsl_dpa_offload/dpa_classifier.c
index e8389fa..ac802fc 100644
--- a/drivers/staging/fsl_dpa_offload/dpa_classifier.c
+++ b/drivers/staging/fsl_dpa_offload/dpa_classifier.c
@@ -3771,7 +3771,7 @@ static int init_update_hm(struct dpa_cls_hm *pupdate_hm)
 
 			memcpy(hm_node->params.u.hdr.customParams.u.
 				ipHdrReplace.hdr,
-				&pupdate_hm->update_params.replace.ipv6_hdr,
+				&pupdate_hm->update_params.replace.new_ipv6_hdr,
 				sizeof(struct ipv6_header));
 		}
 
@@ -3785,7 +3785,7 @@ static int init_update_hm(struct dpa_cls_hm *pupdate_hm)
 				hdrSize = sizeof(struct iphdr);
 			memcpy(hm_node->params.u.hdr.customParams.u.
 				ipHdrReplace.hdr,
-				&pupdate_hm->update_params.replace.ipv4_hdr,
+				&pupdate_hm->update_params.replace.new_ipv4_hdr,
 				sizeof(struct iphdr));
 		}
 	}
diff --git a/include/linux/fsl_dpa_classifier.h b/include/linux/fsl_dpa_classifier.h
index c172d80..439aeb6 100644
--- a/include/linux/fsl_dpa_classifier.h
+++ b/include/linux/fsl_dpa_classifier.h
@@ -1010,13 +1010,13 @@ struct dpa_cls_hm_update_params {
 		 * IPv4 header data. This header is used IPv6 to IPv4 header
 		 * replace.
 		 */
-		struct iphdr				ipv4_hdr;
+		struct iphdr				new_ipv4_hdr;
 
 		/*
 		 * IPv6 header data. This header is used for IPv4 to IPv6
 		 * header replace.
 		 */
-		struct ipv6_header			ipv6_hdr;
+		struct ipv6_header			new_ipv6_hdr;
 	} replace;
 
 	union {
-- 
1.7.5.4

