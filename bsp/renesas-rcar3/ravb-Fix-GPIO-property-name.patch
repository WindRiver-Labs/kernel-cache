From fded58f1690440f7a8135e3bf997ee9a73d9e5d9 Mon Sep 17 00:00:00 2001
From: Kazuya Mizuguchi <kazuya.mizuguchi.ks@renesas.com>
Date: Thu, 15 Dec 2016 12:47:45 +0900
Subject: [PATCH 1423/2066] ravb: Fix GPIO property name

GPIO properties should be named "[<name>-]gpios" according to
Documentation/devicetree/bindings/gpio/gpio.txt.
This patch corrects it.

Signed-off-by: Kazuya Mizuguchi <kazuya.mizuguchi.ks@renesas.com>
[zou:Original patch taken from
git://git.kernel.org/pub/scm/linux/kernel/git/horms/renesas-bsp.git
 v4.9/rcar-3.5.1]
Signed-off-by: zou cao <cao.zou@windriver.com>
---
 drivers/net/ethernet/renesas/ravb_main.c |    4 ++--
 1 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/net/ethernet/renesas/ravb_main.c b/drivers/net/ethernet/renesas/ravb_main.c
index f27f3ac..bd8f4ba 100644
--- a/drivers/net/ethernet/renesas/ravb_main.c
+++ b/drivers/net/ethernet/renesas/ravb_main.c
@@ -1046,10 +1046,10 @@ static int ravb_phy_init(struct net_device *ndev)
 	}
 
 	if (priv->chip_id == RCAR_GEN3) {
-		gpio = of_get_named_gpio_flags(np, "phy-int-gpio", 0, &flags);
+		gpio = of_get_named_gpio_flags(np, "phy-gpios", 0, &flags);
 		if (gpio_is_valid(gpio)) {
 			err = devm_gpio_request_one(dev, gpio, GPIOF_DIR_IN,
-						    "phy-int-gpio");
+						    "phy-gpios");
 			if (err == 0 && (phydev->irq == gpio_to_irq(gpio))) {
 				if (flags & OF_GPIO_ACTIVE_LOW)
 					irq_set_irq_type(phydev->irq,
-- 
1.7.5.4

