From 4b7d94592e0642201bdb967a18392c57e4160d9c Mon Sep 17 00:00:00 2001
From: David Daney <ddaney@caviumnetworks.com>
Date: Tue, 1 Feb 2011 16:17:32 -0800
Subject: [PATCH 67/97] netdev: octeon-ethernet: Remove #if 0 code

Source: Cavium SDK 2.1.0-407

Signed-off-by: David Daney <ddaney@caviumnetworks.com>
Integrated-by: Phil Staub <Phil.Staub@windriver.com>
---
 drivers/net/octeon/ethernet-util.h |   37 ------------------------------------
 1 files changed, 0 insertions(+), 37 deletions(-)

diff --git a/drivers/net/octeon/ethernet-util.h b/drivers/net/octeon/ethernet-util.h
index 113c1ce..f489611 100644
--- a/drivers/net/octeon/ethernet-util.h
+++ b/drivers/net/octeon/ethernet-util.h
@@ -42,40 +42,3 @@ static inline void *cvm_oct_get_buffer_ptr(union cvmx_buf_ptr packet_ptr)
 	return cvmx_phys_to_ptr(((packet_ptr.s.addr >> 7) - packet_ptr.s.back)
 				<< 7);
 }
-#if 0
-/**
- * INTERFACE - convert IPD port to locgical interface
- * @ipd_port: Port to check
- *
- * Returns Logical interface
- */
-static inline int INTERFACE(int ipd_port)
-{
-	int interface;
-
-	/* Non existant interface for POW0 */
-	if (ipd_port == CVMX_PIP_NUM_INPUT_PORTS)
-		return cvmx_helper_get_interface_num(ipd_port-1) + 1;
-
-	interface = cvmx_helper_get_interface_num(ipd_port);
-
-	if (interface == -1)
-		panic("Illegal ipd_port %d passed to INTERFACE\n", ipd_port);
-
-	return interface;
-}
-
-/**
- * INDEX - convert IPD/PKO port number to the port's interface index
- * @ipd_port: Port to check
- *
- * Returns Index into interface port list
- */
-static inline int INDEX(int ipd_port)
-{
-	if (ipd_port == CVMX_PIP_NUM_INPUT_PORTS)
-		return 0;
-
-	return cvmx_helper_get_interface_index_num(ipd_port);
-}
-#endif
-- 
1.7.0

