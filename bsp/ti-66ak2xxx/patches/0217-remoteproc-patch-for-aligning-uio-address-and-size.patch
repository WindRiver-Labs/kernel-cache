From af4e708c58e68e0b35ac4cb41b200e7c2eef370e Mon Sep 17 00:00:00 2001
From: Ivan Pang <i-pang@ti.com>
Date: Thu, 14 Aug 2014 07:38:41 -0400
Subject: [PATCH 217/257] remoteproc: patch for aligning uio address and size

- Uio driver now restricts the mmap to aligned addresses
- This patch is to pass aligned addresses to uio driver.

Signed-off-by: Ivan Pang <i-pang@ti.com>
Signed-off-by: Sam Nelson <sam.nelson@ti.com>
(cherry picked from commit d292791cfea5073c2194c050acbe11feeb294a20)
Signed-off-by: Zumeng Chen <zumeng.chen@windriver.com>
---
 drivers/remoteproc/remoteproc_user.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/drivers/remoteproc/remoteproc_user.c b/drivers/remoteproc/remoteproc_user.c
index bd8ab46..78efb3c 100644
--- a/drivers/remoteproc/remoteproc_user.c
+++ b/drivers/remoteproc/remoteproc_user.c
@@ -539,8 +539,8 @@ static int uproc_driver_probe(struct platform_device *pdev)
 		if (!r)
 			break;
 		uio->mem[i].memtype = UIO_MEM_PHYS;
-		uio->mem[i].addr    = r->start;
-		uio->mem[i].size    = resource_size(r);
+		uio->mem[i].addr    = r->start & PAGE_MASK;
+		uio->mem[i].size    = PAGE_ALIGN(resource_size(r));
 		uio->mem[i].name    = r->name;
 	}
 
-- 
2.7.4

