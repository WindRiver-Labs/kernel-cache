From 8f584c73172de540826eb4dffdb96f6b8a91155a Mon Sep 17 00:00:00 2001
From: Weiwei Wang <weiwei.wang@windriver.com>
Date: Sun, 12 Feb 2012 15:23:31 +0800
Subject: [PATCH 202/479] acpi/osl: revert osl code which is modified by cgl code

revert the code for future patch applying

Signed-off-by: Weiwei Wang <weiwei.wang@windriver.com>
---
 drivers/acpi/osl.c |    4 ----
 1 files changed, 0 insertions(+), 4 deletions(-)

diff --git a/drivers/acpi/osl.c b/drivers/acpi/osl.c
index 21a490f..7594f65 100644
--- a/drivers/acpi/osl.c
+++ b/drivers/acpi/osl.c
@@ -523,8 +523,6 @@ acpi_os_read_memory(acpi_physical_address phys_addr, u32 * value, u32 width)
 	void __iomem *virt_addr;
 
 	virt_addr = ioremap(phys_addr, width);
-	if (!virt_addr)
-		return AE_NO_MEMORY;
 	if (!value)
 		value = &dummy;
 
@@ -553,8 +551,6 @@ acpi_os_write_memory(acpi_physical_address phys_addr, u32 value, u32 width)
 	void __iomem *virt_addr;
 
 	virt_addr = ioremap(phys_addr, width);
-	if (!virt_addr)
-		return AE_NO_MEMORY;
 
 	switch (width) {
 	case 8:
-- 
1.7.0

