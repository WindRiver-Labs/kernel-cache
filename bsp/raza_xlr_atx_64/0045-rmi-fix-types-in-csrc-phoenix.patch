From 2f6dd09d9e781dab9e4a711b65477549cf2824e4 Mon Sep 17 00:00:00 2001
From: Paul Gortmaker <paul.gortmaker@windriver.com>
Date: Sun, 21 Jun 2009 18:53:53 -0400
Subject: [PATCH] rmi: fix types in csrc-phoenix

An incorrect type was leading to a compile warning, and
another function didn't need world exposure, so make that
static.

Signed-off-by: Paul Gortmaker <paul.gortmaker@windriver.com>
---
 arch/mips/kernel/csrc-phoenix.c |    6 ++++--
 1 files changed, 4 insertions(+), 2 deletions(-)

diff --git a/arch/mips/kernel/csrc-phoenix.c b/arch/mips/kernel/csrc-phoenix.c
index b93fd0d..4c8865e 100644
--- a/arch/mips/kernel/csrc-phoenix.c
+++ b/arch/mips/kernel/csrc-phoenix.c
@@ -33,7 +33,7 @@ extern spinlock_t phnx_pic_lock;
 
 #define	PIC_TIMER6_MAX_VAL	0xffffffff
 
-cycle_t xlr_hpt_read(void)
+static cycle_t xlr_hpt_read(void)
 {
 	phoenix_reg_t *mmio = phoenix_io_mmio(PHOENIX_IO_PIC_OFFSET);
 	uint32_t counter = 0;
@@ -51,7 +51,7 @@ struct clocksource phoenix_clocksource = {
 	.flags	= CLOCK_SOURCE_IS_CONTINUOUS,
 };
 
-void __init phoenix_clocksource_init(void)
+int __init phoenix_clocksource_init(void)
 {
 	struct clocksource *cs = &phoenix_clocksource;
 	phoenix_reg_t *mmio = phoenix_io_mmio(PHOENIX_IO_PIC_OFFSET);
@@ -72,4 +72,6 @@ void __init phoenix_clocksource_init(void)
 
 	clocksource_set_clock(cs, PIC_CLKS_PER_SEC);
 	clocksource_register(cs);
+
+	return 0;
 }
-- 
1.6.3.2

