From c1a19233230e15ef38dca31a2aa4434ece79569f Mon Sep 17 00:00:00 2001
From: Kosta Zertsekel <konszert@marvell.com>
Date: Wed, 13 Mar 2013 18:15:24 +0200
Subject: [PATCH 0494/1825] ALP: minor Kconfig cleaning

https://github.com/MISL-EBU-System-SW/misl-windriver.git linux-3.4.69-14t2-read
commit 91cbaf23a27605da0fad3b41d49a341618da5568

Signed-off-by: Kosta Zertsekel <konszert@marvell.com>
Change-Id: I95a32b8526351635c37c744d6ddd66fc6e400a7f
Reviewed-on: http://vgitil04.il.marvell.com:8080/1283
Reviewed-by: Eran Ben-Avi <benavi@marvell.com>
Tested-by: Eran Ben-Avi <benavi@marvell.com>
Signed-off-by: Zhong Hongbo <hongbo.zhong@windriver.com>
---
 arch/arm/plat-armada/Kconfig |   30 ++++++++++++++++--------------
 1 files changed, 16 insertions(+), 14 deletions(-)

diff --git a/arch/arm/plat-armada/Kconfig b/arch/arm/plat-armada/Kconfig
index 499de94..86be077 100644
--- a/arch/arm/plat-armada/Kconfig
+++ b/arch/arm/plat-armada/Kconfig
@@ -179,21 +179,23 @@ config MV_INCLUDE_SWITCH
 endmenu
 
 
-menu "Armada AMP options"
+menu "AMP options"
 
 config MV_AMP_ENABLE
         bool "Enable AMP support"
-	depends on   (ARMADA_XP || (MV78XX0)) && (!ARMADA_XP_REV_Z1) && (SMP) && (!MV_ETH_BM) && (!ARMADAXP_USE_IRQ_INDIRECT_MODE)
+	depends on (ARMADA_XP || MV78XX0 || AVANTA_LP) && \
+		(!ARMADA_XP_REV_Z1) && (SMP) && (!MV_ETH_BM) && \
+		(!ARMADAXP_USE_IRQ_INDIRECT_MODE)
 	default n
 
 config MV_DRAM_BASE_G0
         hex "Image 0 - Base address of physical memory"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0x00000000
 
 config MV_DRAM_BASE_G1
         hex "Image 1 - Base address of physical memory"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0x20000000
 
 config MV_UART_PORT_G0
@@ -203,7 +205,7 @@ config MV_UART_PORT_G0
 
 config MV_UART_PORT_G1
         int "Image 1 - UART port for prints"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 1
 
 config MV_IPC_DRIVER
@@ -213,32 +215,32 @@ config MV_IPC_DRIVER
 
 config MV_IPC_NET
         bool "Enable IPC pseudo network driver"
-        depends on   MV_IPC_DRIVER
+        depends on MV_IPC_DRIVER
         default y
 
 config MV_DRAM_BASE
        hex  "AMP build usage - Do not change"
-       depends on   MV_AMP_ENABLE
+       depends on MV_AMP_ENABLE
        default 0x00000000
 
 config MV_UART_PORT
         int "AMP build usage - Do not change"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0
 
 config MV_ZREL_ADDR
 	hex "AMP build usage - Do not change"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0x00008000
 
 config MV_PARAM_PHYS
         hex "AMP build usage - Do not change"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0x00000100
 
 config MV_INITRD_PHYS
         hex "AMP build usage - Do not change"
-        depends on   MV_AMP_ENABLE
+        depends on MV_AMP_ENABLE
         default 0x00800000
 
 endmenu
@@ -262,7 +264,7 @@ config MV_ICACHE_SIZE
 	default 0x8000 if MV78XX0 || ARMADA_XP || ARMADA_370
 	default 0x4000 if MV88F6500 || MV88F6281
 
-menu "Armada SoC MTD support"
+menu "SoC MTD support"
 
 config MV_FLASH_CTRL
 	bool
@@ -319,12 +321,12 @@ config MTD_NAND_LNC_8BYTE_READ
 	  Choose this option to support NAND 8 Byte Read burst algorithm.
 
 config MTD_NAND_NFC
-	tristate "MTD driver for the Armada Nand Flash Controller"
+	tristate "MTD driver for the Nand Flash Controller"
 	depends on MTD_NAND && MV_INCLUDE_NFC
 	default y
 	help
 	  This enables the driver for the NAND flash controller found in
-	  the Marvell Armada SoC devices.
+	  the Marvell SoC devices.
 
 config MTD_NAND_NFC_GANG_SUPPORT
         bool "NAND Ganged mode support for the NFC"
-- 
1.7.5.4

