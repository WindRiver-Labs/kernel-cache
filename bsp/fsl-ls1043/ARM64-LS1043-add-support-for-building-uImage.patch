From ee482fdc98ff7c082597201e30aa83ace0946656 Mon Sep 17 00:00:00 2001
From: Xulin Sun <xulin.sun@windriver.com>
Date: Mon, 24 Aug 2015 16:03:53 +0800
Subject: [PATCH 325/451] ARM64: LS1043: add support for building uImage.

Add support for building uImage.

Signed-off-by: Xulin Sun <xulin.sun@windriver.com>
---
 arch/arm64/boot/Makefile |    5 +++++
 1 files changed, 5 insertions(+), 0 deletions(-)

diff --git a/arch/arm64/boot/Makefile b/arch/arm64/boot/Makefile
index 1373256..5115159 100644
--- a/arch/arm64/boot/Makefile
+++ b/arch/arm64/boot/Makefile
@@ -32,6 +32,11 @@ ifeq ($(CONFIG_ARCH_FSL_LS2085A), y)
   UIMAGE_ENTRYADDR = 0x080080000
 endif
 
+ifeq ($(CONFIG_ARCH_FSL_LS1043A), y)
+  UIMAGE_LOADADDR = 0x080080000
+  UIMAGE_ENTRYADDR = 0x080080000
+endif
+
 check_for_multiple_loadaddr = \
 if [ $(words $(UIMAGE_LOADADDR)) -ne 1 ]; then \
 	echo 'multiple (or no) load addresses: $(UIMAGE_LOADADDR)'; \
-- 
1.7.5.4

