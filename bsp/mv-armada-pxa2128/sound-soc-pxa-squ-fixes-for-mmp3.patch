From 047f5805004188177b25379f3c03f2c44b1a8a62 Mon Sep 17 00:00:00 2001
From: Zumeng Chen <zumeng.chen@windriver.com>
Date: Tue, 3 Sep 2013 17:04:47 +0800
Subject: [PATCH 48/60] sound: soc: pxa: squ fixes for mmp3

This patch is used to fix function interfaces for forward-porting
from vendor kernel to WRLinux 3.4.x kernel.

Signed-off-by: Zumeng Chen <zumeng.chen@windriver.com>
---
 sound/soc/pxa/mmp2-squ.c |    6 ++++--
 1 files changed, 4 insertions(+), 2 deletions(-)

diff --git a/sound/soc/pxa/mmp2-squ.c b/sound/soc/pxa/mmp2-squ.c
index a235d310..146d261 100644
--- a/sound/soc/pxa/mmp2-squ.c
+++ b/sound/soc/pxa/mmp2-squ.c
@@ -443,9 +443,11 @@ static void mmp2_pcm_free_dma_buffers(struct snd_pcm *pcm)
 
 static u64 mmp2_pcm_dmamask = DMA_32BIT_MASK;
 
-int mmp2_pcm_new(struct snd_card *card, struct snd_soc_dai *dai,
-		 struct snd_pcm *pcm)
+int mmp2_pcm_new (struct snd_soc_pcm_runtime *rtd)
 {
+	struct snd_soc_card *card = rtd->card;
+	struct snd_soc_dai *dai = rtd->codec_dai;
+	struct snd_pcm *pcm = rtd->pcm;
 	int ret = 0;
 
 	if (!card->dev->dma_mask)
-- 
1.7.5.4

