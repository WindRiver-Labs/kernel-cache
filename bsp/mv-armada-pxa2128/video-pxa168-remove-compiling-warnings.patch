From a3aa2425a9cbf5ca6bf4b237fe62f926ab12e5cd Mon Sep 17 00:00:00 2001
From: Zumeng Chen <zumeng.chen@windriver.com>
Date: Fri, 6 Sep 2013 14:47:20 +0800
Subject: [PATCH 56/60] video: pxa168: remove compiling warnings

This is used to avoid compiling warnings like the following:

drivers/video/pxa168fb.c: In function 'pxa168fb_probe':
drivers/video/pxa168fb.c:2158:5: warning: passing argument 1 of '__virt_to_phys' makes integer from pointer without a cast [enabled by default]

Signed-off-by: Zumeng Chen <zumeng.chen@windriver.com>
---
 drivers/video/pxa168fb.c |    2 +-
 1 files changed, 1 insertions(+), 1 deletions(-)

diff --git a/drivers/video/pxa168fb.c b/drivers/video/pxa168fb.c
index 5ec4ced..6a5fdf0 100644
--- a/drivers/video/pxa168fb.c
+++ b/drivers/video/pxa168fb.c
@@ -2155,7 +2155,7 @@ static int __devinit pxa168fb_probe(struct platform_device *pdev)
 			fbi->fb_start = (void *)__get_free_pages(GFP_DMA |
 				GFP_KERNEL, get_order(fbi->fb_size));
 			fbi->fb_start_dma =
-				(dma_addr_t)__virt_to_phys(fbi->fb_start);
+				(dma_addr_t)__virt_to_phys((u32)fbi->fb_start);
 		}
 
 		if (fbi->fb_start == NULL) {
-- 
1.7.5.4

