From d294cd7ea48786d5103a8620fabec6959a31417f Mon Sep 17 00:00:00 2001
From: Kevin Hao <kexin.hao@windriver.com>
Date: Wed, 28 Apr 2010 19:11:23 +0800
Subject: [PATCH 147/148] dpa: remove unneeded type check

In linux kernel we never read/write from/to register with a physical
address. And this check also cause following compile warning when we
enable "Large physical address support" on a ppc32 cpu. So just remove it.

drivers/net/dpa/NetCommSw/inc/types_ext.h:84: warning: cast to pointer
from integer of different size

Signed-off-by: Kevin Hao <kexin.hao@windriver.com>
---
 drivers/net/dpa/NetCommSw/inc/types_ext.h |    2 --
 1 files changed, 0 insertions(+), 2 deletions(-)

diff --git a/drivers/net/dpa/NetCommSw/inc/types_ext.h b/drivers/net/dpa/NetCommSw/inc/types_ext.h
index 73aaa68..bdd1723 100644
--- a/drivers/net/dpa/NetCommSw/inc/types_ext.h
+++ b/drivers/net/dpa/NetCommSw/inc/types_ext.h
@@ -74,14 +74,12 @@ static __inline__ void TypesChecker(void)
     int             on = ON, off = OFF;
     bool            tr = TRUE, fls = FALSE;
     struct strct    *p_Strct = NULL;
-    physAddress_t   addr = 0x100;
 
     on      = off;
     tr      = fls;
     p_Strct = p_Strct;
     size++;
 
-    WRITE_UINT8 (*(uint8_t  *)addr,  GET_UINT8 (*(uint8_t  *)addr ));
 
     WRITE_UINT8 (*(uint8_t  *)UINT8_MAX,  GET_UINT8 (*(uint8_t  *)UINT8_MIN ));
     WRITE_UINT16(*(uint16_t *)UINT16_MAX, GET_UINT16(*(uint16_t *)UINT16_MIN));
-- 
1.6.5.2

